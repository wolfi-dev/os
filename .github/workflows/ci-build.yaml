name: CI build action

on:
  pull_request:
    branches: ['main']

jobs:
  build:
    name: Test building of packages
    runs-on:
      group: canary-test

    steps:
      - uses: actions/checkout@v3

      - run: |
          echo "group"
