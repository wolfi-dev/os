# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/roc-toolkit/APKBUILD
package:
  name: roc-toolkit
  version: 0.4.0
  epoch: 0
  description: Real-time audio streaming over the network
  copyright:
    - license: MPL-2.0

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - libsndfile-dev
      - libunwind-dev
      - libuv-dev
      - openssl-dev
      - speexdsp-dev
      - cpputest
      - gengetopt
      - ragel
      - scons
      - cmake

pipeline:
  - uses: fetch
    with:
      expected-sha256: 60501dfdc8c0de385898dbd1eb8239d93ef962667ddb064ad796b887a41a1a46
      uri: https://github.com/roc-streaming/roc-toolkit/archive/v${{package.version}}/roc-toolkit-${{package.version}}.tar.gz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: roc-toolkit-libs
    pipeline:
      - uses: split/FIXME
    description: roc-toolkit FIXME

  - name: roc-toolkit-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - roc-toolkit
        - libsndfile-dev
        - libunwind-dev
        - libuv-dev
        - openssl-dev
        - speexdsp-dev
    description: roc-toolkit dev

  - name: roc-toolkit-doc
    pipeline:
      - uses: split/manpages
    description: roc-toolkit manpages

test:
  environment: {}
  pipeline:
    - name: Verify roc-toolkit installation, please improve the test as needed
      runs: roc-toolkit --version || exit 1
