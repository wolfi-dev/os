# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/libmbim/APKBUILD
package:
  name: libmbim
  version: 1.30.0
  epoch: 0
  description: MBIM modem protocol helper library
  copyright:
    - license: GPL-2.0-or-later AND LGPL-2.1-or-later

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - bash-completion-dev
      - glib-dev
      - gobject-introspection-dev
      - help2man
      - meson
      - py3-gobject3
      - python3

pipeline:
  - uses: fetch
    with:
      expected-sha256: cfc729d23b9bf699b23a7ef2f5d732d6eff96234e31fed36b778771a6e3d3ee5
      uri: https://gitlab.freedesktop.org/mobile-broadband/libmbim/-/archive/${{package.version}}/libmbim-${{package.version}}.tar.gz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: libmbim-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - libmbim
    description: libmbim dev

  - name: libmbim-doc
    pipeline:
      - uses: split/manpages
    description: libmbim manpages

  - name: libmbim-tools
    pipeline:
      - uses: split/FIXME
    description: libmbim FIXME

  - name: libmbim-bash-completion
    pipeline:
      - uses: split/FIXME
    description: libmbim FIXME

test:
  environment: {}
  pipeline:
    - name: Verify libmbim installation, please improve the test as needed
      runs: libmbim --version || exit 1
