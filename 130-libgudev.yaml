# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/libgudev/APKBUILD
package:
  name: libgudev
  version: "238"
  epoch: 0
  description: GObject-based wrapper library for libudev
  copyright:
    - license: LGPL-2.1-or-later

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - eudev-dev
      - meson
      - glib-dev
      - gobject-introspection-dev
      - vala

pipeline:
  - uses: fetch
    with:
      expected-sha256: 61266ab1afc9d73dbc60a8b2af73e99d2fdff47d99544d085760e4fa667b5dd1
      uri: https://download.gnome.org/sources/libgudev/${{package.version}}/libgudev-${{package.version}}.tar.xz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: libgudev-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - libgudev
        - eudev-dev
    description: libgudev dev

  - name: libgudev-dbg
    pipeline:
      - uses: split/FIXME
    description: libgudev FIXME

test:
  environment: {}
  pipeline:
    - name: Verify libgudev installation, please improve the test as needed
      runs: libgudev --version || exit 1
