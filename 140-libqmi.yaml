# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/libqmi/APKBUILD
package:
  name: libqmi
  version: 1.34.0
  epoch: 0
  description: QMI modem protocol helper library
  copyright:
    - license: GPL-2.0-or-later AND LGPL-2.1-or-later

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - bash-completion-dev
      - glib-dev
      - gobject-introspection-dev
      - help2man
      - libgudev-dev
      - libmbim-dev
      - libqrtr-glib-dev
      - linux-headers
      - meson
      - python3

pipeline:
  - uses: fetch
    with:
      expected-sha256: 8690d25b4d110b6df28b31da0a8bf16c7e966d31abcfeeb854f2753451e7a400
      uri: https://gitlab.freedesktop.org/mobile-broadband/libqmi/-/archive/${{package.version}}/libqmi-${{package.version}}.tar.gz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: libqmi-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - libqmi
    description: libqmi dev

  - name: libqmi-doc
    pipeline:
      - uses: split/manpages
    description: libqmi manpages

  - name: libqmi-bash-completion
    pipeline:
      - uses: split/FIXME
    description: libqmi FIXME

  - name: qmi-utils
    pipeline:
      - uses: split/FIXME
    description: libqmi FIXME

test:
  environment: {}
  pipeline:
    - name: Verify libqmi installation, please improve the test as needed
      runs: libqmi --version || exit 1
