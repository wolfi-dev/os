# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/modemmanager/APKBUILD
package:
  name: modemmanager
  version: 1.22.0
  epoch: 0
  description: ModemManager library
  copyright:
    - license: GPL-2.0-or-later AND LGPL-2.1-or-later

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - libmm-glib
      - bash-completion-dev
      - elogind-dev
      - gobject-introspection-dev
      - gtk-doc
      - libgudev-dev
      - libmbim-dev
      - libqmi-dev
      - linux-headers
      - meson
      - polkit-dev
      - vala
      - libmm-glib-dev
      - dbus
      - polkit

pipeline:
  - uses: fetch
    with:
      expected-sha256: 6c8f8720737a3788e394c700f36236278c9de09d76069a079e6f1daaf08b2768
      uri: https://gitlab.freedesktop.org/mobile-broadband/ModemManager/-/archive/${{package.version}}/ModemManager-${{package.version}}.tar.gz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: modemmanager-lang
    pipeline:
      - uses: split/FIXME
    description: modemmanager FIXME

  - name: modemmanager-doc
    pipeline:
      - uses: split/manpages
    description: modemmanager manpages

  - name: libmm-glib
    pipeline:
      - uses: split/FIXME
    description: modemmanager FIXME

  - name: modemmanager-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - modemmanager
        - libmm-glib
        - dbus
        - polkit
        - libmm-glib
    description: modemmanager dev

  - name: modemmanager-openrc
    pipeline:
      - uses: split/FIXME
    description: modemmanager FIXME

  - name: modemmanager-bash-completion
    pipeline:
      - uses: split/FIXME
    description: modemmanager FIXME

test:
  environment: {}
  pipeline:
    - name: Verify modemmanager installation, please improve the test as needed
      runs: modemmanager --version || exit 1
