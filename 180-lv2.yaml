# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/lv2/APKBUILD
package:
  name: lv2
  version: 1.18.10
  epoch: 0
  description: extensible open standard for audio plugins
  copyright:
    - license: ISC

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - gtk+2.0-dev
      - libsamplerate-dev
      - libsndfile-dev
      - meson
      - py3-lxml
      - py3-rdflib
      - py3-markdown
      - py3-pygments
      - python3
      - lv2-specgen-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 78c51bcf21b54e58bb6329accbb4dae03b2ed79b520f9a01e734bd9de530953f
      uri: https://lv2plug.in/spec/lv2-${{package.version}}.tar.xz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: lv2-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - lv2
        - lv2-specgen
    description: lv2 dev

  - name: lv2-specgen
    pipeline:
      - uses: split/FIXME
    description: lv2 FIXME

test:
  environment: {}
  pipeline:
    - name: Verify lv2 installation, please improve the test as needed
      runs: lv2 --version || exit 1
