# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/sord/APKBUILD
package:
  name: sord
  version: 0.16.18
  epoch: 0
  description: C library for storing RDF data in memory
  copyright:
    - license: ISC

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - meson
      - pcre2-dev
      - serd-dev
      - zix-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 4f398b635894491a4774b1498959805a08e11734c324f13d572dea695b13d3b3
      uri: https://download.drobilla.net/sord-${{package.version}}.tar.xz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: sord-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - sord
    description: sord dev

  - name: sord-libs
    pipeline:
      - uses: split/FIXME
    description: sord FIXME

  - name: sord-doc
    pipeline:
      - uses: split/manpages
    description: sord manpages

test:
  environment: {}
  pipeline:
    - name: Verify sord installation, please improve the test as needed
      runs: sord --version || exit 1
