# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/serd/APKBUILD
package:
  name: serd
  version: 0.32.4
  epoch: 0
  description: lightweight C library for RDF syntax
  copyright:
    - license: ISC

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - meson

pipeline:
  - uses: fetch
    with:
      expected-sha256: cbefb569e8db686be8c69cb3866a9538c7cb055e8f24217dd6a4471effa7d349
      uri: https://download.drobilla.net/serd-${{package.version}}.tar.xz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: serd-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - serd
    description: serd dev

  - name: serd-libs
    pipeline:
      - uses: split/FIXME
    description: serd FIXME

  - name: serd-doc
    pipeline:
      - uses: split/manpages
    description: serd manpages

test:
  environment: {}
  pipeline:
    - name: Verify serd installation, please improve the test as needed
      runs: serd --version || exit 1
