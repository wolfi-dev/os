# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/sratom/APKBUILD
package:
  name: sratom
  version: 0.6.18
  epoch: 0
  description: library for serialising LV2 atoms to and from RDF
  copyright:
    - license: ISC

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - meson
      - serd-dev
      - sord-dev
      - lv2-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 4c6a6d9e0b4d6c01cc06a8849910feceb92e666cb38779c614dd2404a9931e92
      uri: https://download.drobilla.net/sratom-${{package.version}}.tar.xz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: sratom-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - sratom
    description: sratom dev

test:
  environment: {}
  pipeline:
    - name: Verify sratom installation, please improve the test as needed
      runs: sratom --version || exit 1
