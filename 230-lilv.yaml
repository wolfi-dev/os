# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/lilv/APKBUILD
package:
  name: lilv
  version: 0.24.26
  epoch: 0
  description: C library for simpler use of LV2 plugins
  copyright:
    - license: ISC

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - libsndfile-dev
      - lv2-dev
      - meson
      - serd-dev
      - sord-dev
      - sratom-dev
      - zix-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 22feed30bc0f952384a25c2f6f4b04e6d43836408798ed65a8a934c055d5d8ac
      uri: https://download.drobilla.net/lilv-${{package.version}}.tar.xz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: lilv-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - lilv
    description: lilv dev

  - name: lilv-libs
    pipeline:
      - uses: split/FIXME
    description: lilv FIXME

  - name: py3-lilv-pyc
    pipeline:
      - uses: split/FIXME
    description: lilv FIXME

  - name: py3-lilv
    pipeline:
      - uses: split/FIXME
    description: lilv FIXME

  - name: lilv-doc
    pipeline:
      - uses: split/manpages
    description: lilv manpages

  - name: lilv-bash-completion
    pipeline:
      - uses: split/FIXME
    description: lilv FIXME

test:
  environment: {}
  pipeline:
    - name: Verify lilv installation, please improve the test as needed
      runs: lilv --version || exit 1
