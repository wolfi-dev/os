# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/libfreeaptx/APKBUILD
package:
  name: libfreeaptx
  version: 0.2.2
  epoch: 0
  description: Free implementation of Audio Processing Technology codec (aptX)
  copyright:
    - license: LGPL-2.1-or-later

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle

pipeline:
  - uses: melange/git-checkout
    with:
      expected-commit: 6dee419f934ec781e531f885f7e8e740752e67d1
      repository: https://github.com/iamthehorker/libfreeaptx
      tag: ${{package.version}}

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: libfreeaptx-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - libfreeaptx
    description: libfreeaptx dev

  - name: libfreeaptx-tools
    pipeline:
      - uses: split/FIXME
    description: libfreeaptx FIXME

update:
  enabled: true
  manual: false
  require-sequential: false
  github:
    identifier: iamthehorker/libfreeaptx

test:
  environment: {}
  pipeline:
    - name: Verify libfreeaptx installation, please improve the test as needed
      runs: libfreeaptx --version || exit 1
