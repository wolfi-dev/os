# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/openscenegraph/APKBUILD
package:
  name: openscenegraph
  version: 3.6.5
  epoch: 0
  description: High performance 3D graphics toolkit
  copyright:
    - license: LGPL-2.1-or-later

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - mesa-dev
      - cmake
      - curl-dev
      - freetype-dev
      - gdal-dev
      - giflib-dev
      - jpeg-dev
      - libxml2-dev
      - samurai
      - libsdl2-dev
      - tiff-dev
      - zlib-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: aea196550f02974d6d09291c5d83b51ca6a03b3767e234a8c0e21322927d1e12
      uri: https://github.com/openscenegraph/OpenSceneGraph/archive/OpenSceneGraph-${{package.version}}.tar.gz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: openscenegraph-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - openscenegraph
        - mesa-dev
    description: openscenegraph dev

  - name: openscenegraph-doc
    pipeline:
      - uses: split/manpages
    description: openscenegraph manpages

test:
  environment: {}
  pipeline:
    - name: Verify openscenegraph installation, please improve the test as needed
      runs: openscenegraph --version || exit 1
