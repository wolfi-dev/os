# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/fgt/APKBUILD
package:
  name: fgt
  version: 0.4.11
  epoch: 0
  description: C++ library for fast Gauss transforms.
  copyright:
    - license: LGPL-2.0-or-later

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - cmake
      - eigen-dev
      - samurai

pipeline:
  - uses: melange/git-checkout
    with:
      expected-commit: ""
      repository: https://github.com/gadomski/fgt
      tag: ${{package.version}}

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: fgt-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - fgt
    description: fgt dev

update:
  enabled: true
  manual: false
  require-sequential: false
  github:
    identifier: gadomski/fgt

test:
  environment: {}
  pipeline:
    - name: Verify fgt installation, please improve the test as needed
      runs: fgt --version || exit 1
