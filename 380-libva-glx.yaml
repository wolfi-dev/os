# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/libva-glx/APKBUILD
package:
  name: libva-glx
  version: 2.20.0
  epoch: 0
  description: Video Acceleration (VA) API for Linux with MESA support
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - libva-dev
      - mesa-dev
      - libxfixes-dev
      - meson
      - wayland-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 117f8d658a5fc9ea406ca80a3eb4ae1d70b15a54807c9ed77199c812bed73b60
      uri: https://github.com/intel/libva/archive/${{package.version}}.tar.gz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: libva-glx-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - libva-glx
        - libva-dev
        - mesa-dev
        - libxfixes-dev
    description: libva-glx dev

test:
  environment: {}
  pipeline:
    - name: Verify libva-glx installation, please improve the test as needed
      runs: libva-glx --version || exit 1
