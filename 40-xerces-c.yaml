# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/xerces-c/APKBUILD
package:
  name: xerces-c
  version: 3.2.5
  epoch: 0
  description: A validating XML parser written in a portable subset of C++
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - cmake
      - curl-dev
      - icu-dev
      - samurai

pipeline:
  - uses: fetch
    with:
      expected-sha256: 4042f6f11c9eba745dc0e5f8035d98b442097ee4efc69e4853410e4737a987f8
      uri: https://github.com/apache/xerces-c/archive/v${{package.version}}.tar.gz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: xerces-c-samples
    pipeline:
      - uses: split/FIXME
    description: xerces-c FIXME

  - name: xerces-c-doc
    pipeline:
      - uses: split/manpages
    description: xerces-c manpages

  - name: xerces-c-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - xerces-c
    description: xerces-c dev

test:
  environment: {}
  pipeline:
    - name: Verify xerces-c installation, please improve the test as needed
      runs: xerces-c --version || exit 1
