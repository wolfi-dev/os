# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/webrtc-audio-processing-2/APKBUILD
package:
  name: webrtc-audio-processing-2
  version: "2.1"
  epoch: 0
  description: AudioProcessing module from the WebRTC project
  copyright:
    - license: custom

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - abseil-cpp-dev
      - meson
      - cmake
      - libtool
      - linux-headers

pipeline:
  - uses: fetch
    with:
      expected-sha256: 2358883a8551711f3b6169f6799c003dc1a6efd37bdb24a135a015cc9e3d7713
      uri: https://gitlab.freedesktop.org/pulseaudio/webrtc-audio-processing/-/archive/v${{package.version}}/webrtc-audio-processing-v${{package.version}}.tar.gz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: webrtc-audio-processing-2-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - webrtc-audio-processing-2
    description: webrtc-audio-processing-2 dev

test:
  environment: {}
  pipeline:
    - name: Verify webrtc-audio-processing-2 installation, please improve the test as needed
      runs: webrtc-audio-processing-2 --version || exit 1
