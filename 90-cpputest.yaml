# Generated from https://gitlab.alpinelinux.org/alpine/aports/-/raw/master/community/cpputest/APKBUILD
package:
  name: cpputest
  version: "4.0"
  epoch: 0
  description: A unit testing and mocking framework for C/C++
  copyright:
    - license: BSD-3-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - cmake
      - samurai

pipeline:
  - uses: fetch
    with:
      expected-sha256: 21c692105db15299b5529af81a11a7ad80397f92c122bd7bf1e4a4b0e85654f7
      uri: https://github.com/cpputest/cpputest/releases/download/v${{package.version}}/cpputest-${{package.version}}.tar.gz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

test:
  environment: {}
  pipeline:
    - name: Verify cpputest installation, please improve the test as needed
      runs: cpputest --version || exit 1
