package:
  name: Catch2
  version: "3.16.0"
  epoch: 0 # add test coverage
  description: "A modern, C++-native, test framework"
  copyright:
    - license: 'BSL-1.0'
  resources:
    cpu: "1"
    memory: 1Gi
  test-resources:
    cpu: "1"
    memory: 1Gi

environment:
  contents:
    packages:
      - build-base
      - busybox

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/catchorg/Catch2
      tag: v${{package.version}}
      expected-commit: 317ac1ed4c0bb6e6b91eafc817e05c488feffcb3

  - uses: cmake/configure

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

subpackages:
  - name: Catch2-static
    description: "A modern, C++-native, test framework - static libraries"
    pipeline:
      - uses: split/static
    test:
      pipeline:
        - uses: test/tw/staticpackage

  - name: Catch2-dev
    description: "A modern, C++-native, test framework - development headers"
    pipeline:
      - uses: split/dev
      - runs: |
          mkdir -p "${{targets.subpkgdir}}"/usr/share
          mv "${{targets.destdir}}"/usr/share/Catch2 "${{targets.subpkgdir}}"/usr/share/
    test:
      pipeline:
        - uses: test/pkgconf

test:
  environment:
    contents:
      packages:
        - Catch2-dev
        - Catch2-static
        - build-base
        - pkgconf
  pipeline:
    - uses: test/tw/ldd-check
    - name: Compile and run a Catch2 test program
      runs: |
        cat > /tmp/test_catch2.cpp << 'EOF'
        #define CATCH_CONFIG_MAIN
        #include <catch2/catch_all.hpp>

        TEST_CASE("basic arithmetic", "[math]") {
            REQUIRE(1 + 1 == 2);
            REQUIRE(10 - 3 == 7);
        }

        TEST_CASE("string operations", "[string]") {
            std::string s = "hello";
            REQUIRE(s.size() == 5);
            REQUIRE(s == "hello");
        }
        EOF
        c++ -std=c++14 /tmp/test_catch2.cpp \
          $(pkg-config --cflags catch2-with-main) \
          $(pkg-config --libs catch2-with-main) \
          -o /tmp/test_catch2
        /tmp/test_catch2

update:
  enabled: true
  github:
    identifier: catchorg/Catch2
    strip-prefix: v
