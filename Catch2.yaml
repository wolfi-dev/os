package:
  name: Catch2
  version: "3.15.0"
  epoch: 1 # go/wolfi-rsc/Catch2
  description: "A modern, C++-native, test framework"
  copyright:
    - license: 'BSL-1.0'
  resources:
    cpu: "1"
    memory: 1Gi
  test-resources:
    cpu: "1"
    memory: 1Gi

environment:
  contents:
    packages:
      - build-base
      - busybox

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/catchorg/Catch2
      tag: v${{package.version}}
      expected-commit: 6ee0826dcae55ed1e06b2c5701981221e979e1e6

  - uses: cmake/configure

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

subpackages:
  - name: Catch2-static
    description: "A modern, C++-native, test framework - static libraries"
    pipeline:
      - uses: split/static
    test:
      pipeline:
        - uses: test/tw/staticpackage

  - name: Catch2-dev
    description: "A modern, C++-native, test framework - development headers"
    pipeline:
      - uses: split/dev
      - runs: |
          mkdir -p "${{targets.subpkgdir}}"/usr/share
          mv "${{targets.destdir}}"/usr/share/Catch2 "${{targets.subpkgdir}}"/usr/share/
    test:
      pipeline:
        - uses: test/pkgconf

test:
  pipeline:
    - uses: test/tw/ldd-check

update:
  enabled: true
  github:
    identifier: catchorg/Catch2
    strip-prefix: v
