package:
  name: act
  version: "0.2.87"
  epoch: 0
  description: Run your GitHub Actions locally
  copyright:
    - license: MIT
  dependencies: {}
  checks: {}
  cpe: {}

capabilities: {}

environment:
  contents:
    packages:
      - go

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 6ee387ebd0426e57223315c47b0299a721041df8
      repository: https://github.com/nektos/act
      tag: v${{package.version}}

  - uses: go/bump
    with:
      deps: |-
        github.com/go-git/go-git/v5@v5.17.1

  - uses: go/build
    with:
      output: act
      packages: .

  - uses: strip

update:
  enabled: true
  github:
    identifier: nektos/act
    strip-prefix: v

test:
  environment: {}
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/tw/ver-check
      with:
        bins: act
        version: ${{package.version}}
    - uses: test/tw/help-check
      with:
        bins: act
