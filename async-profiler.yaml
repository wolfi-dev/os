package:
  name: async-profiler
  version: "4.3"
  epoch: 1 # go/wolfi-rsc/async-profiler
  description: A low overhead sampling profiler for Java
  copyright:
    - license: Apache-2.0
  resources:
    cpu: "1"
    memory: 1Gi

environment:
  contents:
    packages:
      - bash
      - build-base
      - openjdk-11-default-jdk
      - wolfi-base
  environment:
    JAVA_HOME: /usr/lib/jvm/java-11-openjdk

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/async-profiler/async-profiler
      tag: v${{package.version}}
      expected-commit: 499904dce79a3b4ae9711fd5bae0da933fee7312

  - runs: |
      make all

  - runs: |
      mkdir -p ${{targets.destdir}}/usr/bin ${{targets.destdir}}/usr/lib
      mv build/bin/* ${{targets.destdir}}/usr/bin
      mv build/lib/* ${{targets.destdir}}/usr/lib

test:
  pipeline:
    - runs: |
        /usr/bin/asprof --help
        asprof --version
        asprof --help
    - uses: test/tw/ldd-check

update:
  enabled: true
  github:
    identifier: async-profiler/async-profiler
    strip-prefix: v
