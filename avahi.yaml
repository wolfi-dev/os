package:
  name: avahi
  version: 0.9_rc2
  epoch: 1
  description: A multicast/unicast DNS-SD framework
  copyright:
    - license: LGPL-2.1
  dependencies:
    runtime:
      - merged-usrsbin
      - wolfi-baselayout

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - dbus-dev
      - expat-dev
      - gdbm-dev
      - gettext-dev
      - glib-dev
      - glib-gir
      - gobject-introspection-dev
      - gtk-2.0-dev
      - intltool
      - libcap-dev
      - libdaemon-dev
      - libevent-dev
      - libtool
      - pkgconf-dev
      - systemd-dev

# we only need this because of the rc tag, once we're not using that we can remove this!
var-transforms:
  - from: ${{package.version}}
    match: _rc
    replace: -rc
    to: mangled-package-version

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/avahi/avahi
      tag: v${{vars.mangled-package-version}}
      expected-commit: 1dade81cbf4d3e3f49784e44e543cd77046c79df
      cherry-picks: |
        master/2d48e42d44a183f26a4d12d1f5d41abb9b7c6355: core: refuse to create wide-area record browsers when wide-area is off # GHSA-mhf3-865v-g5rc
        master/f66be13d7f31a3ef806d226bf8b67240179d309a: core: fix DoS bug by removing incorrect assertion # GHSA-cp79-r4x9-vf52
        master/9c6eb53bf2e290aed84b1f207e3ce35c54cc0aa1: core: fix DoS bug by changing assert to return # GHSA-56rf-42xr-qmmg

  - runs: |
      autoreconf -fi

  - uses: autoconf/configure
    with:
      opts: |
        --prefix=/usr \
        --sbindir=/usr/bin \
        --localstatedir=/var \
        --with-distro=none \
        --disable-qt3 \
        --disable-qt4 \
        --disable-qt5 \
        --disable-gtk3 \
        --disable-python \
        --disable-mono \
        --disable-monodoc \
        --disable-manpages # TODO: manpages requires xmltoman, which we haven't packaged yet.

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: avahi-dev
    pipeline:
      - uses: split/dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check
    dependencies:
      runtime:
        - merged-usrsbin
        - wolfi-baselayout

update:
  enabled: true
  github:
    identifier: avahi/avahi
    strip-prefix: v
    use-tag: true

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        avahi-browse --version
        avahi-browse-domains --version
        avahi-publish --version
        avahi-publish-address --version
        avahi-publish-service --version
        avahi-resolve --version
        avahi-resolve-address --version
        avahi-resolve-host-name --version
        avahi-set-host-name --version
        avahi-autoipd --version
        avahi-daemon --version
        avahi-dnsconfd --version
        avahi-browse --help
        avahi-browse-domains --help
        avahi-publish --help
        avahi-publish-address --help
        avahi-publish-service --help
        avahi-resolve --help
        avahi-resolve-address --help
        avahi-resolve-host-name --help
        avahi-set-host-name --help
        avahi-autoipd --help
        avahi-daemon --help
        avahi-dnsconfd --help
    - uses: test/tw/ldd-check
