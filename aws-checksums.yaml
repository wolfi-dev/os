package:
  name: aws-checksums
  version: "0.2.10"
  epoch: 0 # go/wolfi-rsc/aws-checksums
  description: AWS Cross-Platform HW accelerated CRC32c and CRC32 with fallback to efficient SW implementations
  copyright:
    - license: Apache-2.0
  resources:
    cpu: "2"
    memory: 4Gi

environment:
  contents:
    packages:
      - aws-c-common-dev
      - build-base
      - busybox
      - ca-certificates-bundle
      - cmake
      - samurai

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 1d5f2f1f3e5d013aae8810878ceb5b3f6f258c4e
      repository: https://github.com/awslabs/aws-checksums
      tag: v${{package.version}}

  - uses: cmake/configure
    with:
      opts: |
        -DBUILD_SHARED_LIBS=True \
        -DCMAKE_BUILD_TYPE=None \
        -DBUILD_TESTING=OFF \
        -DCMAKE_SYSTEM_NAME=Linux \
        -DCMAKE_HOST_SYSTEM_NAME=Linux

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

subpackages:
  - name: aws-checksums-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - aws-checksums
    description: aws-checksums dev
    test:
      pipeline:
        - uses: test/tw/ldd-check

test:
  pipeline:
    - uses: test/tw/ldd-check

update:
  enabled: true
  github:
    identifier: awslabs/aws-checksums
    strip-prefix: v
    use-tag: true
