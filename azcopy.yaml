package:
  name: azcopy
  version: "10.32.8"
  epoch: 2 # toolchain rebuild for  go-1.27-1.27.1
  description: The new Azure Storage data transfer utility
  copyright:
    - license: MIT
  resources:
    cpu: "1"
    memory: 5Gi
  test-resources:
    cpu: "1"
    memory: 1Gi

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/Azure/azure-storage-azcopy
      tag: v${{package.version}}
      expected-commit: 9dd78ee704c0daf47cc3cdfcbbd41894a4211198

  - uses: go/build
    with:
      packages: .
      output: azcopy
      go-package: go-1.27

  - uses: strip

update:
  enabled: true
  github:
    identifier: Azure/azure-storage-azcopy
    strip-prefix: v

test:
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/tw/ver-check
      with:
        bins: azcopy
    - uses: test/tw/help-check
      with:
        bins: azcopy
        expect-contains: copy sync list bench
    - name: Verify env subcommand
      runs: |
        # azcopy env lists supported environment variables
        azcopy env 2>&1 | grep -Fi "AZCOPY"

environment:
  contents:
    packages:
      - glibc-2.44-dev
      - go-1.27
