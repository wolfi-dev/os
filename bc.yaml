# Generated from https://git.alpinelinux.org/aports/plain/main/bc/APKBUILD
package:
  name: bc
  version: 1.08.1
  epoch: 0
  description: An arbitrary precision numeric processing language (calculator)
  copyright:
    - license: GPL-3.0-or-later

environment:
  contents:
    packages:
      - autoconf
      - automake
      - bison
      - build-base
      - busybox
      - ca-certificates-bundle
      - ed
      - flex
      - readline-dev
      - texinfo

pipeline:
  - uses: fetch
    with:
      expected-sha256: b71457ffeb210d7ea61825ff72b3e49dc8f2c1a04102bbe23591d783d1bfe996
      uri: https://ftp.gnu.org/gnu/bc/bc-${{package.version}}.tar.gz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: bc-doc
    pipeline:
      - uses: split/manpages
      - uses: split/infodir
    description: bc manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 170

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        bc --version
        dc --version
        bc --help
        dc --help
