package:
  name: berg
  version: "0.4.11"
  epoch: 0
  description: "CLI Tool for Codeberg similar to gh and glab"
  copyright:
    - license: AGPL-3.0-only

environment:
  contents:
    packages:
      - build-base
      - cargo-auditable
      - openssl-dev
      - rust
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://codeberg.org/RobWalt/codeberg-cli/
      tag: v${{package.version}}
      expected-commit: 16c951b783741ccf8ec8ef8c8a026cafdbfe4831
      cherry-picks: |
        main/888b2d23503e7dbb9323d40b6de0c3e6fa401292: revert rust 2024 edition

  - uses: rust/cargobump

  - name: Configure and build
    runs: |
      mkdir -p "${{targets.destdir}}"/usr/bin
      cargo auditable build --all-features --release
      install -m755 target/release/berg "${{targets.destdir}}"/usr/bin/

  - uses: strip

update:
  enabled: true
  git:
    strip-prefix: v

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        berg --version
        berg --help
