package:
  name: biome
  version: "2.4.9"
  epoch: 0
  description: Fast formatter, linter, and checker for JavaScript, TypeScript, CSS, HTML, GraphQL, and more
  copyright:
    - license: Apache-2.0
  resources:
    cpu: "19"
    memory: 24Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - cargo-auditable
      - rust-1.94

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/biomejs/biome
      tag: "@biomejs/biome@${{package.version}}"
      expected-commit: ad3752696c34f0ad30311a2a70de1f9b18b31530

  # mitigate CVE-2024-43806 GHSA-c827-hfw6-qwvm
  - uses: rust/cargobump

  - uses: cargo/build
    with:
      output: biome
      opts: --release -p biome_cli
    environment:
      BIOME_VERSION: ${{package.version}}

  - uses: strip

update:
  enabled: true
  ignore-regex-patterns:
    - "-rc"
    - "-beta"
  github:
    identifier: biomejs/biome
    use-tag: true
    strip-prefix: "@biomejs/biome@"

test:
  pipeline:
    - uses: test/tw/ver-check
      with:
        bins: biome
        version: ${{package.version}}
    - uses: test/tw/help-check
      with:
        bins: biome
    - name: Test biome format
      runs: |
        set -euo pipefail
        echo 'const x = 1;' > /tmp/test.js
        biome format /tmp/test.js
    - name: Test biome lint
      runs: |
        set -euo pipefail
        echo 'const x = 1;' > /tmp/test.js
        biome lint /tmp/test.js
    - name: Test biome check
      runs: |
        set -euo pipefail
        echo 'const x = 1;' > /tmp/test.js
        biome check /tmp/test.js
    - name: Test biome ci
      runs: |
        set -euo pipefail
        echo 'const x = 1;' > /tmp/test.js
        biome format --write /tmp/test.js
        biome ci /tmp/test.js
    - uses: test/tw/ldd-check
