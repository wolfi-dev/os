package:
  name: biome
  version: "2.4.7"
  epoch: 0
  description: Fast formatter, linter, and checker for JavaScript, TypeScript, CSS, HTML, GraphQL, and more
  copyright:
    - license: Apache-2.0
  resources:
    cpu: "19"
    memory: 24Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - cargo-auditable
      - rust

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/biomejs/biome
      tag: "@biomejs/biome@${{package.version}}"
      expected-commit: 1f30838b288bf317bf88c9247da029d9a394d009

  # mitigate CVE-2024-43806 GHSA-c827-hfw6-qwvm
  - uses: rust/cargobump

  - runs: |
      cargo auditable build --release -p biome_cli
      install -Dm755 target/release/biome "${{targets.destdir}}"/usr/bin/biome

  - uses: strip

update:
  enabled: true
  ignore-regex-patterns:
    - "-rc"
    - "-beta"
  github:
    identifier: biomejs/biome
    use-tag: true
    strip-prefix: "@biomejs/biome@"

test:
  pipeline:
    - name: Version and help check
      runs: |
        set -euo pipefail
        biome --version
        biome --help
    - name: Test biome format
      runs: |
        set -euo pipefail
        echo 'const x = 1;' > /tmp/test.js
        biome format /tmp/test.js
    - name: Test biome lint
      runs: |
        set -euo pipefail
        echo 'const x = 1;' > /tmp/test.js
        biome lint /tmp/test.js
    - name: Test biome check
      runs: |
        set -euo pipefail
        echo 'const x = 1;' > /tmp/test.js
        biome check /tmp/test.js
    - name: Test biome ci
      runs: |
        set -euo pipefail
        echo 'const x = 1;' > /tmp/test.js
        biome format --write /tmp/test.js
        biome ci /tmp/test.js
    - uses: test/tw/ldd-check
