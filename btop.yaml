package:
  name: btop
  version: "1.4.2"
  epoch: 0
  description: Resource monitor
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 274d0c78e5f18514dfbea23cee9d1c5431eb75e0
      repository: https://github.com/aristocratos/btop
      tag: v${{package.version}}

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

update:
  enabled: true
  github:
    identifier: aristocratos/btop
    strip-prefix: v

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        btop --version
        btop --help
