# Generated from https://git.alpinelinux.org/aports/plain/community/btop/APKBUILD
package:
  name: btop
  version: 1.3.2
  epoch: 1
  description: Resource monitor
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle

pipeline:
  - uses: git-checkout
    with:
      expected-commit:
      repository: https://github.com/aristocratos/btop
      tag: v${{package.version}}/btop-${{package.version}}

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

update:
  enabled: true
  release-monitor:
    identifier: 253331
    strip-prefix: v
