package:
  name: build-base
  version: 1
  epoch: 3
  description: "virtual package for builds"
  target-architecture:
    - all
  copyright:
    - paths:
        - "*"
      attestation: TODO
      license: MIT
  dependencies:
    runtime:
      - wolfi-baselayout
      - binutils
      - gcc
      - pkgconf
      - make
      - glibc-dev
environment:
  contents:
    packages:
      - wolfi-baselayout
      - busybox
pipeline:
  - name: Install
    runs: |
      mkdir -p "${{targets.destdir}}"
