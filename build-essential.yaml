package:
  name: build-essential
  url: https://packages.debian.org/sid/build-essential
  version: 12.12
  epoch: 0
  description: "virtual compat package for build-base"
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - build-base

update:
  enabled: false
  exclude-reason: Native package in debian, meaning manual decisions needed for compat runtime dependencies.
