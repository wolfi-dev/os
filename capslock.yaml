package:
  name: capslock
  version: "0.3.1"
  epoch: 0 # CVE-2025-61725
  description: Capslock is a capability analysis CLI for Go packages that informs users of which privileged operations a given package can access
  copyright:
    - license: BSD-3-Clause

environment:
  contents:
    packages:
      - ca-certificates-bundle
      - go

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/google/capslock
      tag: v${{package.version}}
      expected-commit: 39f66c672864ed85c701720007496ab350f4b9ca

  - uses: go/build
    with:
      packages: ./cmd/capslock
      output: capslock

update:
  enabled: true
  github:
    identifier: google/capslock
    strip-prefix: v
    tag-filter: v
    use-tag: true

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        capslock --help
