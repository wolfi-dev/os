package:
  name: ccze
  version: "0.3.0"
  epoch: 0
  description: "fast log colorizer"
  copyright:
    - license: GPL-2.0-only

environment:
  contents:
    packages:
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - ncurses-dev
      - pcre-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/software-revive/ccze-rv
      tag: ${{package.version}}
      expected-commit: c920f0139f8ac611633a737e8089e17f2b9d8486

  # Update config.guess/config.sub for aarch64 support (bundled ones are from 2002)
  - runs: |
      cp /usr/share/automake-*/config.guess .
      cp /usr/share/automake-*/config.sub .

  - runs: |
      ./configure \
        --prefix=/usr \
        --with-builtins=all

  - runs: |
      make

  - runs: |
      make install DESTDIR="${{targets.destdir}}"

  - uses: strip

subpackages:
  - name: ccze-doc
    description: ccze documentation
    pipeline:
      - uses: split/manpages
    test:
      pipeline:
        - uses: test/tw/docs

update:
  enabled: true
  github:
    identifier: software-revive/ccze-rv

test:
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/tw/help-check
      with:
        bins: ccze
        help-flag: --help
    - uses: test/tw/ver-check
      with:
        bins: ccze
        version: ${{package.version}}
    - runs: |
        set -euo pipefail
        # Verify binary exists
        stat /usr/bin/ccze
        # Test basic colorization with a syslog-style line
        echo "Jan  1 00:00:00 host syslog: test message" | ccze -A | grep -F "test message"
