package:
  name: certificate-transparency
  version: "1.3.3"
  epoch: 3
  description: Auditing for TLS certificates
  copyright:
    - license: Apache-2.0
      paths:
        - "*"
  checks:
    disabled:
      - empty
  resources:
    cpu: "2"
    memory: 2Gi

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - git
      - go

data:
  - name: components
    items:
      # There are a lot of binaries we may want to build out of this
      # repo, but start with what we need for sigstore.
      trillian-ctserver: ./trillian/ctfe/ct_server

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/google/certificate-transparency-go
      tag: v${{package.version}}
      expected-commit: e8f93173135c7817ebd7133dab729c4576ce9a21

  - uses: go/bump
    with:
      deps: |-
        google.golang.org/grpc@v1.79.3

subpackages:
  - range: components
    name: "${{package.name}}-${{range.key}}"
    pipeline:
      - uses: go/build
        with:
          packages: ${{range.value}}
          output: ${{range.key}}
    test:
      pipeline:
        - uses: test/tw/help-check
          with:
            bins: trillian-ctserver
        - name: Verify trillian-ctserver daemon startup
          uses: test/daemon-check-output
          with:
            start: /usr/bin/trillian-ctserver
            expected_output: "."
            timeout: "15"
            error_strings: ""

update:
  enabled: true
  github:
    identifier: google/certificate-transparency-go
    strip-prefix: v

# Based on package size if was determined that this origin package is empty apart from its own SBOM and this test was added to confirm it is empty and will fail if the package is not longer empty (contains more than an SBOM)
test:
  pipeline:
    - uses: test/emptypackage
