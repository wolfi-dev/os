package:
  name: cmake-bootstrap
  version: "4.2.3"
  epoch: 1 # go/wolfi-rsc/cmake-bootstrap
  description: "Bootstrap CMake without using system libraries, enabling to use CMake to build system libraries used by CMake"
  dependencies:
    provider-priority: 5
    provides:
      - cmake=${{package.full-version}}
  copyright:
    - license: BSD-3-Clause
  resources:
    cpu: "23"
    memory: 27Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - openssl-dev
      - samurai

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/Kitware/CMake
      tag: v${{package.version}}
      expected-commit: 53ae242d55f89bee1f44ecc6e2fc460ae0f92f20

  # cmake-bootstrap uses all vendored deps only, such that we can
  # build them with cmake-bootstrap, before building the full cmake
  - runs: |
      ./bootstrap \
        --prefix=/usr \
        --mandir=/share/man \
        --datadir=/share/cmake \
        --docdir=/share/doc/cmake \
        --no-system-libs \
        --generator=Ninja \
        --parallel=$(nproc)

  - runs: |
      ninja

  - runs: |
      DESTDIR="${{targets.destdir}}" ninja install

  - uses: strip

update:
  enabled: true
  manual: true # be careful upgrading cmake as it is a core package
  github:
    identifier: Kitware/CMake
    strip-prefix: v

test:
  environment:
    contents:
      packages:
        - make
        - gcc
        - autoconf
        - automake
        - build-base
  pipeline:
    - uses: test/tw/ldd-check
    - working-directory: wolfi-tests
      runs: |
        cmake --version
        cmake --help
        cpack --version
        cpack --help
        ctest --version
        ctest --help
    - working-directory: wolfi-tests
      runs: |
        mkdir -p build
        cd build
        cmake ..
        make
        ./hello_wolfi
