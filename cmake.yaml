package:
  name: cmake
  version: 3.29.6
  epoch: 0
  description: "CMake is an open-source, cross-platform family of tools designed to build, test and package software"
  copyright:
    - license: BSD-3-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - bzip2-dev
      - ca-certificates-bundle
      - expat-dev
      - libarchive-dev
      - libuv-dev
      - ncurses-dev
      - nghttp2-dev
      - openssl-dev
      - rhash-dev
      - samurai
      - wolfi-base
      - xz-dev
      - zlib-dev

pipeline:
  - uses: fetch
    with:
      uri: https://www.cmake.org/files/v3.29/cmake-${{package.version}}.tar.gz
      expected-sha256: 1391313003b83d48e2ab115a8b525a557f78d8c1544618b48d1d90184a10f0af

  # Depending on system cppdap, jsoncpp, and curl would create a circular
  # dependency; thus, we use bundled ones.
  - runs: |
      ./bootstrap \
        --prefix=/usr \
        --mandir=/share/man \
        --datadir=/share/cmake \
        --docdir=/share/doc/cmake \
        --system-libs \
        --no-system-cppdap \
        --no-system-jsoncpp \
        --no-system-curl \
        --generator=Ninja \
        --parallel=$(nproc)

  - runs: |
      ninja

  - runs: |
      DESTDIR="${{targets.destdir}}" ninja install

  - uses: strip

update:
  enabled: true
  manual: true # be careful upgrading cmake as it is a core package
  release-monitor:
    identifier: 306

test:
  environment:
    contents:
      packages:
        - wolfi-base
        - make
        - gcc
        - autoconf
        - automake
        - build-base
  pipeline:
    - working-directory: wolfi-tests
      runs: |
        cmake --version
    - working-directory: wolfi-tests
      runs: |
        mkdir -p build
        cd build
        cmake ..
        make
        ./hello_wolfi
