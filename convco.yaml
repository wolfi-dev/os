package:
  name: convco
  version: "0.6.4"
  epoch: 2 # go/wolfi-rsc/convco
  description: A Conventional commit cli
  copyright:
    - license: MIT
  resources:
    cpu: "2"
    memory: 6Gi
  test-resources:
    cpu: "1"
    memory: 1Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates
      - cargo-auditable
      - cmake
      - rust-1.95

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/convco/convco
      tag: v${{package.version}}
      expected-commit: 7a47882d1b71b5fd502e5e5d4837be5ab774281c

  - uses: bump
    with:
      tidy: false

  - uses: cargo/build
    with:
      output: convco

  - uses: strip

update:
  enabled: true
  github:
    identifier: convco/convco
    strip-prefix: v

test:
  environment:
    contents:
      packages:
        - git
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/tw/ver-check
      with:
        bins: convco
    - uses: test/tw/help-check
      with:
        bins: convco
    - name: Test convco with conventional commits
      runs: |
        set -euo pipefail

        # Setup: Initialize a git repository with conventional commits
        mkdir -p /tmp/test-repo
        cd /tmp/test-repo
        git init
        git config user.email "test@test.com"
        git config user.name "Test User"

        # Create an initial conventional commit
        echo "initial" > file.txt
        git add file.txt
        git commit -m "feat: initial feature"

        # Create a fix commit
        echo "fix" >> file.txt
        git add file.txt
        git commit -m "fix: resolve a bug"

        # Test: convco check validates commit messages
        convco check

        # Test: convco changelog generates output
        changelog=$(convco changelog)
        echo "$changelog" | grep -F "initial feature"
        echo "$changelog" | grep -F "resolve a bug"

        # Test: convco version computes a semantic version
        version=$(convco version)
        echo "$version" | grep -E "^[0-9]+\.[0-9]+\.[0-9]+$"

        echo "SUCCESS: convco functional tests passed"
