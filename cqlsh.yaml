package:
  name: cqlsh
  version: 4.1.4
  epoch: 1
  description: CQL shell for apache cassandra
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - cython
      - py3-setuptools
      - python3
      - python3-dev

pipeline:
  - uses: git-checkout
    with:
      expected-commit: e7c2a5c1cb2d2807e5bbdb688ed0c61df9b10440
      repository: https://github.com/apache/cassandra
      tag: cassandra-${{package.version}}

  - runs: |
      (cd ./pylib && python setup.py build)

      mkdir -p ${{targets.destdir}}/usr/bin
      cp ./bin/cqlsh ${{targets.destdir}}/usr/bin/cqlsh
      cp ./bin/cqlsh.py ${{targets.destdir}}/usr/bin/cqlsh
      (cd ./pylib &&  python setup.py install --prefix=/usr --root="${{targets.destdir}}")

  - uses: strip

update:
  enabled: true
  ignore-regex-patterns:
    - -tentative
  github:
    identifier: apache/cassandra
    strip-prefix: cassandra-
    use-tag: true
