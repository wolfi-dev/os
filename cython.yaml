package:
  name: cython
  version: 3.0.1
  epoch: 0
  description: Cython is an optimising static compiler for both the Python & the extended Cython programming languages.
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - build-base
      - py3-setuptools
      - python3-dev
      - python3

pipeline:
  - uses: fetch
    with:
      expected-sha256: 8a142815f6483a45065fa2698ee2119bfe097c7a6e292d0da3d223d15d4a73bb
      uri: https://github.com/cython/cython/archive/${{package.version}}.tar.gz

  - runs: |
      python setup.py build
      python setup.py install --prefix=/usr --root="${{targets.destdir}}"

  - uses: strip

update:
  enabled: true
  github:
    identifier: cython/cython
