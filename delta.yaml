package:
  name: delta
  version: "0.19.1"
  epoch: 0 # go/wolfi-rsc/delta
  description: Syntax-highlighting pager for git and diff output
  copyright:
    - license: MIT
  resources:
    cpu: "9"
    memory: 10Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - rust

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/dandavison/delta
      tag: ${{package.version}}
      expected-commit: 12ef3ef0be4aebe0d2d2c810a426dbf314efa495

  - uses: rust/cargobump

  - runs: |
      cargo build --release
      install -Dm755 target/release/delta "${{targets.destdir}}"/usr/bin/delta

  - uses: strip

test:
  pipeline:
    - runs: |
        delta --version
        delta --help

update:
  enabled: true
  github:
    identifier: dandavison/delta
