package:
  name: deno
  version: "2.8.2"
  epoch: 0 # GHSA-phqj-4mhp-q6mq
  description: "A modern runtime for JavaScript and TypeScript."
  copyright:
    - license: MIT
  resources:
    cpu: "22"
    memory: 36Gi
  test-resources:
    cpu: "1"

environment:
  contents:
    packages:
      - bash
      - build-base
      - busybox
      - ca-certificates-bundle
      - cargo-auditable
      - clang-${{vars.clang-version}}
      - cmake
      - glibc-dev
      - posix-libc-utils
      - protobuf-dev
      - protoc
      - python3
      - rust-1.95
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/denoland/deno
      tag: v${{package.version}}
      expected-commit: 085be0b5ff8ca3ccfe58318afe0939c4d4af9743

  - uses: patch
    with:
      patches: rand-pin-bump.patch

  - uses: rust/cargobump

  - runs: |
      cargo update rand@0.9.2 --precise 0.9.3

  - uses: cargo/build
    with:
      output: deno

  - uses: strip

update:
  enabled: true
  github:
    identifier: denoland/deno
    strip-prefix: v

test:
  pipeline:
    - runs: |
        deno --version
        echo 'console.log("Hello, World!");' > hello_world.js
        output=$(deno run hello_world.js)
        if [ "$output" = "Hello, World!" ]; then
          echo "Test passed!"
        else
          echo "Test failed!"
          exit 1
        fi
        deno --help

vars:
  clang-version: 22
