package:
  name: dieharder
  version: 3.31.1.4
  epoch: 6
  description: Random-number generator test front-end
  copyright:
    - license: GPL-2.0-or-later
  resources:
    cpu: "1"
    memory: 5Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - gsl-dev
      - libtool

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 24535c800b8f5a835dccea6fb067ceb94a72f0f3
      repository: https://salsa.debian.org/edd/dieharder
      tag: upstream/${{package.version}}

  - name: Run autoreconf
    runs: |
      autoreconf -vfi

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: dieharder-doc
    pipeline:
      - uses: split/manpages
    description: dieharder manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  ignore-regex-patterns:
    - _ # exclude tags with embedded dates (e.g. upstream/3.31.1_20110601)
  git:
    strip-prefix: upstream/
    tag-filter-prefix: upstream/

test:
  pipeline:
    - uses: test/tw/ldd-check
    - runs: |
        dieharder --version
        dieharder --help
    - runs: |
        # Run an actual statistical test using a built-in PRNG generator.
        # -g 1 = George Marsaglia's KISS generator (built-in, no external input needed)
        # -d 0 = diehard_birthdays test
        # -p 1 = 1 p-value (fast single run)
        output=$(dieharder -g 1 -d 0 -p 1)
        echo "$output"
        # Verify the output contains expected result fields
        echo "$output" | grep -q "diehard_birthdays"
        echo "$output" | grep -q "p-value"
    - uses: test/no-docs
