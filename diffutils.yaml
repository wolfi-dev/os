package:
  name: diffutils
  version: "3.11"
  epoch: 0
  description: "GNU diff utilities"
  copyright:
    - license: GPL-3.0-or-later

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - wolfi-base

pipeline:
  - uses: fetch
    with:
      uri: https://ftp.gnu.org/gnu/diffutils/diffutils-${{package.version}}.tar.xz
      expected-sha256: a73ef05fe37dd585f7d87068e4a0639760419f810138bd75c61ddaa1f9e2131e

  - runs: |
      env FORCE_UNSAFE_CONFIGURE=1 ./configure \
         --prefix=/usr \
         --libdir=/lib \
         --libexecdir=/usr/libexec \
         --sysconfdir=/etc \
         --mandir=/usr/share/man \
         --infodir=/usr/share/info \
         --disable-nls

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: "diffutils-doc"
    description: "documentation for GNU coreutils"
    pipeline:
      - uses: split/manpages
      - uses: split/infodir
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 436

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        cmp --version
        diff --version
        diff3 --version
        sdiff --version
        cmp --help
        diff --help
        diff3 --help
        sdiff --help
