package:
  name: doctest
  version: "2.5.0"
  epoch: 1 # go/wolfi-rsc/doctest
  description: C++11/14/17/20/23 single-header testing framework
  copyright:
    - license: MIT
  resources:
    cpu: "1"
    memory: 2Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - make

pipeline:
  - uses: git-checkout
    with:
      expected-commit: d44d4f6e66232d716af82f00a063759e9d0e50d6
      repository: https://github.com/doctest/doctest
      tag: v${{package.version}}

  - uses: cmake/configure
    with:
      opts: |
        -DCMAKE_INSTALL_PREFIX=/usr \
        -DCMAKE_INSTALL_LIBDIR=lib \
        -DBUILD_SHARED_LIBS=ON \

        # unsure about this flag
        # -DOCTEST_WITH_MAIN_IN_STATIC_LIB=OFF \

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

update:
  enabled: true
  github:
    identifier: doctest/doctest
    strip-prefix: v
    tag-filter: v

test:
  pipeline:
    - uses: test/tw/header-check
      with:
        files: |
          doctest/doctest.h
          doctest/extensions/doctest_util.h
