# Generated from https://git.alpinelinux.org/aports/plain/main/dosfstools/APKBUILD
package:
  name: dosfstools
  version: "4.2"
  epoch: 1
  description: DOS filesystem utilities
  copyright:
    - license: GPL-3.0-or-later

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - gettext
      - gettext-dev
      - libtool
      - linux-headers

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/dosfstools/dosfstools
      expected-commit: bba5a12447991cc1be9c33855c6c68e89b9ab29c
      tag: v${{package.version}}

  - runs: |
      touch config.rpath
      autoreconf -fiv

  - uses: autoconf/configure
    with:
      opts: |
        --enable-compat-symlinks

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: dosfstools-doc
    pipeline:
      - uses: split/manpages
    description: dosfstools manpages

update:
  enabled: true
  github:
    identifier: dosfstools/dosfstools
    strip-prefix: v
    tag-filter: v

test:
  pipeline:
    - runs: |
        tempfile=$(mktemp test-XXXXXXXXX)
        dd if=/dev/zero of=$tempfile bs=1M count=10
        for i in 12 16 32; do
          mkfs.fat -v -c -F $i $tempfile
        done
