package:
  name: doxygen
  version: 1.13.1
  epoch: 0
  description: A documentation system for C++, C, Java, IDL and PHP
  copyright:
    - license: GPL-2.0-or-later

environment:
  contents:
    packages:
      - autoconf
      - automake
      - bison
      - build-base
      - busybox
      - ca-certificates-bundle
      - cmake
      - coreutils
      - flex
      - openssf-compiler-options
      - perl
      - python3
      - samurai

pipeline:
  - uses: fetch
    with:
      expected-sha256: 0512ef85e1c720ee6798e0a0b753ab4994c1204e779d723122fe99e4f8b14c08
      uri: https://doxygen.nl/files/doxygen-${{package.version}}.src.tar.gz

  - runs: |
      cmake -B build -G Ninja \
          -DGIT_EXECUTABLE=/bin/false \
          -DCMAKE_INSTALL_PREFIX=/usr \
          -DCMAKE_BUILD_TYPE=MinSizeRel \
          -Dbuild_xmlparser=ON
      cmake --build build
      DESTDIR="${{targets.destdir}}" cmake --install build

  - uses: strip

update:
  enabled: true
  release-monitor:
    identifier: 457
  version-separator: "_"

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        doxygen --version
        doxygen --help
