diff --git a/source/extensions/dynamic_modules/dynamic_modules.bzl b/source/extensions/dynamic_modules/dynamic_modules.bzl
--- a/source/extensions/dynamic_modules/dynamic_modules.bzl
+++ b/source/extensions/dynamic_modules/dynamic_modules.bzl
@@ -71,10 +71,10 @@ def envoy_dynamic_module_prefix_symbols(name, module_name, archive, tags = [], *
             "for f in $(SRCS); do case $$f in *.pic.a) continue;; *.a) ARCH=$$f; break;; esac; done; " +
             "[ -z \"$$ARCH\" ] && " +
             "for f in $(SRCS); do case $$f in *.a) ARCH=$$f; break;; esac; done; " +
-            "$(location @llvm_toolchain_llvm//:objcopy) " +
+            "llvm-objcopy " +
             "--redefine-syms=$(location :" + redefine_syms_name + ") $$ARCH $@"
         ),
-        tools = ["@llvm_toolchain_llvm//:objcopy"],
+        local = 1,
         tags = tags,
     )

diff --git a/source/extensions/dynamic_modules/sdk/rust/BUILD b/source/extensions/dynamic_modules/sdk/rust/BUILD
--- a/source/extensions/dynamic_modules/sdk/rust/BUILD
+++ b/source/extensions/dynamic_modules/sdk/rust/BUILD
@@ -19,8 +19,8 @@ cargo_build_script(
         "@envoy_repo//:use_local_llvm": {
             "LIBCLANG_PATH": "%s/lib/libclang.so" % LLVM_PATH,
             "BINDGEN_EXTRA_CLANG_ARGS": " ".join([
-                "-resource-dir %s/lib/clang/18" % LLVM_PATH,
-                "-isystem %s/lib/clang/18/include" % LLVM_PATH,
+                "-resource-dir %s/lib/clang/19" % LLVM_PATH,
+                "-isystem %s/lib/clang/19/include" % LLVM_PATH,
                 "-isystem %s/include/x86_64-unknown-linux-gnu/c++/v1/" % LLVM_PATH,
                 "-isystem %s/include/aarch64-unknown-linux-gnu/c++/v1/" % LLVM_PATH,
             ]),
