package:
  name: eslint
  version: "10.8.1"
  epoch: 0 # GHSA-5p4m-2wfm-xmqj
  description: An AST-based pattern checker for JavaScript
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - nodejs
  resources:
    cpu: "1"
    memory: 6Gi
  test-resources:
    cpu: "1"
    memory: 1Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - nodejs-26
      - npm
      - rsync
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/eslint/eslint
      tag: v${{package.version}}
      expected-commit: c049dc3c4294da7afe3d920a1a5fdeba388f4983

  - runs: |
      npm install --omit=dev --legacy-peer-deps
      npm install js-yaml@4.3.1 # GHSA-mh29-5h37-fv8m GHSA-h67p-54hq-rp68 GHSA-52cp-r559-cp3m GHSA-5p4m-2wfm-xmqj
      npm prune --production
      _npmdir=/usr/lib/node_modules/${{package.name}}
      mkdir -p "${{targets.destdir}}"/usr/bin
      mkdir -p "${{targets.destdir}}"/$_npmdir

      rsync -r --exclude .cache --exclude .package-lock.json bin conf lib messages \
        node_modules package.json "${{targets.destdir}}"/$_npmdir

      ln -s $_npmdir/bin/${{package.name}}.js "${{targets.destdir}}"/usr/bin/${{package.name}}

  - uses: strip

update:
  enabled: true
  github:
    identifier: eslint/eslint
    use-tag: true
    tag-filter: v
    strip-prefix: v

test:
  pipeline:
    - runs: eslint --version
    - name: Test for undefined variables
      working-directory: test-js
      runs: |
        set -euo pipefail
        cat << EOF > eslint.config.js
        export default [
            {
                rules: {
                    "no-unused-vars": "warn",
                    "no-undef": "warn"
                }
            }
        ];
        EOF
        echo "console.log('Hello, world!');" > hello.js
        eslint hello.js | grep -F "'console' is not defined"
