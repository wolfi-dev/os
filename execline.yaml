package:
  name: execline
  version: "2.9.8.1"
  epoch: 0
  description: "a small scripting language intended to be an alternative to shell scripting"
  copyright:
    - license: ISC
  dependencies:
    runtime:
      - merged-bin
      - merged-lib
      - wolfi-baselayout

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - skalibs
      - skalibs-dev
      - wolfi-baselayout

pipeline:
  - uses: git-checkout
    with:
      repository: git://git.skarnet.org/execline
      tag: v${{package.version}}
      expected-commit: a0978660ec687c410f0cef99e8aacdbe7c343886

  - name: Configure
    runs: |
      ./configure \
        --enable-shared \
        --enable-static \
        --disable-allstatic \
        --prefix=/usr \
        --with-dynlib=/usr/lib

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: "execline-dev"
    description: "execline development headers"
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - merged-bin
        - merged-lib
        - wolfi-baselayout
    test:
      pipeline:
        - uses: test/tw/ldd-check
          with:
            packages: ${{subpkg.name}}

update:
  enabled: true
  release-monitor:
    identifier: 5482

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        eltest --version
        withstdinas version
        eltest --help
        withstdinas help
    - uses: test/tw/ldd-check
