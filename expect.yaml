# Partly generated from https://git.alpinelinux.org/aports/plain/main/expect/APKBUILD
package:
  name: expect
  version: 5.45.4
  epoch: 0
  description: A tool for automating interactive applications
  copyright:
    - license: NIST-PD
  dependencies:
    runtime:
      - tcl
      - libstdc++

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libstdc++
      - tcl-dev
      - wget

pipeline:
  - uses: fetch
    with:
      expected-sha256: 49a7da83b0bdd9f46d04a04deec19c7767bb9a323e40c4781f89caf760b92c34
      uri: https://downloads.sourceforge.net/project/expect/Expect/${{package.version}}/expect${{package.version}}.tar.gz

  - runs: |
      wget -T5 --dns-timeout=20 --tries=5 --random-wait --retry-on-http-error=429 --retry-connrefused -r -np -A "*.patch" -nH -e robots=off --cut-dirs=4 https://git.alpinelinux.org/aports/plain/main/expect/

  - uses: patch
    with:
      patches: |
        07-file-handle.patch
        09-unsafe-traps.patch
        10-manpage.patch
        12-fdout.patch
        22-segfault-with-stubs.patch
        24-format.patch

  - uses: autoconf/configure
    with:
      opts: |
        --with-tcl=/usr/lib \
        --with-tclinclude=/usr/include \
        --disable-rpath

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

update:
  enabled: true
  release-monitor:
    identifier: 771

test:
  pipeline:
    - runs: |
        make test
bash-5.2# vi 
Display all 2782 possibilities? (y or n)
bash-5.2# vi expect.yaml 
bash-5.2# vi expect.yaml 
bash-5.2# yam
bash-5.2# cat expect.yaml 
# Partly generated from https://git.alpinelinux.org/aports/plain/main/expect/APKBUILD
package:
  name: expect
  version: 5.45.4
  epoch: 0
  description: A tool for automating interactive applications
  copyright:
    - license: NIST-PD
  dependencies:
    runtime:
      - tcl
      - libstdc++

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libstdc++
      - tcl-dev
      - wget

pipeline:
  - uses: fetch
    with:
      expected-sha256: 49a7da83b0bdd9f46d04a04deec19c7767bb9a323e40c4781f89caf760b92c34
      uri: https://downloads.sourceforge.net/project/expect/Expect/${{package.version}}/expect${{package.version}}.tar.gz

  - runs: |
      wget -T5 --dns-timeout=20 --tries=5 --random-wait --retry-on-http-error=429 --retry-connrefused -r -np -A "*.patch" -nH -e robots=off --cut-dirs=4 https://git.alpinelinux.org/aports/plain/main/expect/

  - uses: patch
    with:
      patches: |
        07-file-handle.patch
        09-unsafe-traps.patch
        10-manpage.patch
        12-fdout.patch
        22-segfault-with-stubs.patch
        24-format.patch

  - uses: autoconf/configure
    with:
      opts: |
        --with-tcl=/usr/lib \
        --with-tclinclude=/usr/include \
        --disable-rpath

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

update:
  enabled: true
  release-monitor:
    identifier: 771

test:
  pipeline:
    - name: Validate expect installation
      runs: |
        expect -v