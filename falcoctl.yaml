package:
  name: falcoctl
  version: "0.12.0"
  epoch: 0 # GHSA-j5w8-q4qc-rx2x
  description: Administrative tooling for Falco
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - go

pipeline:
  - uses: git-checkout
    with:
      expected-commit: d4acd2c9a0f826fff41f80eb4e0dead9101ab1b8
      repository: https://github.com/falcosecurity/falcoctl
      tag: v${{package.version}}

  - runs: |
      make falcoctl RELEASE=${{package.version}}
      mkdir -p ${{targets.destdir}}/usr/bin
      mv falcoctl ${{targets.destdir}}/usr/bin/

  - uses: strip

update:
  enabled: true
  manual: false
  github:
    identifier: falcosecurity/falcoctl
    strip-prefix: v
    use-tag: true

test:
  pipeline:
    - name: Verify TLS installation
      runs: |
        falcoctl tls install
        [ -f ca.crt ] && [ -f client.crt ]
        falcoctl --help
    - name: Check falcoctl version
      runs: |
        falcoctl version
