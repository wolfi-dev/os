package:
  name: fastfetch
  version: "2.67.0"
  epoch: 0 # add ldd-check test
  description: A maintained, feature-rich and performance oriented, neofetch like system information tool.
  copyright:
    - license: MIT
  resources:
    memory: 7Gi

environment:
  contents:
    packages:
      - build-base
      - busybox

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/fastfetch-cli/fastfetch
      tag: ${{package.version}}
      expected-commit: 56da8f811068289f6352db8881418aa6e0f994e8

  - uses: cmake/configure

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

update:
  enabled: true
  github:
    identifier: fastfetch-cli/fastfetch
    use-tag: true

test:
  pipeline:
    - uses: test/tw/ldd-check
    - runs: |
        fastfetch --help
        fastfetch --version | grep ${{package.version}}
        fastfetch
