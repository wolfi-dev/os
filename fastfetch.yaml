package:
  name: fastfetch
  version: "2.60.0"
  epoch: 0 # go/wolfi-rsc/fastfetch
  description: A maintained, feature-rich and performance oriented, neofetch like system information tool.
  copyright:
    - license: MIT
  resources:
    memory: 7Gi

environment:
  contents:
    packages:
      - build-base
      - busybox

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/fastfetch-cli/fastfetch
      tag: ${{package.version}}
      expected-commit: 06ecc9c4805794ca8d632844e64950e82a6d5f7e

  - uses: cmake/configure

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

update:
  enabled: true
  github:
    identifier: fastfetch-cli/fastfetch
    use-tag: true

test:
  pipeline:
    - runs: |
        fastfetch --help
        fastfetch --version | grep ${{package.version}}
        fastfetch
