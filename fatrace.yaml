# Generated from https://git.alpinelinux.org/aports/plain/testing/fatrace/APKBUILD
package:
  name: fatrace
  version: 0.17.0
  epoch: 4
  description: Report system wide file access events
  copyright:
    - license: GPL-3.0-or-later

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle

pipeline:
  - uses: fetch
    with:
      expected-sha256: 38f23014be5c2d5f7d40560931ae8bf24161db311d1795631bfaba02845eed90
      uri: https://github.com/martinpitt/fatrace/archive/${{package.version}}/fatrace-${{package.version}}.tar.gz

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

  - name: install manpages
    runs: |
      mkdir -p "${{targets.destdir}}/usr/share/man/man8"
      cp fatrace.8 "${{targets.destdir}}/usr/share/man/man8/"

subpackages:
  - name: fatrace-doc
    pipeline:
      - uses: split/manpages
    description: fatrace docs
    test:
      pipeline:
        - uses: test/docs

update:
  release-monitor:
    identifier: 779

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        fatrace --help
