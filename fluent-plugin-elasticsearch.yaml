# Generated from https://github.com/uken/fluent-plugin-elasticsearch
package:
  name: fluent-plugin-elasticsearch
  version: 5.4.2
  epoch: 0
  description: Elasticsearch output plugin for Fluent event collector
  copyright:
    - license: Apache-2.0
  dependencies:
    runtime:
      - ruby3.2-elasticsearch
      - ruby3.2-excon
      - ruby3.2-faraday
      - ruby3.2-faraday-excon
      - ruby3.2-fluentd

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-3.2
      - ruby-3.2-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/uken/fluent-plugin-elasticsearch.git
      tag: v${{package.version}}
      expected-commit: aac238f4daed0fc693cbaa15560a10bf4a0db4fd

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: fluent-plugin-elasticsearch

update:
  enabled: true
  github:
    identifier: uken/fluent-plugin-elasticsearch
    strip-prefix: v
    use-tag: true
