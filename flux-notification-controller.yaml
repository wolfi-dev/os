package:
  name: flux-notification-controller
  version: "1.8.4"
  epoch: 6 # toolchain rebuild for  go-1.26-1.26.4
  description: The GitOps Toolkit event forwarded and notification dispatcher
  copyright:
    - license: Apache-2.0

var-transforms:
  - from: ${{package.version}}
    match: ^(\d+).*
    replace: $1
    to: major-version

environment:
  contents:
    packages:
      - go-1.26

pipeline:
  - uses: git-checkout
    with:
      expected-commit: f98e1eb5aa97adbdbe33018d4b9dd1e47763fad0
      repository: https://github.com/fluxcd/notification-controller
      tag: v${{package.version}}

  - uses: bump
    with:
      deps: |-
        golang.org/x/crypto@v0.52.0
        google.golang.org/grpc@v1.79.3
        go.opentelemetry.io/otel/sdk@v1.43.0
        go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracehttp@v1.43.0
        golang.org/x/net@v0.55.0
        golang.org/x/text@v0.36.0
        golang.org/x/crypto@v0.52.0

  - uses: go/build
    with:
      packages: .
      output: notification-controller
      ldflags: -X main.Version=${{package.version}}
      go-package: go-1.26

subpackages:
  - name: ${{package.name}}-iamguarded-compat
    description: "compat package for iamguarded"
    pipeline:
      - runs: |
          mkdir -p /opt/iamguarded/fluxcd-notification-controller/bin/
          ln -sf /usr/bin/notification-controller /opt/iamguarded/fluxcd-notification-controller/bin/notification-controller
      - uses: iamguarded/build-compat
        with:
          package: flux-notification-controller
          version: ${{vars.major-version}}
      - uses: iamguarded/finalize-compat
        with:
          package: flux-notification-controller
          version: ${{vars.major-version}}
    test:
      pipeline:
        - runs: |
            stat /opt/iamguarded/fluxcd-notification-controller/bin/notification-controller
        - uses: iamguarded/test-compat
          with:
            package: flux-notification-controller
            version: ${{vars.major-version}}

  - name: ${{package.name}}-crds
    description: Upstream CRDs for ${{package.name}} sourced from fluxcd/notification-controller v${{package.version}}
    pipeline:
      - runs: |
          # Chart-layer conditional gating (.Values.notificationController.installCRDs) is applied by
          # the consuming chart manifest when it stages this file into templates/crds/.
          install -d ${{targets.subpkgdir}}/charts/crds
          # insert an explicit YAML document separator before each upstream file so the concatenated bundle is valid multi-doc YAML
          {
            for f in config/crd/bases/*.yaml; do
              echo '---'
              cat "$f"
            done
          } > ${{targets.subpkgdir}}/charts/crds/notification-controller-crds.yaml
    test:
      environment:
        contents:
          packages:
            - yq
      pipeline:
        - runs: |
            yq /charts/crds/*.yaml

update:
  enabled: true
  github:
    identifier: fluxcd/notification-controller
    strip-prefix: v
    tag-filter: v

test:
  environment:
    contents:
      packages:
        - curl
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/kwok/cluster
    - name: Launch controller with kwok cluster
      uses: test/daemon-check-output
      with:
        setup: |
          kubectl apply -f https://github.com/fluxcd/notification-controller/releases/download/v${{package.version}}/notification-controller.crds.yaml
          kubectl wait --for=condition=Ready nodes --all
        start: notification-controller --health-addr :8080 --metrics-addr :8081
        timeout: 30
        expected_output: |
          Starting Controller
          Starting workers
        post: |
          #!/bin/sh -e
          set -o pipefail
          curl -s localhost:8081/metrics  | grep -F rest_client_requests_total
          curl -v localhost:8080/healthz 2>&1 | grep -Fi "200 OK"
