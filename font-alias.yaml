# Generated from https://git.alpinelinux.org/aports/plain/main/font-alias/APKBUILD
package:
  name: font-alias
  version: 1.0.5
  epoch: 4
  description: X.org font alias files
  copyright:
    - license: LicenseRef-xorg-font-alias
      license-path: COPYING

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - font-util-dev
      - util-macros

pipeline:
  - uses: git-checkout
    with:
      repository: https://gitlab.freedesktop.org/xorg/font/alias
      tag: font-alias-${{package.version}}
      expected-commit: 05282e1160350c2340d51ac991704f7e390f8420

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

update:
  enabled: true
  release-monitor:
    identifier: 15059

test:
  pipeline:
    - uses: test/fonts
