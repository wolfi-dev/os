# Generated from https://git.alpinelinux.org/aports/plain/main/font-liberation/APKBUILD
package:
  name: font-noto-emoji
  version: "2.051"
  epoch: 1 # go/wolfi-rsc/font-noto-emoji
  description: Google Noto emoji fonts
  copyright:
    - license: OFL-1.1
  resources:
    cpu: "1"
    memory: 5Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - fontconfig

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 8998f5dd683424a73e2314a8c1f1e359c19e8742
      repository: https://github.com/googlefonts/noto-emoji
      tag: v${{package.version}}

  - runs: install -Dm644 -t ${{targets.destdir}}/usr/share/fonts/${{package.name}} fonts/NotoColorEmoji.ttf

  - uses: strip

update:
  enabled: true
  ignore-regex-patterns:
    - '.*-unicode*'
    - '.*-color-emoji-binary'
    - '.*-license-apache'
  github:
    identifier: googlefonts/noto-emoji
    strip-prefix: v

test:
  pipeline:
    - uses: test/fonts
