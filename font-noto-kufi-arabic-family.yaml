package:
  name: font-noto-kufi-arabic-family
  version: "2.110"
  epoch: 0
  description: Noto Kufi Arabic, built from the notofonts sources
  copyright:
    - license: OFL-1.1
  dependencies:
    runtime:
      - font-noto-common
      - fontconfig

environment:
  contents:
    packages:
      - busybox

# Built with upstream's own recipe (gftools-builder, as notofonts' notobuilder
# runs it) from the family repository at the family's release tag. Repository
# HEADs carry unreleased edits under the same version string, hence the tag;
# the test/font-family check refuses fonts whose name table does not carry
# this version.
#
# Named with the -family suffix because font-noto-kufi-arabic is the existing
# per-script subpackage of font-noto (which now depends on this package). A
# name already published with font-noto's date-based version can never be
# taken over by a family-versioned package: apk would keep preferring it.
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/notofonts/arabic
      tag: NotoKufiArabic-v${{package.version}}
      expected-commit: 1b2b7e5c6ce3ab4d50681c854892325530084c35

  - uses: fonts/gftools-build
    with:
      config: sources/config-kufi-arabic.yaml
      family: NotoKufiArabic
      destination: ${{targets.destdir}}/usr/share/fonts/noto
      recipe-provider: noto

update:
  enabled: true
  github:
    identifier: notofonts/arabic
    strip-prefix: NotoKufiArabic-v
    tag-filter-prefix: NotoKufiArabic-v
    use-tag: true

test:
  pipeline:
    - uses: test/fonts
    - uses: test/font-family
      with:
        family: NotoKufiArabic
        version: ${{package.version}}
        count: "9"
        codepoint: "0x0627"
        text: "بسم الله الرحمن الرحيم"
