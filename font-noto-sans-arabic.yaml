package:
  name: font-noto-sans-arabic
  version: "2.013"
  epoch: 1
  description: Noto Sans Arabic, built from the notofonts sources
  copyright:
    - license: OFL-1.1
  dependencies:
    runtime:
      - font-noto-common
      - fontconfig
    # font-noto shipped these files itself before this package existed; let
    # apk hand them over when a system is upgraded in place (apk installs
    # the new dependency before it upgrades font-noto, so without this the
    # install is refused and the fonts are lost).
    replaces:
      - font-noto

environment:
  contents:
    packages:
      - busybox

# Built with upstream's own recipe (gftools-builder, as notofonts' notobuilder
# runs it) from the family repository at the family's release tag. Repository
# HEADs carry unreleased edits under the same version string, hence the tag;
# the test/font-family check refuses fonts whose name table does not carry
# this version.
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/notofonts/arabic
      tag: NotoSansArabic-v${{package.version}}
      expected-commit: 1b2b7e5c6ce3ab4d50681c854892325530084c35

  - uses: fonts/gftools-build
    with:
      config: sources/config-sans-arabic.yaml
      family: NotoSansArabic
      destination: ${{targets.destdir}}/usr/share/fonts/noto
      recipe-provider: noto

update:
  enabled: true
  github:
    identifier: notofonts/arabic
    strip-prefix: NotoSansArabic-v
    tag-filter-prefix: NotoSansArabic-v
    use-tag: true

test:
  pipeline:
    - uses: test/fonts
    - uses: test/font-family
      with:
        family: NotoSansArabic
        version: ${{package.version}}
        count: "36"
        codepoint: "0x0627"
        text: "بسم الله الرحمن الرحيم"
