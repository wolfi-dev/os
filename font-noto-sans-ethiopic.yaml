package:
  name: font-noto-sans-ethiopic
  version: "2.102"
  epoch: 1
  description: Noto Sans Ethiopic, built from the notofonts sources
  copyright:
    - license: OFL-1.1
  dependencies:
    runtime:
      - font-noto-common
      - fontconfig
    # font-noto shipped these files itself before this package existed; let
    # apk hand them over when a system is upgraded in place (apk installs
    # the new dependency before it upgrades font-noto, so without this the
    # install is refused and the fonts are lost).
    replaces:
      - font-noto
  resources:
    # gftools-builder compiles the instances in parallel with ninja.
    cpu: "16"
    memory: 16Gi

environment:
  contents:
    packages:
      - busybox

# Built with upstream's own recipe (gftools-builder, as notofonts' notobuilder
# runs it) from the family repository at the family's release tag. Repository
# HEADs carry unreleased edits under the same version string, hence the tag;
# the test/font-family check refuses fonts whose name table does not carry
# this version.
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/notofonts/ethiopic
      tag: NotoSansEthiopic-v${{package.version}}
      expected-commit: 1cc6933c58b7c42bbade15d6c7173897c24759e3

  - uses: fonts/gftools-build
    with:
      config: sources/config-sans-ethiopic.yaml
      family: NotoSansEthiopic
      destination: ${{targets.destdir}}/usr/share/fonts/noto
      recipe-provider: noto

update:
  enabled: true
  github:
    identifier: notofonts/ethiopic
    strip-prefix: NotoSansEthiopic-v
    tag-filter-prefix: NotoSansEthiopic-v
    use-tag: true

test:
  pipeline:
    - uses: test/fonts
    - uses: test/font-family
      with:
        family: NotoSansEthiopic
        version: ${{package.version}}
        count: "36"
        codepoint: "0x1200"
        text: "ቧዑጵⶵ"
