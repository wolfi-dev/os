package:
  name: font-noto-sans-gujarati
  version: "2.106"
  epoch: 0
  description: Noto Sans Gujarati, built from the notofonts sources
  copyright:
    - license: OFL-1.1
  dependencies:
    runtime:
      - font-noto-common
      - fontconfig
  resources:
    # gftools-builder compiles the instances in parallel with ninja.
    cpu: "16"
    memory: 16Gi

environment:
  contents:
    packages:
      - busybox

# Built with upstream's own recipe (gftools-builder, as notofonts' notobuilder
# runs it) from the family repository at the family's release tag. Repository
# HEADs carry unreleased edits under the same version string, hence the tag;
# the test/font-family check refuses fonts whose name table does not carry
# this version.
#
# The same release also defines the Noto Sans Gujarati UI
# family; those instances are built and installed here as well.
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/notofonts/gujarati
      tag: NotoSansGujarati-v${{package.version}}
      expected-commit: 17e99472719c29aa46534e4690e84443bcc69eab

  - uses: fonts/gftools-build
    with:
      config: sources/config-sans-gujarati.yaml
      family: NotoSansGujarati
      destination: ${{targets.destdir}}/usr/share/fonts/noto
      recipe-provider: noto

update:
  enabled: true
  github:
    identifier: notofonts/gujarati
    strip-prefix: NotoSansGujarati-v
    tag-filter-prefix: NotoSansGujarati-v
    use-tag: true

test:
  pipeline:
    - uses: test/fonts
    - uses: test/font-family
      with:
        family: NotoSansGujarati
        version: ${{package.version}}
        count: "36"
        codepoint: "0x0A85"
        text: "ઐજદળ"
