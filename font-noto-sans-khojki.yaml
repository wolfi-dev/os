package:
  name: font-noto-sans-khojki
  version: "2.005"
  epoch: 0
  description: Noto Sans Khojki, built from the notofonts sources
  copyright:
    - license: OFL-1.1
  dependencies:
    runtime:
      - font-noto-common
      - fontconfig
    # font-noto shipped these files itself before this package existed; let
    # apk hand them over when a system is upgraded in place (apk installs
    # the new dependency before it upgrades font-noto, so without this the
    # install is refused and the fonts are lost).
    replaces:
      - font-noto

environment:
  contents:
    packages:
      - busybox

# Built with upstream's own recipe (gftools-builder, as notofonts' notobuilder
# runs it) from the family repository at the family's release tag. Repository
# HEADs carry unreleased edits under the same version string, hence the tag;
# the test/font-family check refuses fonts whose name table does not carry
# this version.
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/notofonts/khojki
      tag: NotoSansKhojki-v${{package.version}}
      expected-commit: 02e25bf94664812076be4fa7dd1d688641e4bf8a

  # The source has one master, placed at Width 5, while its instances sit at
  # the default Width 100. glyphsLib 6.14 writes a designspace whose Width
  # axis spans 5..5, fontmake then finds no instance inside it and produces
  # nothing (upstream built the release with an older toolchain). With one
  # master its coordinate is arbitrary: move it to the instances' 100.
  - uses: patch
    with:
      patches: single-master-width.patch

  - uses: fonts/gftools-build
    with:
      config: sources/config-sans-khojki.yaml
      family: NotoSansKhojki
      destination: ${{targets.destdir}}/usr/share/fonts/noto
      recipe-provider: noto

update:
  enabled: true
  github:
    identifier: notofonts/khojki
    strip-prefix: NotoSansKhojki-v
    tag-filter-prefix: NotoSansKhojki-v
    use-tag: true

test:
  pipeline:
    - uses: test/fonts
    - uses: test/font-family
      with:
        family: NotoSansKhojki
        version: ${{package.version}}
        count: "1"
        codepoint: "0x11200"
        text: "\U00011209\U00011213\U0001121C\U00011225"
