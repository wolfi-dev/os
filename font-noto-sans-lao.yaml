package:
  name: font-noto-sans-lao
  version: "2.003"
  epoch: 0
  description: Noto Sans Lao, built from the notofonts sources
  copyright:
    - license: OFL-1.1
  dependencies:
    runtime:
      - font-noto-common
      - fontconfig
  resources:
    # gftools-builder compiles the instances in parallel with ninja.
    cpu: "16"
    memory: 16Gi

environment:
  contents:
    packages:
      - busybox

# Built with upstream's own recipe (gftools-builder, as notofonts' notobuilder
# runs it) from the family repository at the family's release tag. Repository
# HEADs carry unreleased edits under the same version string, hence the tag;
# the test/font-family check refuses fonts whose name table does not carry
# this version.
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/notofonts/lao
      tag: NotoSansLao-v${{package.version}}
      expected-commit: 0e4e088065a03785bd5304e6a1eb565d191bd391

  - uses: fonts/gftools-build
    with:
      config: sources/config-sans-lao.yaml
      family: NotoSansLao
      destination: ${{targets.destdir}}/usr/share/fonts/noto
      recipe-provider: noto

update:
  enabled: true
  github:
    identifier: notofonts/lao
    strip-prefix: NotoSansLao-v
    tag-filter-prefix: NotoSansLao-v
    use-tag: true

test:
  pipeline:
    - uses: test/fonts
    - uses: test/font-family
      with:
        family: NotoSansLao
        version: ${{package.version}}
        count: "36"
        codepoint: "0x0E81"
        text: "ຏບວຳ"
