package:
  name: font-noto-sans-marchen
  version: "2.004"
  epoch: 0
  description: Noto Sans Marchen, built from the notofonts sources
  copyright:
    - license: OFL-1.1
  dependencies:
    runtime:
      - font-noto-common
      - fontconfig
    # font-noto shipped these files itself before this package existed; let
    # apk hand them over when a system is upgraded in place (apk installs
    # the new dependency before it upgrades font-noto, so without this the
    # install is refused and the fonts are lost).
    replaces:
      - font-noto

environment:
  contents:
    packages:
      - busybox

# Built with upstream's own recipe (gftools-builder, as notofonts' notobuilder
# runs it) from the family repository at the family's release tag. Repository
# HEADs carry unreleased edits under the same version string, hence the tag;
# the test/font-family check refuses fonts whose name table does not carry
# this version.
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/notofonts/marchen
      tag: NotoSansMarchen-v${{package.version}}
      expected-commit: 0efb2639aef028a6e8be45790b60b71e5450b177

  - uses: fonts/gftools-build
    with:
      config: sources/config-sans-marchen.yaml
      family: NotoSansMarchen
      destination: ${{targets.destdir}}/usr/share/fonts/noto
      recipe-provider: noto

update:
  enabled: true
  github:
    identifier: notofonts/marchen
    strip-prefix: NotoSansMarchen-v
    tag-filter-prefix: NotoSansMarchen-v
    use-tag: true

test:
  pipeline:
    - uses: test/fonts
    - uses: test/font-family
      with:
        family: NotoSansMarchen
        version: ${{package.version}}
        count: "1"
        # Released unhinted upstream (ttfautohint --fail-ok in the recipe).
        hinted: "false"
        codepoint: "0x11C72"
        text: "\U00011C78\U00011C7E\U00011C84\U00011C8A"
