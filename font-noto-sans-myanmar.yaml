package:
  name: font-noto-sans-myanmar
  version: "2.107"
  epoch: 0
  description: Noto Sans Myanmar, built from the notofonts sources
  copyright:
    - license: OFL-1.1
  dependencies:
    runtime:
      - font-noto-common
      - fontconfig
  resources:
    # gftools-builder compiles the instances in parallel with ninja.
    cpu: "16"
    memory: 16Gi

environment:
  contents:
    packages:
      - busybox

# Built with upstream's own recipe (gftools-builder, as notofonts' notobuilder
# runs it) from the family repository at the family's release tag. Repository
# HEADs carry unreleased edits under the same version string, hence the tag;
# the test/font-family check refuses fonts whose name table does not carry
# this version.
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/notofonts/myanmar
      tag: NotoSansMyanmar-v${{package.version}}
      expected-commit: bd348056676051ac9076de5737436f0308b0de29

  - uses: fonts/gftools-build
    with:
      config: sources/config-sans-myanmar.yaml
      family: NotoSansMyanmar
      destination: ${{targets.destdir}}/usr/share/fonts/noto
      recipe-provider: noto

update:
  enabled: true
  github:
    identifier: notofonts/myanmar
    strip-prefix: NotoSansMyanmar-v
    tag-filter-prefix: NotoSansMyanmar-v
    use-tag: true

test:
  pipeline:
    - uses: test/fonts
    - uses: test/font-family
      with:
        family: NotoSansMyanmar
        version: ${{package.version}}
        count: "36"
        codepoint: "0x1000"
        text: "ဗၓၿꩡ"
