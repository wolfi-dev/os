package:
  name: font-noto-sans-symbols
  version: "2.003"
  epoch: 1
  description: Noto Sans Symbols, built from the notofonts sources
  copyright:
    - license: OFL-1.1
  dependencies:
    runtime:
      - font-noto-common
      - fontconfig
    # font-noto shipped these files itself before this package existed; let
    # apk hand them over when a system is upgraded in place (apk installs
    # the new dependency before it upgrades font-noto, so without this the
    # install is refused and the fonts are lost).
    replaces:
      - font-noto

environment:
  contents:
    packages:
      - busybox

# Built with upstream's own recipe (gftools-builder, as notofonts' notobuilder
# runs it) from the family repository at the family's release tag. The
# pipeline refuses fonts whose name table does not carry this version, so a
# checkout that is not the release fails the build. Repository HEADs carry
# unreleased edits under the same version string, hence the tag.
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/notofonts/symbols
      tag: NotoSansSymbols-v${{package.version}}
      expected-commit: b61fa7f86fb7d4163ee11d46d0977393f98d4f7a

  - uses: fonts/gftools-build
    with:
      config: sources/config-sans-symbols.yaml
      family: NotoSansSymbols
      destination: ${{targets.destdir}}/usr/share/fonts/noto
      recipe-provider: noto

update:
  enabled: true
  github:
    identifier: notofonts/symbols
    strip-prefix: NotoSansSymbols-v
    tag-filter-prefix: NotoSansSymbols-v
    use-tag: true

test:
  pipeline:
    - uses: test/fonts
    - uses: test/font-family
      with:
        family: NotoSansSymbols
        version: ${{package.version}}
        count: "9"
        codepoint: "0x24B6"
        text: "ⒶⅠⅡⅢ"
