package:
  name: font-noto-sans-tai-le
  version: "2.002"
  epoch: 0
  description: Noto Sans Tai Le, built from the notofonts sources
  copyright:
    - license: OFL-1.1
  dependencies:
    runtime:
      - font-noto-common
      - fontconfig
    # font-noto shipped these files itself before this package existed; let
    # apk hand them over when a system is upgraded in place (apk installs
    # the new dependency before it upgrades font-noto, so without this the
    # install is refused and the fonts are lost).
    replaces:
      - font-noto
      # font-noto's per-script subpackage carried these files under
      # /usr/share/fonts/noto/ while the rest of the script was already built
      # from source; it must hand them over as well.
      - font-noto-tai

environment:
  contents:
    packages:
      - busybox

# Built with upstream's own recipe (gftools-builder, as notofonts' notobuilder
# runs it) from the family repository at the family's release tag. Repository
# HEADs carry unreleased edits under the same version string, hence the tag;
# the test/font-family check refuses fonts whose name table does not carry
# this version.
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/notofonts/tai-le
      tag: NotoSansTaiLe-v${{package.version}}
      expected-commit: 9fa6d7f3f20c6fe0cdbeadfa9be4da0ba31eac3f

  - uses: fonts/gftools-build
    with:
      config: sources/config-sans-tai-le.yaml
      family: NotoSansTaiLe
      destination: ${{targets.destdir}}/usr/share/fonts/noto
      recipe-provider: noto

update:
  enabled: true
  github:
    identifier: notofonts/tai-le
    strip-prefix: NotoSansTaiLe-v
    tag-filter-prefix: NotoSansTaiLe-v
    use-tag: true

test:
  pipeline:
    - uses: test/fonts
    - uses: test/font-family
      with:
        family: NotoSansTaiLe
        version: ${{package.version}}
        count: "1"
        codepoint: "0x1950"
        text: "ᥗᥞᥥᥬ"
