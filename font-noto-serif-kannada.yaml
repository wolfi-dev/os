package:
  name: font-noto-serif-kannada
  version: "2.005"
  epoch: 0
  description: Noto Serif Kannada, built from the notofonts sources
  copyright:
    - license: OFL-1.1
  dependencies:
    runtime:
      - font-noto-common
      - fontconfig

environment:
  contents:
    packages:
      - busybox

# Built with upstream's own recipe (gftools-builder, as notofonts' notobuilder
# runs it) from the family repository at the family's release tag. Repository
# HEADs carry unreleased edits under the same version string, hence the tag;
# the test/font-family check refuses fonts whose name table does not carry
# this version.
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/notofonts/kannada
      tag: NotoSerifKannada-v${{package.version}}
      expected-commit: b0e78acf3176fb4d30c95ece3825ef8561dbd7aa

  - uses: fonts/gftools-build
    with:
      config: sources/config-serif-kannada.yaml
      family: NotoSerifKannada
      destination: ${{targets.destdir}}/usr/share/fonts/noto
      recipe-provider: noto

update:
  enabled: true
  github:
    identifier: notofonts/kannada
    strip-prefix: NotoSerifKannada-v
    tag-filter-prefix: NotoSerifKannada-v
    use-tag: true

test:
  pipeline:
    - uses: test/fonts
    - uses: test/font-family
      with:
        family: NotoSerifKannada
        version: ${{package.version}}
        count: "9"
        codepoint: "0x0C80"
        text: "ಐಝಪಷ"
