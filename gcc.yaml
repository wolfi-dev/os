package:
  name: gcc
  version: 13.2.0
  epoch: 3
  description: "the GNU compiler collection"
  copyright:
    - license: GPL-3.0-or-later
  dependencies:
    runtime:
      - binutils
      - libstdc++-dev
      - posix-cc-wrappers

environment:
  contents:
    packages:
      - wolfi-baselayout
      - busybox
      - build-base
      - ca-certificates-bundle
      - texinfo
      - gawk
      - bison
      - flex-dev
      - gmp-dev
      - mpfr-dev
      - mpc-dev
      - isl-dev
      - zlib-dev
      - make
      - patch

pipeline:
  - uses: fetch
    with:
      uri: https://ftp.gnu.org/gnu/gcc/gcc-${{package.version}}/gcc-${{package.version}}.tar.gz
      expected-sha256: 8cb4be3796651976f94b9356fa08d833524f62420d6292c5033a9a26af315078

  - uses: patch
    with:
      patches: CVE-2023-4039.patch

  - name: 'Set up build directory'
    runs: |
      mkdir build

  - name: 'Configure GCC'
    runs: |
      cd build
      ../configure \
        --prefix=/usr \
        --disable-nls \
        --disable-werror \
        --with-glibc-version=2.35 \
        --enable-initfini-array \
        --disable-nls \
        --disable-multilib \
        --enable-host-shared \
        --enable-shared \
        --enable-threads \
        --enable-tls \
        --enable-default-pie \
        --enable-default-ssp \
        --with-system-zlib \
        --enable-languages=c,c++,objc,fortran,jit,go \
        --enable-bootstrap \
        --enable-gnu-indirect-function \
        --enable-gnu-unique-object \
        --enable-cet=auto \
        --with-linker-hash-style=gnu

  - runs: |
      make -C build -j$(nproc)

  - runs: |
      make -C build -j$(nproc) install DESTDIR="${{targets.contextdir}}"

  # We don't want to keep the .la files.
  - runs: |
      find ${{targets.contextdir}} -name '*.la' -print -exec rm \{} \;

  - runs: |
      chmod 755 ${{targets.contextdir}}/usr/lib64/libgcc_s.*

  - name: 'Clean up documentation'
    runs: |
      rm -rf ${{targets.contextdir}}/usr/share/info

  - uses: strip

subpackages:
  - name: 'gcc-doc'
    pipeline:
      - uses: split/manpages

  - name: 'libstdc++'
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib64
          mv "${{targets.contextdir}}"/usr/lib64/*++.so.* "${{targets.contextdir}}"/usr/lib64/

  - name: 'libstdc++-dev'
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib64
          mkdir -p "${{targets.contextdir}}"/usr/include
          mkdir -p "${{targets.contextdir}}"/usr/share/gcc-${{package.version}}/python/libstdcxx
          mv "${{targets.contextdir}}"/usr/lib64/*++.a "${{targets.contextdir}}"/usr/lib64/
          mv "${{targets.contextdir}}"/usr/lib64/libstdc++.so "${{targets.contextdir}}"/usr/lib64/
          mv "${{targets.contextdir}}"/usr/include/*++* "${{targets.contextdir}}"/usr/include/
          mv "${{targets.contextdir}}"/usr/share/gcc-${{package.version}}/python/libstdcxx/* \
            "${{targets.contextdir}}"/usr/share/gcc-${{package.version}}/python/libstdcxx/

  - name: "libquadmath"
    description: "128-bit math library provided by GCC"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib64
          [ -f "${{targets.contextdir}}"/usr/lib64/libquadmath.so.* ] && mv "${{targets.contextdir}}"/usr/lib64/libquadmath.so.* "${{targets.contextdir}}"/usr/lib64/

  - name: "libgfortran"
    description: "Fortran runtime library provided by GCC"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib64
          mv "${{targets.contextdir}}"/usr/lib64/libgfortran.so.* "${{targets.contextdir}}"/usr/lib64/

  - name: "gfortran"
    description: "GNU Fortran Compiler"
    pipeline:
      - runs: |
          _libexecdir=/usr/libexec/gcc/${{host.triplet.gnu}}/${{package.version}}
          _libdir=/usr/lib/gcc/${{host.triplet.gnu}}/${{package.version}}

          for i in "$_libexecdir" "$_libdir" usr/lib64 usr/bin; do
            mkdir -p "${{targets.contextdir}}"/$i
          done

          mv "${{targets.contextdir}}"/usr/bin/*fortran "${{targets.contextdir}}"/usr/bin
          mv "${{targets.contextdir}}"/usr/lib64/libgfortran* "${{targets.contextdir}}"/usr/lib64
          [ -f "${{targets.contextdir}}"/usr/lib64/libquadmath* ] && mv "${{targets.contextdir}}"/usr/lib64/libquadmath* "${{targets.contextdir}}"/usr/lib64

          mv "${{targets.contextdir}}"/$_libdir/finclude "${{targets.contextdir}}"/$_libdir
          mv "${{targets.contextdir}}"/$_libexecdir/f951 "${{targets.contextdir}}"/$_libexecdir
    dependencies:
      runtime:
        - gcc
        - libgfortran

  - name: "libobjc"
    description: "GNU Objective-C runtime"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib64
          mv "${{targets.contextdir}}"/usr/lib64/libobjc.so.* "${{targets.contextdir}}"/usr/lib64

  - name: "gcc-objc"
    description: "GNU Objective-C compiler"
    pipeline:
      - runs: |
          _libexecdir=/usr/libexec/gcc/${{host.triplet.gnu}}/${{package.version}}
          _libdir=/usr/lib/gcc/${{host.triplet.gnu}}/${{package.version}}

          for i in "$_libexecdir" "$_libdir"/include usr/lib64; do
            mkdir -p "${{targets.contextdir}}"/$i
          done

          mv "${{targets.contextdir}}"/$_libexecdir/cc1obj "${{targets.contextdir}}"/$_libexecdir
          mv "${{targets.contextdir}}"/$_libdir/include/objc "${{targets.contextdir}}"/$_libdir/include/
          mv "${{targets.contextdir}}"/usr/lib64/libobjc.* "${{targets.contextdir}}"/usr/lib64

  - name: "libgccjit"
    description: "GCC JIT library"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib
          mv "${{targets.contextdir}}"/usr/lib/libgccjit.so.* "${{targets.contextdir}}"/usr/lib/

  - name: "libgccjit-dev"
    description: "GCC JIT library headers"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib
          mkdir -p "${{targets.contextdir}}"/usr/include

          mv "${{targets.contextdir}}"/usr/lib/libgccjit.so "${{targets.contextdir}}"/usr/lib/
          mv "${{targets.contextdir}}"/usr/include/libgccjit*.h "${{targets.contextdir}}"/usr/include/

  - name: "libgomp"
    description: "GNU parallel programming library"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib64
          mv "${{targets.contextdir}}"/usr/lib64/libgomp.so.* "${{targets.contextdir}}"/usr/lib64/

  - name: "libgcc"
    description: "GCC runtime library"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib64
          mv "${{targets.contextdir}}"/usr/lib64/libgcc_s.so.* "${{targets.contextdir}}"/usr/lib64/

  - name: "libatomic"
    description: "GCC atomic library"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib64
          mv "${{targets.contextdir}}"/usr/lib64/libatomic.so.* "${{targets.contextdir}}"/usr/lib64/

  - name: "libgo"
    description: "GCC go runtime library"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib64
          mv "${{targets.contextdir}}"/usr/lib64/libgo.so.* "${{targets.contextdir}}"/usr/lib64/

  - name: "gcc-go"
    description: "GCC go compiler"
    dependencies:
      provides:
        - go-bootstrap=1.18
    pipeline:
      - runs: |
          _libexecdir=/usr/libexec/gcc/${{host.triplet.gnu}}/${{package.version}}

          for i in "$_libexecdir" usr/lib64 usr/bin; do
            mkdir -p "${{targets.contextdir}}"/$i
          done

          mv "${{targets.contextdir}}"/usr/bin/${{host.triplet.gnu}}-gccgo "${{targets.contextdir}}"/usr/bin/
          for i in go gofmt gccgo; do
            mv "${{targets.contextdir}}"/usr/bin/$i "${{targets.contextdir}}"/usr/bin/
          done

          mv "${{targets.contextdir}}"/usr/lib64/go "${{targets.contextdir}}"/usr/lib64/
          mv "${{targets.contextdir}}"/usr/lib64/libgo.a "${{targets.contextdir}}"/usr/lib64/
          mv "${{targets.contextdir}}"/usr/lib64/libgobegin.a "${{targets.contextdir}}"/usr/lib64/
          mv "${{targets.contextdir}}"/usr/lib64/libgolibbegin.a "${{targets.contextdir}}"/usr/lib64/
          mv "${{targets.contextdir}}"/usr/lib64/libgo.so "${{targets.contextdir}}"/usr/lib64/

          for i in cgo go1; do
            mv "${{targets.contextdir}}"/"$_libexecdir"/$i "${{targets.contextdir}}"/"$_libexecdir"/
          done

update:
  enabled: true
  release-monitor:
    identifier: 6502
