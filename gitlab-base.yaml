package:
  name: gitlab-base
  # The release-monitor identified is for the gitlab-runner,
  # but these seem to be co-versioned.
  version: 16.4.1
  epoch: 0
  description: GitLab Base is the base image, which other GitLab containers can build on top of.
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - bash
      - busybox
      - ca-certificates-bundle
      - curl
      - gitlab-cng-base-scripts
      - gitlab-logger
      - gomplate
      - procps
      - xtail

update:
  enabled: true
  release-monitor:
    identifier: 11337
