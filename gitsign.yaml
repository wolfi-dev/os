package:
  name: gitsign
  version: 0.5.2
  epoch: 1
  description: Keyless Git signing with Sigstore!
  copyright:
    - license: Apache-2.0

pipeline:
  - uses: go/install
    with:
      package: github.com/sigstore/gitsign@v${{package.version}}
