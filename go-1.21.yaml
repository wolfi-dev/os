package:
  name: go-1.21
  version: 1.21.3
  epoch: 0
  description: "the Go programming language"
  copyright:
    - license: BSD-3-Clause
  dependencies:
    provides:
      - go=1.21
      - go-bootstrap=1.21
    runtime:
      - build-base
      - bash
      - binutils-gold # Needed for cgo linking due to upstream issue #15696 which forces use of the gold linker.

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - build-base
      - bash
      - go-bootstrap~1.20

# transform melange version to contain "+" rather than third "." so we can use a var in the fetch URL
var-transforms:
  - from: ${{package.version}}
    match: \_
    replace: ''
    to: mangled-package-version

pipeline:
  - uses: fetch
    with:
      uri: https://go.dev/dl/go${{vars.mangled-package-version}}.src.tar.gz
      expected-sha256: 186f2b6f8c8b704e696821b09ab2041a5c1ee13dcbc3156a13adcf75931ee488
      strip-components: 0

  - runs: |
      cd go/src
      ./make.bash -v

  - runs: |
      cd go

      mkdir -p "${{targets.contextdir}}"/usr/bin "${{targets.contextdir}}"/usr/lib/go/bin "${{targets.contextdir}}"/usr/share/doc/go

      for bin in go gofmt; do
        install -Dm755 bin/$bin "${{targets.contextdir}}"/usr/lib/go/bin/$bin
        ln -s /usr/lib/go/bin/$bin "${{targets.contextdir}}"/usr/bin/
      done

      cp -a pkg lib "${{targets.contextdir}}"/usr/lib/go/
      cp -r doc misc "${{targets.contextdir}}"/usr/share/doc/go
      cp -a src "${{targets.contextdir}}"/usr/lib/go/
      cp -p go.env "${{targets.contextdir}}"/usr/lib/go/go.env

      rm -rf "${{targets.contextdir}}"/usr/lib/go/pkg/obj
      rm -rf "${{targets.contextdir}}"/usr/lib/go/pkg/bootstrap
      rm -rf "${{targets.contextdir}}"/usr/lib/go/pkg/tool/*/api
      rm -rf "${{targets.contextdir}}"/usr/lib/go/pkg/*/cmd
      rm -rf "${{targets.contextdir}}"/usr/lib/go/pkg/tool/*/api
      rm -rf "${{targets.contextdir}}"/usr/lib/go/pkg/tool/*/go_bootstrap
      rm -rf "${{targets.contextdir}}"/usr/lib/go/src/cmd/dist/dist

      # Remove tests from /usr/lib/go/src, not needed at runtime
      find "${{targets.contextdir}}"/usr/lib/go/src \( -type f -a -name "*_test.go" \) \
        -exec rm -rf \{\} \+
      find "${{targets.contextdir}}"/usr/lib/go/src \( -type d -a -name "testdata" \) \
        -exec rm -rf \{\} \+
      find "${{targets.contextdir}}"/usr/lib/go/src \( -type f -a -name "*.rc" \) \
        -exec rm -rf \{\} \+
      find "${{targets.contextdir}}"/usr/lib/go/src \( -type f -a -name "*.bat" \) \
        -exec rm -rf \{\} \+

  - uses: strip

subpackages:
  - name: "go-1.21-doc"
    description: "go documentation"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/share
          mv "${{targets.contextdir}}"/usr/share/doc "${{targets.contextdir}}"/usr/share/

update:
  enabled: true
  shared: true
  github:
    identifier: golang/go
    strip-prefix: go
    tag-filter: go1.21
    use-tag: true
