#nolint:valid-pipeline-git-checkout-tag
package:
  name: go-discover
  version: "0_git20260302"
  epoch: 0 # CVE-2025-68121
  description: go-discover is a Go (golang) library and command line tool to discover ip addresses of nodes in cloud environments based on meta information like tags provided by the environment.
  copyright:
    - license: MPL-2.0
  resources:
    cpu: "3"
    memory: 5Gi

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/hashicorp/go-discover
      expected-commit: b888502fd8a02aa0e4b515d7ff508c2ca720323c
      branch: master

  - uses: go/build
    with:
      output: discover
      packages: ./cmd/discover

update:
  enabled: true
  git: {}
  schedule:
    period: daily
    reason: Upstream does not maintain tags or releases

test:
  pipeline:
    - name: Verify help
      runs: |
        discover --help
        discover version
