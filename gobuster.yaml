package:
  name: gobuster
  version: "3.8.2"
  epoch: 4 # CVE-2025-61729
  description: "a tool used to brute force attack for URIs, DNS, etc."
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - go

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/OJ/gobuster
      tag: v${{package.version}}
      expected-commit: e8410cad6cacf08b82e2f610055f636f0be61c40

  - uses: go/bump
    with:
      deps: |-
        golang.org/x/crypto@v0.45.0

  - uses: go/build
    with:
      packages: .
      modroot: .
      output: gobuster
      ldflags: -X main.version=${{package.version}} -X main.commit=$(git rev-parse HEAD) -X main.date=$(date ${SOURCE_DATE_EPOCH:+ -d@${SOURCE_DATE_EPOCH}} "+%Y-%m-%dT%H:%M:%SZ")

  - uses: strip

update:
  enabled: true
  github:
    strip-prefix: v
    identifier: OJ/gobuster

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        gobuster --help
        gobuster --version
