package:
  name: goreleaser
  version: "2.14.1"
  epoch: 1 # GHSA-q9hv-hpm4-hj6x
  description: Deliver Go binaries as fast and easily as possible
  copyright:
    - license: MIT
  resources:
    cpu: "10"
    memory: 12Gi

environment:
  environment:
    GOTOOLCHAIN: local+auto
  contents:
    packages:
      - git

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/goreleaser/goreleaser
      tag: v${{package.version}}
      expected-commit: 6bce54f02c901c4e42e699d35e66b4d82f2ce162

  - uses: go/bump
    with:
      deps: |-
        github.com/cloudflare/circl@v1.6.3

  - uses: go/build
    with:
      packages: .
      modroot: .
      output: goreleaser
      ldflags: -X main.version=${{package.version}} -X main.commit=$(git rev-parse HEAD) -X main.date=$(date ${SOURCE_DATE_EPOCH:+ -d@${SOURCE_DATE_EPOCH}} "+%Y-%m-%dT%H:%M:%SZ")

update:
  enabled: true
  github:
    identifier: goreleaser/goreleaser
    strip-prefix: v

test:
  pipeline:
    - name: version check
      runs: |
        goreleaser --version
        goreleaser --help
