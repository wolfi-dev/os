package:
  name: grpcurl
  version: 1.9.0
  epoch: 0
  description: CLI tool to interact with gRPC servers
  copyright:
    - license: MIT
      paths:
        - "*"
  dependencies:
    runtime:
      - ca-certificates-bundle

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - go

pipeline:
  - uses: fetch
    with:
      expected-sha256: 86aeb4e54bd360b75a94884a6bdc5f22310044137b8d5504392987ec62a42c7a
      uri: https://github.com/fullstorydev/grpcurl/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: go/bump
    with:
      deps: golang.org/x/net@v0.17.0 google.golang.org/protobuf@v1.33.0

  - uses: go/build
    with:
      packages: ./cmd/grpcurl
      output: grpcurl
      ldflags: -s -w -X main.version=v${{package.version}}

  - uses: strip

update:
  enabled: true
  github:
    identifier: fullstorydev/grpcurl
    strip-prefix: v
