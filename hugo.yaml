package:
  name: hugo
  version: "0.163.2"
  epoch: 0 # toolchain rebuild for  go-1.25-1.25.11
  description: The world's fastest framework for building websites.
  copyright:
    - license: Apache-2.0
  resources:
    cpu: "4"
    memory: 7Gi
  test-resources:
    memory: 1Gi
    cpu: "1"

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - git
      - go-1.26

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/gohugoio/hugo
      tag: v${{package.version}}
      expected-commit: 19a5cec0b9618163bb519487382e861d29edf383

  - uses: go/build
    with:
      packages: .
      output: hugo
      tags: extended

update:
  enabled: true
  github:
    identifier: gohugoio/hugo
    strip-prefix: v

test:
  environment:
    contents:
      packages:
        - git
        - bash
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/tw/help-check
      with:
        bins: hugo
    - uses: test/tw/ver-check
      with:
        bins: hugo
    - name: Create a new Hugo site
      runs: |
        hugo new site test-site
    - uses: test/daemon-check-output
      working-directory: test-site
      with:
        start: /usr/bin/hugo serve
        expected_output: "Web Server is available at"
        timeout: 10
