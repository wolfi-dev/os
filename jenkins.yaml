package:
  name: jenkins
  version: "2.387"
  epoch: 1
  description:
  target-architecture:
    - all
  copyright:
    - paths:
        - "*"
      attestation:
      license: MIT
  dependencies:
    runtime:
      - ttf-dejavu
environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - build-base
      - automake
      - autoconf
      - coreutils
      - bash
      - openssh-client
      - tini
      - openjdk-17
      - maven
      - tzdata
      - glibc-locale-en
      - git
      - patch
      - ttf-dejavu
pipeline:
  - uses: fetch
    with:
      uri: https://github.com/jenkinsci/jenkins/archive/refs/tags/jenkins-${{package.version}}.tar.gz
      expected-sha256: 375dd3f31e5266382888dc3c82099180aa8410d42ca2327c920f1c66c48139de
  - uses: patch
    with:
      patches: ignoreArchiveNotReadableTest.patch
  - runs: |
      export JAVA_HOME=/usr/lib/jvm/openjdk
      export LANG=en_US.UTF-8

      export MAVEN_OPTS="-DforkCount=2"
      mvn install

      mkdir -p ${{targets.destdir}}/usr/share/java/jenkins
      mv war/target/jenkins.war ${{targets.destdir}}/usr/share/java/jenkins/
