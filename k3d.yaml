package:
  name: k3d
  version: 5.5.0
  epoch: 0
  description: Little helper to run CNCF's k3s in Docker
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - go
      - git
      - curl

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/k3d-io/k3d
      tag: v${{package.version}}
      expected-commit: 38a122182665a4fe47d54ce99ad5a3f290080742

  - runs: |
      make build
      install -Dm755 ./bin/k3d ${{targets.destdir}}/usr/bin/k3d

update:
  enabled: true
  github:
    identifier: k3d-io/k3d
    strip-prefix: v
