package:
  name: k9s
  version: "0.50.18"
  epoch: 14 # GHSA-78h2-9frx-2jm8
  description: Kubernetes CLI To Manage Your Clusters In Style!
  copyright:
    - license: Apache-2.0
  resources:
    cpu: "8"
    memory: 16Gi
  test-resources:
    memory: 1Gi

environment:
  contents:
    packages:
      - go-1.25

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 6dbf571c59fd48dc5b384aa46ee7f3e5decfae2b
      repository: https://github.com/derailed/k9s
      tag: v${{package.version}}

  - uses: go/bump
    with:
      deps: |-
        github.com/cloudflare/circl@v1.6.3
        go.opentelemetry.io/otel/sdk@v1.40.0
        github.com/docker/cli@v29.2.0
        google.golang.org/grpc@v1.79.3
        github.com/anchore/syft@v1.42.3
        github.com/go-git/go-git/v5@v5.17.1
        github.com/go-jose/go-jose/v4@v4.1.4

  - uses: go/build
    with:
      packages: .
      ldflags: |
        -X github.com/derailed/k9s/cmd.version=v${{package.version}}
        -X github.com/derailed/k9s/cmd.commit=$(git rev-parse HEAD)
        -X github.com/derailed/k9s/cmd.date=$(date -u -d "@$SOURCE_DATE_EPOCH" +"%Y-%m-%dT%H:%M:%SZ")
      output: k9s

update:
  enabled: true
  github:
    identifier: derailed/k9s
    strip-prefix: v

test:
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/tw/help-check
      with:
        bins: k9s
    - uses: test/tw/ver-check
      with:
        bins: k9s
    - uses: test/kwok/cluster
    - runs: |
        k9s info
