package:
  name: kaniko
  version: "1.25.6"
  epoch: 0 # GHSA-pwhc-rpq9-4c8w
  description: Build Container Images In Kubernetes
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - go

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-forks/kaniko
      tag: v${{package.version}}
      expected-commit: 5e0b68437a17ca17c4d980f403cb4ef864e5696b

  # patch to make kaniko more compatible with wolfi-baselayout.
  - uses: patch
    with:
      patches: remove-usrmerge-symlinks.patch

  - uses: go/build
    with:
      output: executor
      packages: ./cmd/executor
      ldflags: -X github.com/chainguard-dev/kaniko/pkg/version.version=v${{package.version}}

subpackages:
  - name: kaniko-warmer
    pipeline:
      - uses: go/build
        with:
          output: warmer
          packages: ./cmd/warmer
          ldflags: -X github.com/chainguard-dev/kaniko/pkg/version.version=v${{package.version}}
    test:
      pipeline:
        - runs: |
            warmer --help

  - name: kaniko-compat
    pipeline:
      - runs: |
          # Symlink the binary from usr/bin to /kaniko/
          mkdir -p ${{targets.subpkgdir}}/kaniko/
          ln -sf /usr/bin/executor ${{targets.subpkgdir}}/kaniko/
    # test added by a robot (compat)
    test:
      pipeline:
        - runs: |
            readlink -v /kaniko/executor

  - name: kaniko-warmer-compat
    pipeline:
      - runs: |
          # Symlink the binary from usr/bin to /kaniko/
          mkdir -p ${{targets.subpkgdir}}/kaniko/
          ln -sf /usr/bin/warmer ${{targets.subpkgdir}}/kaniko/
    # test added by a robot (compat)
    test:
      pipeline:
        - runs: |
            readlink -v /kaniko/warmer

update:
  enabled: true
  github:
    identifier: chainguard-forks/kaniko
    strip-prefix: v
    use-tag: true
    tag-filter: v

test:
  environment:
    contents:
      packages:
        - bash
        - coreutils
        - docker
        - git
  pipeline:
    - name: "Verify executor binary and version"
      runs: |
        executor --help
        executor version | grep -q v${{package.version}}
    - name: "Test executor basic functionality"
      runs: |
        # Create a simple Dockerfile
        cat > Dockerfile << 'EOF'
        FROM scratch
        EOF

        # Build a simple image
        # We need --force when running under non-containerized environments (e.g. QEMU)
        executor \
          --force \
          --context dir://. \
          --dockerfile Dockerfile \
          --no-push \
          --tarPath test.tar

        # Verify the tar was created
        test -f test.tar
