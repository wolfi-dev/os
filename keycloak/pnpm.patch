--- a/pom.xml
+++ b/pom.xml
@@ -1526,7 +1526,8 @@
                             </configuration>
                         </execution>
                     </executions>
-                    <configuration>
+		   <configuration>
+			   <skip.node-and-npm.install>true</skip.node-and-npm.install>
                         <nodeVersion>${node.version}</nodeVersion>
                         <pnpmVersion>${pnpm.version}</pnpmVersion>
                         <installDirectory>${maven.multiModuleProjectDirectory}</installDirectory>
