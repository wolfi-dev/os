package:
  name: kitty-terminfo
  version: "0.48.1"
  epoch: 0
  description: Terminfo for the kitty terminal emulator
  copyright:
    - license: GPL-3.0-only

environment:
  contents:
    packages:
      - busybox
      - ncurses

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/kovidgoyal/kitty
      tag: v${{package.version}}
      expected-commit: 4f2f6a076b2baa9310d54cbf4d8191f7dbaffd20

  - runs: |
      mkdir -p "${{targets.destdir}}/etc/terminfo"
      tic -x -o "${{targets.destdir}}/etc/terminfo" terminfo/kitty.terminfo

update:
  enabled: true
  github:
    identifier: kovidgoyal/kitty
    strip-prefix: v

test:
  pipeline:
    - uses: test/tw/contains-files
      with:
        dir: /etc/terminfo/x/
        name: xterm-kitty
