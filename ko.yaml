package:
  name: ko
  version: 0.16.0_invalid123
  epoch: 5
  description: Simple, fast container image builder for Go applications.
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - go
  environment:
    CGO_ENABLED: "0"

pipeline:
  - uses: git-checkout
    with:
      destination: ko
      expected-commit: 0dcace336cc5f723150d77487dbaf56f849cc361
      repository: https://github.com/ko-build/ko
      tag: v0.16.0

  - uses: go/bump
    with:
      deps: github.com/docker/docker@v27.1.2
      modroot: ko

  - uses: go/build
    with:
      ldflags: -X github.com/google/ko/pkg/commands.Version=0.16.0
      modroot: ko
      output: ko
      packages: .
      vendor: true

  - uses: strip

update:
  enabled: true
  github:
    identifier: ko-build/ko
    strip-prefix: v

test:
  pipeline:
    - runs: |
        ko --help
        ko version
