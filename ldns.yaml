package:
  name: ldns
  version: "1.9.1"
  epoch: 0 # go/wolfi-rsc/ldns
  description: Lowlevel DNS(SEC) library
  copyright:
    - license: BSD-3-Clause
  resources:
    cpu: "1"
    memory: 4Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libpcap-dev
      - libtool
      - openssl-hardened-dev
      - perl

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/NLnetLabs/ldns.git
      tag: ${{package.version}}
      expected-commit: 1867b1d7719424e1ab91e3f3827794b4bf849dc6

  - runs: |
      autoreconf -vfi
      cp drill/install-sh .

  - uses: autoconf/configure
    with:
      opts: |
        --build=$CBUILD \
        --host=$CHOST \
        --prefix=/usr \
        --sysconfdir=/etc \
        --mandir=/usr/share/man \
        --infodir=/usr/share/info \
        --localstatedir=/var \
        --with-drill \
        --disable-dane-verify

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: "drill"
    description: "DNS(SEC) query tool ala dig"
    pipeline:
      - runs: |
          mkdir -p "${{targets.subpkgdir}}"/usr/bin
          mv "${{targets.destdir}}"/usr/bin/drill "${{targets.subpkgdir}}"/usr/bin/
    test:
      pipeline:
        - runs: |
            drill version
            drill help

  - name: ldns-doc
    description: ldns docs
    pipeline:
      - uses: split/manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  ignore-regex-patterns:
    - "^testns.*"
    - "^release.*"
    - "^ldns.*"
  github:
    identifier: NLnetLabs/ldns
    use-tag: true

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        ldns-config --version
        ldns-config --help
    - uses: test/pkgconf
    - uses: test/tw/ldd-check
    - uses: test/no-docs
