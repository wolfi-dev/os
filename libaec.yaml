package:
  name: libaec
  description: Adaptive Entropy Coding library
  url: https://gitlab.dkrz.de/k202009/libaec
  version: "1.1.7"
  epoch: 0
  copyright:
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - wolfi-baselayout

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/MathisRosenhauer/libaec
      expected-commit: 0c4c01463d2c64a112a61271d317b74efb660608
      tag: v${{package.version}}

  - uses: cmake/configure

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

subpackages:
  - name: "libaec-static"
    description: "libaec static libraries"
    pipeline:
      - uses: split/static
    test:
      pipeline:
        - uses: test/tw/staticpackage

  - name: "libaec-dev"
    description: "libaec development headers"
    pipeline:
      - uses: split/dev
    test:
      pipeline:
        - uses: test/tw/ldd-check

  - name: "libsz"
    description: "free alternative to szip library"
    pipeline:
      - working-directory: ${{targets.contextdir}}/usr/lib
        runs: |
          mv "${{targets.destdir}}"/usr/lib/libsz* .
    test:
      pipeline:
        - uses: test/tw/ldd-check

test:
  pipeline:
    - uses: test/tw/ldd-check

update:
  enabled: true
  github:
    identifier: MathisRosenhauer/libaec
    use-tag: true
    strip-prefix: v
