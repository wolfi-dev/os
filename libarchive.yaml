package:
  name: libarchive
  version: 3.7.2
  epoch: 0
  description: "multi-format archive and compression library"
  copyright:
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - wolfi-base
      - busybox
      - ca-certificates-bundle
      - build-base
      - bzip2-dev
      - lz4-dev
      - zstd-dev
      - zlib-dev
      - xz-dev
      - acl-dev
      - attr-dev
      - openssl-dev
      - expat-dev

pipeline:
  - uses: fetch
    with:
      uri: http://libarchive.org/downloads/libarchive-${{package.version}}.tar.xz
      expected-sha256: 04357661e6717b6941682cde02ad741ae4819c67a260593dfb2431861b251acb

  - uses: autoconf/configure
    with:
      opts: --without-xml2

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: "libarchive-doc"
    description: "libarchive documentation"
    pipeline:
      - uses: split/manpages
      - uses: split/infodir

  - name: "libarchive-dev"
    description: "libarchive development headers"
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - bzip2-dev
        - lz4-dev
        - xz-dev
        - zlib-dev
        - zstd-dev

  - name: "libarchive-tools"
    description: "bsdtar and bsdcpio programs from libarchive"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/bin
          mv "${{targets.contextdir}}"/usr/bin/bsd* "${{targets.contextdir}}"/usr/bin/

update:
  enabled: true
  release-monitor:
    identifier: 1558
