# Generated from https://git.alpinelinux.org/aports/plain/community/libasyncns/APKBUILD
package:
  name: libasyncns
  version: "0.8"
  epoch: 5
  description: Asynchronous Name Service Library
  copyright:
    - license: LGPL-2.1

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libtool
      - lynx

pipeline:
  - uses: git-checkout
    with:
      repository: https://git.0pointer.net/clone/libasyncns.git
      depth: -1 # repository does not support shallow clones
      tag: v${{package.version}}
      expected-commit: fdea9803a04417f00089f9e61a7b009405a3abb1

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: libasyncns-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - libasyncns
    description: libasyncns dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

  - name: libasyncns-doc
    description: libasyncns docs
    pipeline:
      - name: "relocate docs"
        runs: |
          mkdir -p "${{targets.subpkgdir}}"/usr/share
          mv "${{targets.destdir}}"/usr/share/doc "${{targets.subpkgdir}}"/usr/share
    test:
      pipeline:
        - uses: test/docs

update:
  release-monitor:
    identifier: 14598

test:
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/no-docs
