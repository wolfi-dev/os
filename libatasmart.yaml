package:
  name: libatasmart
  version: "0.19"
  epoch: 4
  description: ATA S.M.A.R.T. Reading and Parsing Library
  copyright:
    - license: LGPL-2.1-or-later

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - linux-headers
      - openssf-compiler-options
      - systemd-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 61f0ea345f63d28ab2ff0dc352c22271661b66bf09642db3a4049ac9dbdb0f8d
      uri: https://0pointer.de/public/libatasmart-${{package.version}}.tar.xz

  - uses: autoconf/configure
    with:
      opts: |
        --disable-static \
        --localstatedir=/var

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: libatasmart-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - libatasmart
        - systemd-dev
    description: libatasmart dev
    test:
      pipeline:
        - uses: test/pkgconf

  - name: libatasmart-doc
    pipeline:
      - uses: split/manpages
    description: libatasmart manpages

update:
  enabled: true
  release-monitor:
    identifier: 14599

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        skdump --help
