# Generated from https://git.alpinelinux.org/aports/plain/main/libdrm/APKBUILD
package:
  name: libdrm
  version: "2.4.128"
  epoch: 0
  description: Userspace interface to kernel DRM services
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - libpciaccess-dev
      - linux-headers
      - meson
      - systemd-dev
      - xmlto
  environment:
    CFLAGS: "-O2"

pipeline:
  - uses: fetch
    with:
      expected-sha256: 343601a4fa0d705324bcbcecdb7ee7b501c9c5765543ad0505f2a6cdca1f7885
      uri: https://gitlab.freedesktop.org/mesa/drm/-/archive/libdrm-${{package.version}}/drm-libdrm-${{package.version}}.tar.gz

  - runs: |
      meson \
        -Db_lto=true \
        -Dfreedreno=enabled \
        -Dtegra=enabled \
        -Domap=enabled \
        -Dexynos=enabled \
        -Dvc4=enabled \
        -Detnaviv=enabled \
        -Dudev=true \
        -Dinstall-test-programs=true \
        -Dtests=true \
        --prefix=/usr \
        . output
      meson compile --verbose -C output
      DESTDIR="${{targets.destdir}}" meson install --no-rebuild -C output

  - uses: strip

subpackages:
  - name: libdrm-dev
    pipeline:
      - uses: split/dev
    description: libdrm dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

update:
  enabled: true
  release-monitor:
    identifier: 1596

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        etnaviv_cmd_stream_test --version
        exynos_fimg2d_event --version
        exynos_fimg2d_perf --version
        exynos_fimg2d_test --version
        modetest --version
        vbltest --version
        etnaviv_cmd_stream_test --help
        exynos_fimg2d_event --help
        exynos_fimg2d_perf --help
        exynos_fimg2d_test --help
        modetest --help
        vbltest --help
    - uses: test/tw/ldd-check
