# Generated from https://git.alpinelinux.org/aports/plain/main/libdrm/APKBUILD
package:
  name: libdrm
  version: "2.4.125"
  epoch: 1
  description: Userspace interface to kernel DRM services
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libpciaccess-dev
      - linux-headers
      - meson
      - systemd-dev
      - xmlto

pipeline:
  - uses: fetch
    with:
      expected-sha256: dd3e86a9fae99322d6430b28397f8e03ac4fb8deeed9a6e214bd5ced86db68c6
      uri: https://gitlab.freedesktop.org/mesa/drm/-/archive/libdrm-${{package.version}}/drm-libdrm-${{package.version}}.tar.gz

  - runs: |
      CFLAGS="$CFLAGS -O2" CPPFLAGS="$CPPFLAGS -O2" CXXFLAGS="$CXXFLAGS -O2" meson \
        -Db_lto=true \
        -Dfreedreno=enabled \
        -Dtegra=enabled \
        -Domap=enabled \
        -Dexynos=enabled \
        -Dvc4=enabled \
        -Detnaviv=enabled \
        -Dudev=true \
        -Dinstall-test-programs=true \
        -Dtests=true \
        --prefix=/usr \
        . output
      meson compile -C output
      DESTDIR="${{targets.destdir}}" meson install --no-rebuild -C output

  - uses: strip

subpackages:
  - name: libdrm-dev
    pipeline:
      - uses: split/dev
    description: libdrm dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

update:
  enabled: true
  release-monitor:
    identifier: 1596

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        etnaviv_cmd_stream_test --version
        exynos_fimg2d_event --version
        exynos_fimg2d_perf --version
        exynos_fimg2d_test --version
        modetest --version
        vbltest --version
        etnaviv_cmd_stream_test --help
        exynos_fimg2d_event --help
        exynos_fimg2d_perf --help
        exynos_fimg2d_test --help
        modetest --help
        vbltest --help
    - uses: test/tw/ldd-check
