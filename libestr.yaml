package:
  name: libestr
  version: 0.1.11
  epoch: 0
  description: essentials for string handling (and a bit more)
  copyright:
    - license: LGPL-2.1-or-later

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libtool

pipeline:
  - uses: git-checkout
    with:
      expected-commit: e385ee85ace19466ebaec690197b89df8ab3c72e
      repository: https://github.com/rsyslog/libestr
      tag: v${{package.version}}

  - runs: |
      libtoolize --copy
      aclocal -I m4 --install
      autoheader
      autoconf
      automake --foreign --add-missing --force-missing --copy
      ./configure --prefix=/usr

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: libestr
    description: ${{package.description}}
    pipeline:
      - runs: |
          mkdir -p "${{targets.subpkgdir}}"/usr/lib
          mv "${{targets.destdir}}"/usr/lib/libestr.so* "${{targets.subpkgdir}}"/usr/lib/

  - name: libestr-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - libestr
    description: libestr dev

update:
  enabled: true
  github:
    identifier: rsyslog/libestr
    strip-prefix: v
    tag-filter: v

test:
  environment:
    contents:
      packages:
        - pkgconf
        - libestr
        - libestr-dev
  pipeline:
    - runs: |
        pkg-config --libs libestr | grep /usr/lib
