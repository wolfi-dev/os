# Generated from https://git.alpinelinux.org/aports/plain/community/libgeotiff/APKBUILD
package:
  name: libgeotiff
  version: "1.7.4"
  epoch: 6 # glibc: 2.43->2.44
  description: TIFF based interchange format for georeferenced raster imagery
  copyright:
    - license: MIT
  resources:
    cpu: "2"
    memory: 6Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - glibc-2.44-dev
      - libtool
      - proj-dev
      - tiff-dev

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 96024f677642486f97cac43659bef57f4ed0590b
      repository: https://github.com/OSGeo/libgeotiff
      tag: ${{package.version}}

  - working-directory: /home/build/libgeotiff
    pipeline:
      - uses: cmake/configure
      - uses: cmake/build
      - uses: cmake/install

  - uses: strip

subpackages:
  - name: libgeotiff-dev
    pipeline:
      - uses: split/dev
    description: libgeotiff dev
    dependencies:
      runtime:
        - curl-dev
        - proj-dev
        - sqlite-dev
        - tiff-dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

  - name: libgeotiff-doc
    pipeline:
      - uses: split/manpages
    description: libgeotiff manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  github:
    identifier: OSGeo/libgeotiff
    tag-filter: 1.

test:
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/no-docs
