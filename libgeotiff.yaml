# Generated from https://git.alpinelinux.org/aports/plain/community/libgeotiff/APKBUILD
package:
  name: libgeotiff
  version: 1.7.3
  epoch: 2
  description: TIFF based interchange format for georeferenced raster imagery
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libtool
      - openssf-compiler-options
      - proj-dev
      - tiff-dev

pipeline:
  - uses: git-checkout
    with:
      expected-commit: d2c72dba35ac9d1af2201191064ca4cbe7f57f11
      repository: https://github.com/OSGeo/libgeotiff
      tag: ${{package.version}}

  - working-directory: /home/build/libgeotiff
    pipeline:
      - uses: cmake/configure
      - uses: cmake/build
      - uses: cmake/install

  - uses: strip

subpackages:
  - name: libgeotiff-dev
    pipeline:
      - uses: split/dev
    description: libgeotiff dev
    dependencies:
      runtime:
        - curl-dev
        - proj-dev
        - sqlite-dev
        - tiff-dev
    test:
      pipeline:
        - uses: test/pkgconf

  - name: libgeotiff-doc
    pipeline:
      - uses: split/manpages
    description: libgeotiff manpages

update:
  enabled: true
  github:
    identifier: OSGeo/libgeotiff
    tag-filter: 1.

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        makegeo --version
        makegeo --help
