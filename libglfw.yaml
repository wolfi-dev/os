package:
  name: libglfw
  version: "3.5.1"
  epoch: 1 # glibc: 2.43->2.44
  description: A multi-platform library for OpenGL, OpenGL ES, Vulkan, window and input
  copyright:
    - license: Zlib
  resources:
    cpu: "2"
    memory: 4Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - cmake
      - doxygen
      - glibc-2.44-dev
      - libglvnd-dev
      - libx11-dev
      - libxcursor-dev
      - libxi-dev
      - libxinerama-dev
      - libxrandr-dev
      - libxrender-dev
      - pkgconf
      - pkgconf-dev
      - xorg-server-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/glfw/glfw
      tag: ${{package.version}}
      expected-commit: d9d6f0f1f967807ffade6598ea9a631ebaf37a56

  - uses: cmake/configure
    with:
      opts: |
        -DGLFW_BUILD_WAYLAND=0 -DGLFW_LIBRARY_TYPE=SHARED

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

subpackages:
  - name: "libglfw-doc"
    description: "libglfw documentation"
    pipeline:
      - uses: split/manpages
      - uses: split/infodir
      - runs: |
          mkdir -p "${{targets.subpkgdir}}"/usr/share/doc
          mv "${{targets.destdir}}"/usr/share/doc/GLFW "${{targets.subpkgdir}}"/usr/share/doc/
    test:
      pipeline:
        - uses: test/docs

  - name: "libglfw-dev"
    description: "glfw development headers"
    pipeline:
      - uses: split/dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

update:
  enabled: true
  github:
    identifier: glfw/glfw

test:
  pipeline:
    - uses: test/tw/ldd-check
