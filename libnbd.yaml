package:
  name: libnbd
  description: NBD client library in userspace
  url: https://gitlab.com/nbdkit/libnbd
  version: "1.25.3"
  epoch: 0
  copyright:
    - license: LGPL-2.1-only

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - libtool
      - wolfi-baselayout

vars:
  channel: "development"

var-transforms:
  - from: ${{package.version}}
    match: ^(\d.\d+).*
    replace: $1
    to: major_version

pipeline:
  # NOTE: not using git checkout because we would need ocalmc to generate some source files
  - uses: fetch
    with:
      uri: https://download.libguestfs.org/libnbd/${{vars.major_version}}-${{vars.channel}}/${{package.name}}-${{package.version}}.tar.gz
      expected-sha512: 25f4a06eb207cc9ef4b4e43b04f6202c7d62275d47ab7964fc0f8c9981fabc9ab91bc4d2ee01b399b7741aa0b45b33b0ce411cc8a558fc2419dda62dfb0d5b5d
      strip-components: 1

  - runs: |
      # 1.23.13 only builds if TLS_PRIORITY is set even though it's only
      # #define'd by the build system when building against gnutls (which we
      # don't): we set it to some value here to unblock the build
      sed -i 's/#undef TLS_PRIORITY/#define TLS_PRIORITY "unused-but-expected-by-build"/' ./config.h.in

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: "libnbd-static"
    description: "libnbd static libraries"
    pipeline:
      - uses: split/static

  - name: "libnbd-dev"
    description: "libnbd development headers"
    pipeline:
      - uses: split/dev
    test:
      pipeline:
        - uses: test/tw/ldd-check

test:
  pipeline:
    - uses: test/tw/ldd-check

update:
  enabled: true
  release-monitor:
    identifier: 371504
