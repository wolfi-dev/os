package:
  name: libpaper
  version: "2.2.6"
  epoch: 2
  description: Library for handling paper characteristics
  copyright:
    - license: GPL-2.0-only

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle

pipeline:
  - uses: fetch
    with:
      expected-sha256: 500d39dc58768ee09688738c8b5bfe07640ba2fd6c25a6dc78810eb69c719e93
      uri: https://github.com/rrthomas/libpaper/releases/download/v${{package.version}}/libpaper-${{package.version}}.tar.gz

  - uses: autoconf/configure
    with:
      opts: --disable-static

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: libpaper-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - libpaper
    description: libpaper dev
    test:
      pipeline:
        - uses: test/tw/ldd-check

  - name: libpaper-doc
    pipeline:
      - uses: split/manpages
    description: libpaper manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  github:
    identifier: rrthomas/libpaper
    strip-prefix: v

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        paper --version
        paperconf -h
        paper --help
    - uses: test/tw/ldd-check
