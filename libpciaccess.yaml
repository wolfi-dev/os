# Generated from https://git.alpinelinux.org/aports/plain/main/libpciaccess/APKBUILD
package:
  name: libpciaccess
  version: 0.18.1
  epoch: 5
  description: X11 PCI access library
  copyright:
    - license: X11

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - hwdata-pci
      - meson
      - util-macros

pipeline:
  - uses: git-checkout
    with:
      repository: https://gitlab.freedesktop.org/xorg/lib/libpciaccess
      tag: libpciaccess-${{package.version}}
      expected-commit: 2ec2576cabefef1eaa5dd9307c97de2e887fc347

  - runs: |
      meson \
        -Db_lto=true \
        --prefix=/usr \
        . output
      meson compile -C output
      DESTDIR="${{targets.destdir}}" meson install --no-rebuild -C output

  - uses: strip

subpackages:
  - name: libpciaccess-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - libpciaccess
        - hwdata-pci
    description: libpciaccess dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

update:
  enabled: true
  release-monitor:
    identifier: 1703

test:
  pipeline:
    - uses: test/tw/ldd-check
