# Generated from https://git.alpinelinux.org/aports/plain/community/libpipeline/APKBUILD
package:
  name: libpipeline
  version: 1.5.8
  epoch: 4 # use git/gnulib-bootstrap pipeline
  description: C pipeline manipulation library
  copyright:
    - license: GPL-3.0-or-later
  resources:
    cpu: "2"
    memory: 4Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libtool
      - pkgconf-dev
      - python3

pipeline:
  - uses: git-checkout
    with:
      repository: https://gitlab.com/libpipeline/libpipeline
      tag: ${{package.version}}
      expected-commit: dd37cf76496b4b999509bd133657c1fc021e3a5f

  - uses: git/gnulib-bootstrap

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: libpipeline-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - libpipeline
    description: libpipeline dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

  - name: libpipeline-doc
    pipeline:
      - uses: split/manpages
    description: libpipeline manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 1704

test:
  pipeline:
    - uses: test/tw/ldd-check
