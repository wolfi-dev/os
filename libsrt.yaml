package:
  name: libsrt
  version: "1.5.5"
  epoch: 0
  description: "Secure Reliable Transport (SRT)"
  copyright:
    - license: MPL-2.0

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - cmake-3
      - openssl
      - openssl-hardened-dev
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/Haivision/srt
      tag: v${{package.version}}
      expected-commit: b6b4ae990daa8193625a4ddeaeaed03023b23125

  - uses: cmake/configure
    with:
      opts: |
        -DUSE_ENCLIB=openssl-evp \
        -DENABLE_STATIC=OFF

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

update:
  enabled: true
  github:
    identifier: Haivision/srt
    strip-prefix: v

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        srt-file-transmit --version
        srt-live-transmit --version
        srt-file-transmit --help
        srt-live-transmit --help
    - uses: test/pkgconf
    - uses: test/tw/ldd-check
