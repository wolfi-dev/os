package:
  name: libsrt
  version: "1.5.7"
  epoch: 1
  description: "Secure Reliable Transport (SRT)"
  copyright:
    - license: MPL-2.0

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - cmake-3
      - glibc-2.43-dev
      - openssl
      - openssl-hardened-dev
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/Haivision/srt
      tag: v${{package.version}}
      expected-commit: 899348d8318eb9a3c5a5b6ec43c4a1114288773a

  - uses: cmake/configure
    with:
      opts: |
        -DUSE_ENCLIB=openssl-evp \
        -DENABLE_STATIC=OFF

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

update:
  enabled: true
  github:
    identifier: Haivision/srt
    strip-prefix: v

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        srt-file-transmit --version
        srt-live-transmit --version
        srt-file-transmit --help
        srt-live-transmit --help
    - uses: test/pkgconf
    - uses: test/tw/ldd-check
