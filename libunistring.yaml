package:
  name: libunistring
  version: "1.4.2"
  epoch: 0 # go/wolfi-rsc/libunistring
  description: Library for manipulating Unicode strings and C strings
  copyright:
    - license: GPL-2.0-or-later OR LGPL-3.0-or-later
  resources:
    cpu: "4"
    memory: 6Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - bash
      - build-base
      - busybox
      - ca-certificates-bundle
      - glibc-iconv
      - posix-libc-utils

pipeline:
  - uses: fetch
    with:
      expected-sha256: 5b46e74377ed7409c5b75e7a96f95377b095623b689d8522620927964a41499c
      uri: https://ftpmirror.gnu.org/gnu/libunistring/libunistring-${{package.version}}.tar.xz

  - uses: autoconf/configure
    with:
      opts: |
        --host=${{host.triplet.gnu}} \
        --build=${{host.triplet.gnu}} \
        --sysconfdir=/etc \
        --mandir=/usr/share/man \
        --infodir=/usr/share/info \
        --localstatedir=/var \

  - uses: autoconf/make

  - runs: |
      make check

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: libunistring-doc
    pipeline:
      - uses: split/manpages
      - runs: |
          mkdir -p ${{targets.subpkgdir}}/usr/share/doc
          mv ${{targets.destdir}}/usr/share/doc/libunistring ${{targets.subpkgdir}}/usr/share/doc/libunistring
    test:
      pipeline:
        - uses: test/docs

  - name: libunistring-static
    pipeline:
      - uses: split/static
    description: libunistring static
    test:
      pipeline:
        - name: "verify some .a file was shipped in this static library package"
          runs: find /usr/lib -name *.a -type f | grep ${{package.name}}

  - name: libunistring-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - libunistring
        - glibc-iconv
    description: libunistring dev
    test:
      pipeline:
        - uses: test/tw/ldd-check

update:
  enabled: true
  release-monitor:
    identifier: 1747

test:
  pipeline:
    - uses: test/tw/ldd-check
