# Generated from https://git.alpinelinux.org/aports/plain/main/libxkbcommon/APKBUILD
package:
  name: libxkbcommon
  version: "1.9.2"
  epoch: 0
  description: keyboard handling library
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - autoconf
      - automake
      - bison
      - build-base
      - busybox
      - ca-certificates-bundle
      - libxcb-dev
      - libxml2-dev
      - meson
      - wayland-dev
      - wayland-protocols
      - xkeyboard-config
      - xkeyboard-config-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/xkbcommon/libxkbcommon.git
      tag: xkbcommon-${{package.version}}
      expected-commit: dd642359f8d43c09968e34ca7f1eb1121b2dfd70

  - runs: |
      meson \
        --prefix=/usr \
        -Denable-wayland=true \
        -Denable-docs=false \
        -Denable-x11=true \
        -Ddefault_library=both \
        build
      meson compile -C build
      DESTDIR="${{targets.destdir}}" meson install --no-rebuild -C build

  - uses: strip

subpackages:
  - name: libxkbcommon-static
    pipeline:
      - uses: split/static
    description: libxkbcommon static

  - name: libxkbcommon-dev
    pipeline:
      - uses: split/dev
    description: libxkbcommon dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

update:
  enabled: true
  git:
    strip-prefix: xkbcommon-

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        xkbcli --version
        xkbcli --help
    - uses: test/tw/ldd-check
