# Generated from https://git.alpinelinux.org/aports/plain/main/libxkbcommon/APKBUILD
package:
  name: libxkbcommon
  version: 1.7.0
  epoch: 1
  description: keyboard handling library
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - autoconf
      - automake
      - bison
      - build-base
      - busybox
      - ca-certificates-bundle
      - libxcb-dev
      - libxml2-dev
      - meson
      - openssf-compiler-options
      - wayland-dev
      - wayland-protocols
      - xkeyboard-config
      - xkeyboard-config-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 65782f0a10a4b455af9c6baab7040e2f537520caa2ec2092805cdfd36863b247
      uri: https://xkbcommon.org/download/libxkbcommon-${{package.version}}.tar.xz

  - runs: |
      meson \
        --prefix=/usr \
        -Denable-wayland=true \
        -Denable-docs=false \
        -Denable-x11=true \
        -Ddefault_library=both \
        build
      meson compile -C build
      DESTDIR="${{targets.destdir}}" meson install --no-rebuild -C build

  - uses: strip

subpackages:
  - name: libxkbcommon-static
    pipeline:
      - uses: split/static
    description: libxkbcommon static

  - name: libxkbcommon-dev
    pipeline:
      - uses: split/dev
    description: libxkbcommon dev
    test:
      pipeline:
        - uses: test/pkgconf

update:
  enabled: true
  release-monitor:
    identifier: 1780

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        xkbcli --version
        xkbcli --help
