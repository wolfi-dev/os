# Generated from https://git.alpinelinux.org/aports/plain/main/libxshmfence/APKBUILD
package:
  name: libxshmfence
  version: 1.3.3
  epoch: 4 # glibc: 2.43->2.44
  description: X11 shared memory fences
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - glibc-2.44-dev
      - libtool
      - linux-headers
      - pkgconf-dev
      - util-macros-dev
      - xorgproto

pipeline:
  - uses: git-checkout
    with:
      repository: https://gitlab.freedesktop.org/xorg/lib/libxshmfence
      tag: libxshmfence-${{package.version}}
      expected-commit: 89f064748554e11832a3ec783945e1f4c7fe846e

  - uses: autoconf/configure
    with:
      opts: |
        --disable-static \
        --enable-futex \
        CFLAGS="-flto=auto"

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: libxshmfence-dev
    pipeline:
      - uses: split/dev
    description: libxshmfence dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

test:
  pipeline:
    - uses: test/tw/ldd-check

update:
  enabled: true
  release-monitor:
    identifier: 1792
