package:
  name: libxslt
  version: 1.1.42
  epoch: 1
  description: XML stylesheet transformation library
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libgcrypt-dev
      - libgpg-error-dev
      - libtool
      - libxml2-dev
      - openssf-compiler-options
      - pkgconf-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://gitlab.gnome.org/GNOME/libxslt.git
      tag: v${{package.version}}
      expected-commit: 165219e5a3fc79cc4f529785e6e60f19a4b9222a

  - runs: ./autogen.sh

  - uses: autoconf/configure
    with:
      opts: --with-python=no

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: libxslt-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - libxslt
    description: libxslt dev
    test:
      pipeline:
        - runs: |
            xslt-config --version
            xslt-config --help
        - uses: test/pkgconf

  - name: libxslt-doc
    pipeline:
      - uses: split/manpages
    description: libxslt manpages

update:
  enabled: true
  release-monitor:
    identifier: 13301

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        xsltproc --version
