# Generated from https://git.alpinelinux.org/aports/plain/main/libxxf86vm/APKBUILD
package:
  name: libxxf86vm
  version: "1.1.7"
  epoch: 2 # go/wolfi-rsc/libxxf86vm
  description: X11 XFree86 video mode extension library
  copyright:
    - license: MIT
  resources:
    cpu: "1"
    memory: 1Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libtool
      - libx11-dev
      - libxext-dev
      - pkgconf-dev
      - util-macros
      - xorgproto

pipeline:
  - uses: git-checkout
    with:
      repository: https://gitlab.freedesktop.org/xorg/lib/libxxf86vm
      tag: libXxf86vm-${{package.version}}
      expected-commit: 7f200bf00e3e288e47265c01decc401616c57f10

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: libxxf86vm-dev
    pipeline:
      - uses: split/dev
    description: libxxf86vm dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

  - name: libxxf86vm-doc
    pipeline:
      - uses: split/manpages
    description: libxxf86vm manpages
    test:
      pipeline:
        - uses: test/docs

test:
  pipeline:
    - uses: test/tw/ldd-check

update:
  enabled: true
  release-monitor:
    identifier: 1799
