Author: Chris Herborth <chris.herborth@chainguard.dev>
Forwarded: not-needed
Description: Fix CVEs in dependencies where we can, document the rest.
---
diff --git a/Gemfile.template b/Gemfile.template
index b3aec17..6b90328 100644
--- a/Gemfile.template
+++ b/Gemfile.template
@@ -39,7 +39,31 @@ gem "webmock", "~> 3", :group => :development
 gem "simplecov", "~> 0.22.0", :group => :development
 gem "simplecov-json", require: false, :group => :development
 gem "murmurhash3", "= 0.1.6" # Pins until version 0.1.7-java is released
-gem "date", "= 3.3.3"
+gem "date", "~> 3.4.1"
 gem "thwait"
 gem "bigdecimal", "~> 3.1"
 gem "jar-dependencies", "= 0.5.4" # Pin to avoid conflict with default
+
+# Attempt to address CVEs
+gem "erb", "~> 4.0.4.1"
+gem "faraday", "~> 2.14.2"
+gem "net-imap", "~> 0.6.4"
+gem "nokogiri", "~> 1.19.3"
+gem "logstash-input-beats", "~> 7.0.9"
+gem "logstash-input-http", "~> 4.1.8"
+gem "logstash-input-tcp", "~> 7.0.9"
+# jrjackson 0.5.0 bundles jackson-core 2.21.2 (fixes GHSA-72hv-8253-57qq)
+gem "jrjackson", "= 0.5.0"
+# jruby-openssl 0.16.0 bundles Bouncy Castle 1.84 (fixes GHSA-c3fc, p93r, wg6q)
+gem "jruby-openssl", "= 0.16.0"
+
+# Remaining CVEs blocked on upstream gems that have not released a fixed version:
+#
+# - jackson-core 2.16.2 inside logstash-input-azure_event_hubs 1.5.5
+#   (Medium GHSA-72hv-8253-57qq fixed in 2.18.6)
+# - jackson-core 2.15.0 shaded inside logstash-filter-elastic_integration 9.4.3
+#   (Medium GHSA-72hv-8253-57qq fixed in 2.18.6)
+#
+# Remaining CVEs blocked on getting Logstash to use our JDK, JRuby, and
+# JRuby-OpenSSL (OS-1796) — the JRuby stdlib at
+# /usr/share/logstash/vendor/jruby/lib/ruby/stdlib/ still bundles BC 1.83.
diff --git a/logstash-core/build.gradle b/logstash-core/build.gradle
index fb0ad2b..3fc81a3 100644
--- a/logstash-core/build.gradle
+++ b/logstash-core/build.gradle
@@ -247,6 +247,7 @@ dependencies {
     }
     implementation 'org.hdrhistogram:HdrHistogram:2.2.2'
     implementation 'org.javassist:javassist:3.30.2-GA'
+    implementation 'io.netty:netty-codec:4.1.133.Final'
     testImplementation "org.apache.logging.log4j:log4j-core-test:${log4jVersion}"
     testImplementation 'org.hamcrest:hamcrest:2.2'
     testImplementation 'org.hamcrest:hamcrest-library:2.2'
