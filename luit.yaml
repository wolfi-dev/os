package:
  name: luit
  version: "20250912"
  epoch: 1
  description: Filter that can be run between an arbitrary application and a UTF-8 terminal emulator
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libfontenc-dev
      - libx11-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 46958060e66f35bcb8a51ba22da1c13d726d28a86c1cf520511bcf7914bef39e
      uri: https://invisible-mirror.net/archives/${{package.name}}/${{package.name}}-${{package.version}}.tgz

  - uses: autoconf/configure
    with:
      opts: |
        --enable-fontenc

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: luit-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - luit
    description: luit dev
    # test added by a robot. The APK was empty when it was checked,
    # which may not be true forever. If this test fails in the future, please
    # replace it with a more accurate test.
    test:
      pipeline:
        - uses: test/emptypackage

  - name: luit-doc
    description: luit docs
    pipeline:
      - uses: split/manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 15047

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        luit -h
    - uses: test/no-docs
