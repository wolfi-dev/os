package:
  name: lychee
  version: 0.14.0
  epoch: 0
  description: "Fast, async, stream-based link checker written in Rust"
  copyright:
    - license: Apache-2.0 AND MIT

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - openssl-dev
      - rust

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/lycheeverse/lychee
      tag: v${{package.version}}
      expected-commit: bcc00441eba7888d2a4e711385229d0a07f6f09c

  - runs: |
      cargo build --release
      install -Dm755 target/release/lychee "${{targets.destdir}}"/usr/bin/lychee

  - uses: strip

update:
  enabled: true
  github:
    identifier: lycheeverse/lychee
    strip-prefix: v
