package:
  name: mimalloc
  version: "1.9.11"
  epoch: 0 # add strip pipeline step
  description: "A compact general purpose allocator with excellent performance"
  copyright:
    - license: MIT
  resources:
    cpu: "1"
    memory: 1Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - wolfi-baselayout

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/microsoft/mimalloc
      tag: v${{package.version}}
      expected-commit: ff11fa73452a1339ab95c8c39251d1c877041d89

  - uses: cmake/configure

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

subpackages:
  - name: "mimalloc-dev"
    description: "headers for mimalloc"
    pipeline:
      - uses: split/dev
    test:
      pipeline:
        - uses: test/tw/devpackage
        - uses: test/pkgconf
        - uses: test/tw/ldd-check
    dependencies:
      runtime:
        - mimalloc

test:
  environment:
    contents:
      packages:
        - stress-ng
  pipeline:
    - uses: test/tw/ldd-check
    - runs: |
        LD_PRELOAD=/usr/lib/libmimalloc.so.1 stress-ng --vdso 1 -t 5 --metrics

update:
  enabled: true
  github:
    identifier: microsoft/mimalloc
    strip-prefix: v
    tag-filter: v1.
    use-tag: true
