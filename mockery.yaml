package:
  name: mockery
  version: 2.49.0
  epoch: 0
  description: A mock code autogenerator for Go
  copyright:
    - license: BSD-3-Clause

environment:
  contents:
    packages:
      - go
      - openssf-compiler-options
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/vektra/mockery
      expected-commit: a27cb03053efde18d2f77e76a228a78c567a2643
      tag: v${{package.version}}

  - runs: |
      mkdir -p ${{targets.destdir}}/usr/bin/
      go build -v -o ${{targets.destdir}}/usr/bin/mockery -ldflags="-s -w -X github.com/vektra/mockery/v2/pkg/logging.SemVer=v${{package.version}}"

  - uses: strip

update:
  enabled: true
  github:
    identifier: vektra/mockery
    strip-prefix: v
    use-tag: true

test:
  environment:
    contents:
      packages:
        - posix-libc-utils
  pipeline:
    - runs: |
        mockery --version
        mockery --help
