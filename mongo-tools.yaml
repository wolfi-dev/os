package:
  name: mongo-tools
  version: "100.12.1"
  epoch: 0
  description: Tools for MongoDB
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - go
      - krb5-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/mongodb/mongo-tools
      tag: ${{package.version}}
      expected-commit: 6ed2d84211f54467fd6495a381a3e7102f48ba42

  - uses: patch
    with:
      patches: release-platform.patch

  - runs: |
      ./make build
      mkdir -p ${{targets.destdir}}/usr/bin
      mv bin/* ${{targets.destdir}}/usr/bin

  - uses: strip

update:
  enabled: true
  github:
    identifier: mongodb/mongo-tools
    strip-prefix: v
    use-tag: true
    tag-filter: "100"

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        bsondump --version
        mongodump --version
        mongoexport --version
        mongofiles --version
        mongoimport --version
        mongorestore --version
        mongostat --version
        mongotop --version
        bsondump --help
        mongodump --help
        mongoexport --help
        mongofiles --help
        mongoimport --help
        mongorestore --help
        mongostat --help
        mongotop --help
