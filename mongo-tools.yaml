package:
  name: mongo-tools
  version: "100.11.0"
  epoch: 1
  description: Tools for MongoDB
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - go
      - krb5-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/mongodb/mongo-tools
      tag: ${{package.version}}
      expected-commit: 198ec6c6771d2b734969bd2a673ca8888bfeeb0d

  - uses: patch
    with:
      patches: release-platform.patch

  - runs: |
      ./make build
      mkdir -p ${{targets.destdir}}/usr/bin
      mv bin/* ${{targets.destdir}}/usr/bin

  - uses: strip

update:
  enabled: true
  github:
    identifier: mongodb/mongo-tools
    strip-prefix: v
    use-tag: true
    tag-filter: "100"

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        bsondump --version
        mongodump --version
        mongoexport --version
        mongofiles --version
        mongoimport --version
        mongorestore --version
        mongostat --version
        mongotop --version
        bsondump --help
        mongodump --help
        mongoexport --help
        mongofiles --help
        mongoimport --help
        mongorestore --help
        mongostat --help
        mongotop --help
