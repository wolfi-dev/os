package:
  name: mtools
  version: "4.0.48"
  epoch: 0
  description: collection of utilities to access MS-DOS disks from Unix without mounting them
  copyright:
    - license: GPL-3.0-or-later
  dependencies:
    runtime:
      - merged-sbin
      - wolfi-baselayout

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - gawk
      - util-linux-dev

pipeline:
  - uses: fetch
    with:
      uri: https://ftp.gnu.org/gnu/mtools/mtools-${{package.version}}.tar.gz
      expected-sha256: 88f273c6629ad5464482e98a9980727f585eead8736c50717c95e36f05ca05dc

  - uses: autoconf/configure
    with:
      opts: |
        --host=${{host.triplet.gnu}} \
        --build=${{host.triplet.gnu}} \
        --mandir=/usr/share/man \
        --enable-elf-shlibs \
        --enable-lto \
        --enable-symlink-install \
        --infodir=/usr/share/info \
        --prefix='/usr' \
        --with-root-prefix='' \
        --sbindir="/usr/bin" \
        --without-x

  - uses: autoconf/make

  - runs: |
      make -j1 MKDIR_P="install -d"  DESTDIR=${{targets.destdir}} install

  - uses: strip

subpackages:
  - name: "mtools-doc"
    description: "mtools documentation"
    pipeline:
      - uses: split/manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 2028

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        mtools --version
