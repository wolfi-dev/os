# Disabling this patch since it is incompatible with Neo4j 2025.12 and causes a NoClassDefFoundError during Bolt transport initialization.
# Newer versions of Neo4j use Netty 4.1.129 Final, we could try to find upstream patches to make this version compatible with Neo4j
# patches:
#   - groupId: io.netty
#     artifactId: netty-codec-http
#     version: 4.2.9.Final
