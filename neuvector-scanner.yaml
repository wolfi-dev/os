package:
  name: neuvector-scanner
  version: "4.132"
  epoch: 0 # CVE-2026-32280
  description: NeuVector vulnerability scanner for the SUSE NeuVector Container Security Platform
  copyright:
    - license: Apache-2.0
  dependencies:
    runtime:
      - ca-certificates
      - neuvector-db
      - neuvector-scanner-monitor
      - neuvector-scanner-task
      - neuvector-sigstore-interface
  resources:
    cpu: "3"
    memory: 5Gi
  test-resources:
    cpu: "1"
    memory: 2Gi

environment:
  contents:
    packages:
      - busybox
      - go-1.25

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/neuvector/scanner
      tag: v${{package.version}}
      expected-commit: 885cb2dbd5bbbd11d93a9eaa435a616756686785

  - uses: go/build
    with:
      modroot: .
      packages: .
      output: scanner
      prefix: usr/local
      vendor: true

  # Empty file that tells NV that the scanner is running in a container
  - runs: touch ${{targets.contextdir}}/usr/local/bin/.nvcontainer

  - uses: strip

subpackages:
  - name: ${{package.name}}-task
    description: NeuVector Scanner Task
    pipeline:
      - uses: go/build
        with:
          modroot: .
          packages: ./task
          output: scannerTask
          prefix: usr/local
          vendor: true
    test:
      pipeline:
        - runs: |
            output=$(scannerTask -h 2>&1 || true)
            echo "$output" | grep -F "usage: scannerTask"
            echo "$output" | grep -F " -t "

  # Need a separate monitor subpackage for neuvector/scanner/monitor different from neuvector/neuvector/monitor
  - name: ${{package.name}}-monitor
    description: NeuVector Scanner Monitor
    pipeline:
      - runs: |
          make -C monitor
          mkdir -p ${{targets.contextdir}}/usr/local/bin
          install -Dm755 monitor/monitor ${{targets.contextdir}}/usr/local/bin/monitor
    test:
      pipeline:
        - uses: test/tw/help-check
          with:
            bins: monitor

test:
  pipeline:
    - runs: |
        if scanner --help | grep -q "usage: scan [OPTIONS]"; then
            exit 0
        fi

update:
  enabled: true
  git:
    tag-filter-prefix: v
    strip-prefix: v
