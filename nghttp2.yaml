package:
  name: nghttp2
  version: 1.64.0
  epoch: 1
  description: "experimental HTTP/2 client, server and library"
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - autoconf
      - build-base
      - busybox
      - c-ares-dev
      - ca-certificates-bundle
      - libev-dev
      - libtool
      - openssf-compiler-options
      - openssl-dev
      - pkgconf-dev
      - wolfi-base
      - zlib-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/nghttp2/nghttp2.git
      tag: v${{package.version}}
      expected-commit: 526ff38e0249acbcc4d0e8958c12cdeae9960cfe

  - runs: autoreconf -vfi

  - uses: autoconf/configure
    with:
      opts: --enable-app

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: "nghttp2-dev"
    description: "headers for nghttp2"
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - nghttp2
    test:
      pipeline:
        - uses: test/pkgconf

  - name: "nghttp2-doc"
    description: "documentation for nghttp2"
    pipeline:
      - uses: split/manpages

  - name: "libnghttp2-14"
    description: "nghttp2 client library"
    pipeline:
      - runs: |
          mkdir -p "${{targets.subpkgdir}}"/usr/lib
          mv "${{targets.destdir}}"/usr/lib/libnghttp2.so.* "${{targets.subpkgdir}}"/usr/lib/

update:
  enabled: true
  github:
    identifier: nghttp2/nghttp2
    strip-prefix: v

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        h2load --version
        nghttp --version
        nghttpd --version
        nghttpx --version
        h2load --help
        nghttp --help
        nghttpd --help
        nghttpx --help
