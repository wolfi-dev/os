package:
  name: nri-apache
  version: "1.15.3"
  epoch: 0 # CVE-2025-47910
  description: New Relic Infrastructure Apache Integration
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - go

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/newrelic/nri-apache
      expected-commit: 960218d5dc1c8a5f023768b833baf138d36a1d47
      tag: v${{package.version}}

  - uses: go/build
    with:
      modroot: .
      packages: ./src/
      output: nri-apache
      ldflags: -w

  - uses: strip

subpackages:
  - name: ${{package.name}}-compat
    description: "Compatibility package to place binaries in the location expected by newrelic-infrastructure-bundle"
    pipeline:
      - runs: |
          mkdir -p ${{targets.contextdir}}/var/db/newrelic-infra/newrelic-integrations/bin
          ln -sf /usr/bin/${{package.name}} ${{targets.contextdir}}/var/db/newrelic-infra/newrelic-integrations/bin/${{package.name}}
          install -Dm644 legacy/apache-definition.yml ${{targets.contextdir}}/var/db/newrelic-infra/newrelic-integrations/apache-definition.yml
    # test added by a robot (compat)
    test:
      pipeline:
        - runs: |
            readlink -v /var/db/newrelic-infra/newrelic-integrations/bin/nri-apache

update:
  enabled: true
  github:
    identifier: newrelic/nri-apache
    strip-prefix: v

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        nri-apache --help
