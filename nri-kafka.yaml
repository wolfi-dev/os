package:
  name: nri-kafka
  version: "3.15.1"
  epoch: 0 # CVE-2025-61729
  description: New Relic Infrastructure Kafka Integration
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - nrjmx
      - openjdk-8-jre

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/newrelic/nri-kafka
      expected-commit: fc3d9354eba42f1c895d5df8981ee3daea6c1f91
      tag: v${{package.version}}

  - uses: go/bump
    with:
      deps: |-
        golang.org/x/crypto@v0.45.0

  - uses: go/build
    with:
      modroot: .
      packages: ./src/
      output: nri-kafka
      ldflags: |
        -X main.integrationVersion=${{package.version}}
        -X main.gitCommit=$(git rev-parse HEAD)
        -X main.buildDate=$(date -u -d "@$SOURCE_DATE_EPOCH" "+%Y-%m-%dT%H:%M:%SZ")

  - uses: strip

subpackages:
  - name: ${{package.name}}-compat
    description: "Compatibility package to place binaries in the location expected by newrelic-infrastructure-bundle"
    pipeline:
      - runs: |
          mkdir -p ${{targets.subpkgdir}}/var/db/newrelic-infra/newrelic-integrations/bin
          ln -sf /usr/bin/${{package.name}} ${{targets.subpkgdir}}/var/db/newrelic-infra/newrelic-integrations/bin/${{package.name}}
          install -Dm644 legacy/kafka-definition.yml ${{targets.subpkgdir}}/var/db/newrelic-infra/newrelic-integrations/kafka-definition.yml
    test:
      environment:
        contents:
          packages:
            - ${{package.name}}
      pipeline:
        - uses: test/tw/symlink-check
          with:
            allow-absolute: "true"

  - name: ${{package.name}}-jdk11
    description: New Relic Infrastructure Kafka Integration with JDK 11 runtime
    dependencies:
      runtime:
        - nrjmx
        - openjdk-11-jre
    pipeline:
      - runs: |
          mkdir -p ${{targets.subpkgdir}}/usr/bin
          cp ${{targets.destdir}}/usr/bin/nri-kafka ${{targets.subpkgdir}}/usr/bin/nri-kafka

update:
  enabled: true
  github:
    identifier: newrelic/nri-kafka
    strip-prefix: v

test:
  pipeline:
    - uses: test/tw/help-check
      with:
        bins: nri-kafka
    - uses: test/tw/ver-check
      with:
        bins: nri-kafka
        version-flag: "-show_version"
    # This test runs nri-kafka without a Kafka cluster. It verifies that the binary starts correctly
    - name: Test binary execution
      uses: test/daemon-check-output
      with:
        start: nri-kafka
        expected_output: |
          com.newrelic.kafka
          protocol_version
          integration_version
