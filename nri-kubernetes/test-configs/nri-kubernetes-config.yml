# Config schema reference: https://github.com/newrelic/nri-kubernetes/blob/main/internal/config/config.go#L29
# If config schema changes, update this file to match the new structure
clusterName: test-cluster
interval: 15s

sink:
  type: http
  http:
    port: 8001
    timeout: 3s

kubelet:
  enabled: false

ksm:
  enabled: false

controlPlane:
  enabled: false
