package:
  name: nri-nginx
  version: "3.8.0"
  epoch: 0 # CVE-2026-32280
  description: New Relic Infrastructure Nginx Integration
  copyright:
    - license: Apache-2.0

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/newrelic/nri-nginx
      expected-commit: 16c1b76fff6f6d48aeadb2ea259768543eae2c95
      tag: v${{package.version}}

  - uses: go/build
    with:
      modroot: .
      packages: ./src/
      output: nri-nginx
      ldflags: -w

  - uses: strip

subpackages:
  - name: ${{package.name}}-compat
    description: "Compatibility package to place binaries in the location expected by newrelic-infrastructure-bundle"
    pipeline:
      - runs: |
          mkdir -p ${{targets.subpkgdir}}/var/db/newrelic-infra/newrelic-integrations/bin
          ln -sf /usr/bin/${{package.name}} ${{targets.subpkgdir}}/var/db/newrelic-infra/newrelic-integrations/bin/${{package.name}}
          install -Dm644 legacy/nginx-definition.yml ${{targets.subpkgdir}}/var/db/newrelic-infra/newrelic-integrations/nginx-definition.yml
    # test added by a robot (compat)
    test:
      pipeline:
        - name: Verify compat symlink target
          runs: |
            set -euo pipefail
            # Verify symlink exists and points to expected target
            readlink /var/db/newrelic-infra/newrelic-integrations/bin/nri-nginx | grep -F "/usr/bin/nri-nginx"
        - name: Verify definition file exists
          uses: test/tw/contains-files
          with:
            files: /var/db/newrelic-infra/newrelic-integrations/nginx-definition.yml

update:
  enabled: true
  github:
    identifier: newrelic/nri-nginx
    strip-prefix: v

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        nri-nginx --help
    - name: Functional test - verify nri-nginx binary executes
      runs: |
        set -euo pipefail

        # Run nri-nginx with --help and verify it produces meaningful output
        # that indicates the integration binary is functional
        output=$(nri-nginx --help 2>&1)
        echo "$output" | grep -iF "nginx"

        # Verify the binary is executable and linked correctly
        stat /usr/bin/nri-nginx
        test -x /usr/bin/nri-nginx

environment:
  contents:
    packages:
      - go-1.25
