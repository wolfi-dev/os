package:
  name: nvitop
  version: "1.5.1"
  epoch: 0
  description: "An interactive NVIDIA-GPU process viewer and beyond, the one-stop solution for GPU process management"
  copyright:
    - license: Apache-2.0 AND GPL-3.0-only
  dependencies:
    runtime:
      - python3
      - py3-supported-nvidia-ml-py
      - py3-supported-psutil
      - py3-supported-colorama
      - py3-supported-cachetools

vars:
  pypi-package: nvitop

data:
  - name: py-versions
    items:
      3.10: '310'
      3.11: '311'
      3.12: '312'
      3.13: '313'

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - py3-supported-python
      - py3-gpep517
      - py3-pip
      - py3-setuptools
      - py3-wheel

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/XuehaiPan/nvitop
      tag: v${{package.version}}
      expected-commit: f4ba519ae1aa4a491510927d1f38229bbf687e12

  - uses: py/pip-build-install
    with:
      python: python3.13

  - uses: strip

test:
  pipeline:
    - runs: |
        # Test basic imports
        python3 -c "import nvitop; print('✅ nvitop import successful')"
        python3 -c "import nvitop.api; print('✅ nvitop.api import successful')"
        python3 -c "import nvitop.gui; print('✅ nvitop.gui import successful')"
        
        # Test version commands
        python3 -m nvitop --version
        python3 -m nvitop.select --version
        
        # Test help commands (should not error)
        python3 -m nvitop --help > /dev/null
        python3 -m nvitop.select --help > /dev/null
        
        # Test that commands are available
        which nvitop
        which nvisel
        
        echo "✅ All nvitop tests passed!"

subpackages:
  - range: py-versions
    name: py${{range.key}}-${{vars.pypi-package}}
    description: ${{vars.pypi-package}} installed for python${{range.key}}
    dependencies:
      runtime:
        - py${{range.key}}-nvidia-ml-py
        - py${{range.key}}-psutil  
        - py${{range.key}}-colorama
        - py${{range.key}}-cachetools
      provides:
        - py3-${{vars.pypi-package}}
      provider-priority: ${{range.value}}
    pipeline:
      - uses: py/pip-build-install
        with:
          python: python${{range.key}}
    test:
      environment:
        contents:
          packages:
            - python-${{range.key}}
      pipeline:
        - uses: python/import
          with:
            python: python${{range.key}}
            import: nvitop

  - name: py3-supported-${{vars.pypi-package}}
    description: "meta package providing ${{vars.pypi-package}} for supported python versions"
    dependencies:
      runtime:
        - py3.10-${{vars.pypi-package}}
        - py3.11-${{vars.pypi-package}}
        - py3.12-${{vars.pypi-package}}
        - py3.13-${{vars.pypi-package}}

  - name: ${{vars.pypi-package}}-bin
    description: "Executable binaries for ${{vars.pypi-package}}"
    dependencies:
      runtime:
        - py3-supported-${{vars.pypi-package}}
    pipeline:
      - runs: |
          mkdir -p ${{targets.contextdir}}/usr/bin
          # Install console scripts
          python3 -m pip install --prefix=/usr --root=${{targets.contextdir}} --no-deps .
    test:
      pipeline:
        - runs: |
            nvitop --version
            nvitop --help

update:
  enabled: true
  github:
    identifier: XuehaiPan/nvitop
    use-tag: true
    strip-prefix: v 