package:
  name: nvtop
  version: "3.3.2"
  epoch: 3 # go/wolfi-rsc/nvtop
  description: "GPU & Accelerator process monitoring for AMD, Apple, Huawei, Intel, NVIDIA and Qualcomm"
  copyright:
    - license: GPL-3.0-or-later
  resources:
    cpu: "1"
    memory: 1Gi

environment:
  contents:
    packages:
      - build-base
      - cmake
      - libdrm-dev
      - ncurses-dev
      - samurai
      - systemd-dev
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/Syllo/nvtop
      tag: ${{package.version}}
      expected-commit: 76890233d759199f50ad3bdb57a0c0988e96fc44

  - uses: cmake/configure
    with:
      opts: |
        -DCMAKE_INSTALL_PREFIX=/usr \
        -DCMAKE_BUILD_TYPE=None \
        -DAMDGPU_SUPPORT=ON \
        -DINTEL_SUPPORT=ON \
        -DNVIDIA_SUPPORT=ON

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

update:
  enabled: true
  github:
    identifier: Syllo/nvtop
    use-tag: true

test:
  pipeline:
    - uses: test/tw/ldd-check
    - name: Verify nvtop binary is functional
      runs: |
        # Verify nvtop binary runs and exits cleanly
        nvtop --version
    - name: Verify nvtop handles absent GPU gracefully
      runs: |
        # In a test environment without GPU hardware, nvtop should
        # exit or be killed by timeout, not hang or segfault
        timeout 5 nvtop 2>&1 || true

subpackages:
  - name: nvtop-doc
    description: nvtop documentation
    pipeline:
      - uses: split/manpages
    test:
      pipeline:
        - uses: test/docs
