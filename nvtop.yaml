package:
  name: nvtop
  version: "3.3.2"
  epoch: 1 # go/wolfi-rsc/nvtop
  description: "GPU & Accelerator process monitoring for AMD, Apple, Huawei, Intel, NVIDIA and Qualcomm"
  copyright:
    - license: GPL-3.0-or-later
  resources:
    cpu: "1"
    memory: 1Gi

environment:
  contents:
    packages:
      - build-base
      - cmake
      - libdrm-dev
      - ncurses-dev
      - samurai
      - systemd-dev
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/Syllo/nvtop
      tag: ${{package.version}}
      expected-commit: 76890233d759199f50ad3bdb57a0c0988e96fc44

  - uses: cmake/configure
    with:
      opts: |
        -DCMAKE_INSTALL_PREFIX=/usr \
        -DCMAKE_BUILD_TYPE=None \
        -DAMDGPU_SUPPORT=ON \
        -DINTEL_SUPPORT=ON \
        -DNVIDIA_SUPPORT=ON

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

update:
  enabled: true
  github:
    identifier: Syllo/nvtop
    use-tag: true

test:
  pipeline:
    - runs: |
        nvtop --version | grep ${{package.version}}
        nvtop --help

subpackages:
  - name: nvtop-doc
    description: nvtop documentation
    pipeline:
      - uses: split/manpages
    test:
      pipeline:
        - uses: test/docs
