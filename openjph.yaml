package:
  name: openjph
  version: "0.24.0"
  epoch: 0
  description: Open source implementation of High-throughput JPEG2000 (HTJ2K)
  copyright:
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - tiff-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/aous72/OpenJPH.git
      expected-commit: f6e094c25e406ebe8a088c76cb41544879f882a8
      tag: ${{package.version}}

  - uses: cmake/configure

  - uses: cmake/build

  - uses: cmake/install

update:
  enabled: true
  github:
    identifier: aous72/OpenJPH

subpackages:
  - name: ${{package.name}}-dev
    description: ${{package.name}} development libraries and headers
    pipeline:
      - uses: split/dev
    test:
      pipeline:
        - uses: test/tw/ldd-check
        - uses: test/pkgconf

test:
  pipeline:
    - uses: test/tw/ldd-check
    - name: Smoke tests
      runs: |
        set -o pipefail
        {
          ojph_compress || true
        } | grep -F -e "The following arguments are necessary"
        {
          ojph_expand || true
        } | grep -F -e "The following arguments are necessary"
