package:
  name: openssl
  version: 3.1.4
  epoch: 0
  description: "the OpenSSL cryptography suite"
  copyright:
    - license: Apache-2.0
  # For now, continue to install the legacy openssl provider if the parent openssl
  # package is installed.  This is done because users may be installing the openssl
  # package to gain that provider.
  dependencies:
    runtime:
      - openssl-provider-legacy

vars:
  fips-enable: 'no-fips'

options:
  fips:
    vars:
      fips-enable: 'enable-fips'

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - perl
      - build-base

pipeline:
  - uses: fetch
    with:
      uri: https://www.openssl.org/source/openssl-${{package.version}}.tar.gz
      expected-sha256: 840af5366ab9b522bde525826be3ef0fb0af81c6a9ebd84caa600fea1731eee3

  - name: Configure and build
    runs: |
      export CC=${{host.triplet.gnu}}-gcc
      export CXX=${{host.triplet.gnu}}-g++
      export CPP=${{host.triplet.gnu}}-cpp

      perl ./Configure \
         linux-$(uname -m) \
         --prefix=/usr \
         --libdir=lib \
         --openssldir=/etc/ssl \
         ${{vars.fips-enable}} \
         enable-ktls \
         shared \
         no-zlib \
         no-async \
         no-comp \
         no-idea \
         no-mdc2 \
         no-rc5 \
         no-ec2m \
         no-sm2 \
         no-sm4 \
         no-ssl3 \
         no-seed \
         no-weak-ssl-ciphers \
         -Wa,--noexecstack
      make -j$(nproc)

  - uses: autoconf/make-install

  - runs: |
      rm ${{targets.contextdir}}/usr/bin/c_rehash

  - uses: strip

  # The fipsmodule.cnf generated by the OpenSSL build system is incorrect.
  # Regenerate it ourselves.
  - if: ${{options.fips.enabled}} == 'true'
    runs: |
      rm -f ${{targets.contextdir}}/etc/ssl/fipsmodule.cnf

      LD_LIBRARY_PATH="${{targets.contextdir}}/usr/lib" \
        ${{targets.contextdir}}/usr/bin/openssl fipsinstall \
          -module ${{targets.contextdir}}/usr/lib/ossl-modules/fips.so \
          -out ${{targets.contextdir}}/etc/ssl/fipsmodule.cnf

data:
  - name: engines
    items:
      afalg: Linux AF_ALG engine
      capi: CAPI engine
      padlock: VIA Padlock engine

subpackages:
  - name: "openssl-doc"
    description: "OpenSSL documentation"
    pipeline:
      - uses: split/manpages
      - runs: |
          mv "${{targets.contextdir}}"/usr/share/doc "${{targets.contextdir}}"/usr/share

  - name: "libcrypto3"
    description: "OpenSSL libcrypto library"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib
          mv "${{targets.contextdir}}"/usr/lib/libcrypto.so.3 "${{targets.contextdir}}"/usr/lib

          mkdir -p "${{targets.contextdir}}"/usr/lib/engines-3
          mv "${{targets.contextdir}}"/usr/lib/engines-3/loader_attic.so "${{targets.contextdir}}"/usr/lib/engines-3/
    dependencies:
      runtime:
        - openssl-config

  - name: "libssl3"
    description: "OpenSSL libssl library"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib
          mv "${{targets.contextdir}}"/usr/lib/libssl.so.3 "${{targets.contextdir}}"/usr/lib

  - name: "openssl-dev"
    description: "OpenSSL headers"
    pipeline:
      - uses: split/dev

  - range: engines
    name: "openssl-engine-${{range.key}}"
    description: "OpenSSL ${{range.value}}"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib/engines-3
          mv "${{targets.contextdir}}"/usr/lib/engines-3/${{range.key}}.so "${{targets.contextdir}}"/usr/lib/engines-3/

  - name: "openssl-provider-legacy"
    description: "OpenSSL legacy provider"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib/ossl-modules
          mv "${{targets.contextdir}}"/usr/lib/ossl-modules/legacy.so "${{targets.contextdir}}"/usr/lib/ossl-modules/

  - if: ${{options.fips.enabled}} == 'true'
    name: "openssl-provider-fips"
    description: "OpenSSL fips provider"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/lib/ossl-modules
          mv "${{targets.contextdir}}"/usr/lib/ossl-modules/fips.so "${{targets.contextdir}}"/usr/lib/ossl-modules/

          mkdir -p "${{targets.contextdir}}"/etc/ssl
          mv "${{targets.contextdir}}"/etc/ssl/fipsmodule.cnf "${{targets.contextdir}}"/etc/ssl/

  - if: ${{options.fips.enabled}} == 'true'
    name: "openssl-config-fips"
    description: "OpenSSL configuration (FIPS mode)"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/etc/ssl
          cat fips-preamble.cnf "${{targets.contextdir}}"/etc/ssl/openssl.cnf > "${{targets.contextdir}}"/etc/ssl/openssl.cnf
    dependencies:
      # We overlay the fips mode openssl.cnf on top of the openssl-config package to
      # enable FIPS mode.  So we require and replace the openssl-config package.
      runtime:
        - openssl-config
        - openssl-provider-fips
      replaces:
        - openssl-config

  - name: "openssl-config"
    description: "OpenSSL configuration"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/etc
          mv "${{targets.contextdir}}"/etc/ssl "${{targets.contextdir}}"/etc/

update:
  enabled: true
  release-monitor:
    identifier: 2566
