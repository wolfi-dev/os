package:
  name: parseable
  version: "2.6.6"
  epoch: 4 # GHSA-82j2-j2ch-gfr8
  description: "Parseable is a log analytics system built for high throughput log ingestion cases."
  copyright:
    - license: AGPL-3.0-or-later
  resources:
    cpu: "28"
    memory: 19Gi

vars:
  rust-version: "1.94"

environment:
  contents:
    packages:
      - bash
      - build-base
      - busybox
      - ca-certificates-bundle
      - cargo-auditable
      - rust-${{vars.rust-version}}

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/parseablehq/parseable
      tag: v${{package.version}}
      expected-commit: eacb1b9edab03f0b58260a86e1fb6e35a8edfbbc

  - uses: rust/cargobump

  - runs: |
      cargo auditable build --release
      install -Dm755 target/release/parseable "${{targets.destdir}}"/usr/bin/parseable

  - uses: strip

update:
  enabled: true
  github:
    identifier: parseablehq/parseable
    strip-prefix: v

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        parseable --version
        parseable --help
