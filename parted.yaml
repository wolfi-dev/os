package:
  name: parted
  version: "3.6"
  epoch: 0
  description: Utility to create, destroy, resize, check and copy partitions
  copyright:
    - license: GPL-3.0-or-later

environment:
  contents:
    packages:
      - autoconf
      - automake
      - bash
      - build-base
      - busybox
      - ca-certificates-bundle
      - check
      - cmake
      - coreutils
      - gettext
      - glib-dev
      - gperf
      - libtool
      - lvm2-dev
      - ncurses-dev
      - pkgconf-dev
      - readline-dev
      - rsync
      - texinfo
      - util-linux-dev
      - wget

pipeline:
  - uses: git-checkout
    with:
      repository: https://git.savannah.gnu.org/git/parted.git
      expected-commit: 0a275792222a71fcfbd6a78b6d8f81cf6439f929
      tag: v${{package.version}}

  - runs: ./bootstrap

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: parted-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - parted
    description: parted dev

  - name: parted-doc
    pipeline:
      - uses: split/manpages
    description: parted manpages

update:
  enabled: true
  release-monitor:
    identifier: 2596

test:
  pipeline:
    - runs: |
        parted --help
