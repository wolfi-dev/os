package:
  name: patchelf
  version: "0.19.0"
  epoch: 1 # add ldd-check and fix grep flags in tests
  description: Small utility to modify the dynamic linker and RPATH of ELF executables
  copyright:
    - license: GPL-3.0-or-later
  resources:
    cpu: "1"
    memory: 5Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libtool

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 1e279e99b952c765824fa0401abb8dc0d944e4ea
      repository: https://github.com/NixOS/patchelf
      tag: ${{package.version}}

  - runs: |
      autoreconf -fiv

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: patchelf-docs
    pipeline:
      - uses: split/manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  github:
    identifier: NixOS/patchelf

test:
  environment:
    contents:
      packages:
        - build-base
        - gcc
        - binutils
        - coreutils
  pipeline:
    - uses: test/tw/ldd-check
    - name: "Verify patchelf installation"
      runs: |
        patchelf --version
        which patchelf
    - name: "Test basic help output"
      runs: |
        patchelf --help
    - name: "Create and modify test binary"
      runs: |
        cat > test.c << 'EOF'
        #include <stdio.h>
        int main() { printf("Hello\n"); return 0; }
        EOF
        gcc -o test test.c
        patchelf --print-interpreter test
    - name: "Test RPATH modification"
      runs: |
        patchelf --set-rpath /usr/lib test
        patchelf --print-rpath test | grep -F "/usr/lib"
    - name: "Test interpreter modification"
      runs: |
        patchelf --set-interpreter /lib/test-ld.so test
        patchelf --print-interpreter test | grep -F "/lib/test-ld.so"
