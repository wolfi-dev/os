package:
  name: perl-cpan-uploader
  version: "0.103019"
  epoch: 2
  description: "Upload things to the CPAN"
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl
  dependencies:
    runtime:
      - perl
      - perl-getopt-long-descriptive
      - perl-http-message
      - perl-libwww
      - perl-lwp-protocol-https
      - perl-term-readkey
  resources:
    cpu: "2"
    memory: 4Gi

environment:
  contents:
    packages:
      - build-base
      - perl
      - perl-dist-zilla
      - perl-getopt-long-descriptive
      - perl-http-message
      - perl-libwww
      - perl-lwp-protocol-https
      - perl-term-readkey

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/rjbs/CPAN-Uploader
      tag: ${{package.version}}
      expected-commit: 396401620dc92c9ce6f55da7efbed4978f55f8cd

  - uses: perl/dzil

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: ${{package.name}}-doc
    pipeline:
      - uses: split/alldocs
    description: Documentation for ${{package.name}}
    test:
      pipeline:
        - uses: test/docs

test:
  pipeline:
    - uses: test/tw/help-check
      with:
        bins: cpan-upload
    - uses: test/no-docs
    - uses: test/perl-module-check
      with:
        modules: CPAN::Uploader

update:
  enabled: true
  github:
    identifier: rjbs/CPAN-Uploader
    use-tag: true
