# Generated from https://git.alpinelinux.org/aports/plain/main/perl-datetime-locale/APKBUILD
package:
  name: perl-datetime-locale
  version: "1.46"
  epoch: 0 # toolchain rebuild for  perl-dev-5.44.0
  description: Localization support for DateTime.pm
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl
  dependencies:
    runtime:
      - perl
      - perl-dist-checkconflicts
      - perl-file-sharedir
      - perl-file-sharedir-install
      - perl-namespace-autoclean
      - perl-params-validationcompiler
      - perl-specio
  resources:
    cpu: "2"
    memory: 4Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl
      - perl-dev
      - perl-dist-checkconflicts
      - perl-file-sharedir
      - perl-file-sharedir-install
      - perl-namespace-autoclean
      - perl-params-validationcompiler
      - perl-specio

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/houseabsolute/DateTime-Locale
      tag: v${{package.version}}
      expected-commit: 2bc9171c46abdcfc254bcdf73b67a11937463708

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-datetime-locale-doc
    pipeline:
      - uses: split/manpages
    description: perl-datetime-locale manpages
    test:
      pipeline:
        - uses: test/docs

test:
  pipeline:
    - uses: test/no-docs

update:
  enabled: true
  github:
    identifier: houseabsolute/DateTime-Locale
    strip-prefix: v
    tag-filter-prefix: v
    use-tag: true
