# Generated from https://git.alpinelinux.org/aports/plain/main/perl-extutils-installpaths/APKBUILD
package:
  name: perl-extutils-installpaths
  version: "0.015"
  epoch: 0
  description: Build.PL install path logic made easy
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl
      - perl-dev
      - perl-extutils-config

pipeline:
  - uses: fetch
    with:
      expected-sha256: 7d64eb2dfa87ead010cdf55c8a1bdfde50b7b5852d7cb8cf2304f55bea2eb007
      uri: https://cpan.metacpan.org/authors/id/L/LE/LEONT/ExtUtils-InstallPaths-${{package.version}}.tar.gz

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

test:
  pipeline:
    - uses: test/no-docs

subpackages:
  - name: perl-extutils-installpaths-doc
    pipeline:
      - uses: split/manpages
    description: perl-extutils-installpaths manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 11849
