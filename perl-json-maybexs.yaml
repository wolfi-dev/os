# Generated from https://git.alpinelinux.org/aports/plain/main/perl-json-maybexs/APKBUILD
package:
  name: perl-json-maybexs
  version: "1.004008"
  epoch: 5 # go/wolfi-rsc/perl-json-maybexs
  description: Use L<Cpanel::JSON::XS> with a fallback to L<JSON::XS> and L<JSON::PP>
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl
  dependencies:
    runtime:
      - perl
  resources:
    cpu: "2"
    memory: 4Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl
      - perl-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/p5sagit/JSON-MaybeXS
      tag: v${{package.version}}
      expected-commit: 587f1da453aa236d9972a0c4e40697ee42045ddf

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-json-maybexs-doc
    pipeline:
      - uses: split/manpages
    description: perl-json-maybexs manpages
    test:
      pipeline:
        - uses: test/docs

test:
  pipeline:
    - uses: test/no-docs

update:
  enabled: true
  github:
    identifier: p5sagit/JSON-MaybeXS
    strip-prefix: v
    tag-filter-prefix: v
    use-tag: true
