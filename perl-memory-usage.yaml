# Generated from https://git.alpinelinux.org/aports/plain/testing/perl-memory-usage/APKBUILD
package:
  name: perl-memory-usage
  version: "0.201"
  epoch: 3
  description: Tools to determine actual memory usage
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl
      - perl-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 8f2afad21e40a748e81c8c0f3e40ca718c14ddb9fb2d880bfbd28ae913ce5349
      uri: https://cpan.metacpan.org/authors/id/D/DO/DONEILL/Memory-Usage-${{package.version}}.tar.gz

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-memory-usage-doc
    pipeline:
      - uses: split/manpages
    description: perl-memory-usage manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  github:
    identifier: dave0/Memory-Usage
    use-tag: true

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        module-size --version
        module-size --help
