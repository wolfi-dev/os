# Generated from https://git.alpinelinux.org/aports/plain/main/perl-module-pluggable/APKBUILD
package:
  name: perl-module-pluggable
  version: "6.3"
  epoch: 5 # go/wolfi-rsc/perl-module-pluggable
  description: automatically give your module the ability to have plugins
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl
  dependencies:
    runtime:
      - perl
  resources:
    cpu: "1"
    memory: 2Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl
      - perl-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/simonwistow/Module-Pluggable
      tag: ${{package.version}}
      expected-commit: 799fc152863c8409234561f3cf9da4c3d2c41c2a

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-module-pluggable-doc
    pipeline:
      - uses: split/manpages
    description: perl-module-pluggable manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  github:
    identifier: simonwistow/Module-Pluggable
    use-tag: true

test:
  pipeline:
    - uses: test/no-docs
