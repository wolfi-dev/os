# Generated from https://git.alpinelinux.org/aports/plain/main/perl-module-runtime/APKBUILD
package:
  name: perl-module-runtime
  version: "0.018"
  epoch: 5 # go/wolfi-rsc/perl-module-runtime
  description: runtime module handling
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl
  dependencies:
    runtime:
      - perl
      - perl-module-build
  resources:
    cpu: "1"
    memory: 2Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl
      - perl-dev
      - perl-module-build

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/haarg/Module-Runtime
      tag: v${{package.version}}
      expected-commit: 50938f9c9af50357d79c3c42e9a2c7d00fc8262c

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-module-runtime-doc
    pipeline:
      - uses: split/manpages
    description: perl-module-runtime manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  # use git backend because GitHub backend fails with non-semver Perl decimal versions
  ignore-regex-patterns:
    - _ # exclude Perl dev/trial releases (e.g. v0.018_001)
  github:
    identifier: haarg/Module-Runtime
    strip-prefix: v
    tag-filter-prefix: v
    use-tag: true

test:
  pipeline:
    - uses: test/no-docs
