#nolint:git-checkout-must-use-github-updates
# Generated from https://git.alpinelinux.org/aports/plain/main/perl-module-scandeps/APKBUILD
package:
  name: perl-module-scandeps
  version: "1.37"
  epoch: 51 # go/wolfi-rsc/perl-module-scandeps
  description: Recursively scan Perl code for dependencies
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl
  dependencies:
    runtime:
      - perl
  resources:
    cpu: "1"
    memory: 1Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/rschupp/Module-ScanDeps
      # Version 1.37 was not tagged in GitHub; pin to the release-preparation commit
      tag: ""
      branch: master
      expected-commit: b76f2541c4faf3145413d94c54a4ad19ffd07664

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-module-scandeps-doc
    pipeline:
      - uses: split/manpages
    description: perl-module-scandeps manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 3112

test:
  pipeline:
    - uses: test/no-docs
