package:
  name: perl-net-http
  version: "6.24"
  epoch: 0
  description: Low-level HTTP connection (client)
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl
  dependencies:
    runtime:
      - perl-uri

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl
      - perl-uri

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 6d8ad51962f536b0cb0f2a07176a3a5530a8e6a7
      repository: https://github.com/libwww-perl/Net-HTTP.git
      tag: v${{package.version}}

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-net-http-doc
    pipeline:
      - uses: split/manpages
    description: perl-net-http manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  ignore-regex-patterns:
    # 2 tags problematic, 6.08_001, 6.08_002
    - "6[.]08_00[12]"
  github:
    identifier: libwww-perl/Net-HTTP
    strip-prefix: v
    use-tag: true

test:
  pipeline:
    - uses: test/no-docs
