package:
  name: perl-role-identifiable
  version: "0.009"
  epoch: 1
  description: "A thing you can identify somehow, either by an ident attribute or by a list of tags"
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl
  dependencies:
    runtime:
      - perl
      - perl-moose
  resources:
    cpu: "2"
    memory: 4Gi

environment:
  contents:
    packages:
      - build-base
      - perl
      - perl-dist-zilla
      - perl-moose

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/rjbs/Role-Identifiable
      tag: ${{package.version}}
      expected-commit: fc65f6c0742bb758835cdef4ddeefaf3b544b9a2

  - uses: perl/dzil

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: ${{package.name}}-doc
    pipeline:
      - uses: split/alldocs
    description: Documentation for ${{package.name}}
    test:
      pipeline:
        - uses: test/docs

test:
  pipeline:
    - uses: test/no-docs
    - uses: test/perl-module-check
      with:
        modules: Role::Identifiable

update:
  enabled: true
  github:
    identifier: rjbs/Role-Identifiable
    use-tag: true
