# Generated from https://git.alpinelinux.org/aports/plain/main/perl-role-tiny/APKBUILD
package:
  name: perl-role-tiny
  version: "2.002004"
  epoch: 6
  description: "Roles: a nouvelle cuisine portion size slice of Moose"
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl
  dependencies:
    runtime:
      - perl

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/moose/Role-Tiny.git
      tag: v${{package.version}}
      expected-commit: 8907ca285cb0fe94e84e33edb79124488d2b8556

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-role-tiny-doc
    pipeline:
      - uses: split/manpages
    description: perl-role-tiny manpages
    test:
      pipeline:
        - uses: test/docs

test:
  pipeline:
    - uses: test/no-docs

update:
  enabled: true
  github:
    identifier: moose/Role-Tiny
    strip-prefix: v
    use-tag: true
