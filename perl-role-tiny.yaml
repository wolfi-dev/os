# Generated from https://git.alpinelinux.org/aports/plain/main/perl-role-tiny/APKBUILD
package:
  name: perl-role-tiny
  version: "2.002005"
  epoch: 0 # go/wolfi-rsc/perl-role-tiny
  description: "Roles: a nouvelle cuisine portion size slice of Moose"
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl
  dependencies:
    runtime:
      - perl
  resources:
    cpu: "1"
    memory: 1Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/moose/Role-Tiny.git
      tag: v${{package.version}}
      expected-commit: 5997095d81a0f159c524960b05da3781b94556ca

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-role-tiny-doc
    pipeline:
      - uses: split/manpages
    description: perl-role-tiny manpages
    test:
      pipeline:
        - uses: test/docs

test:
  pipeline:
    - uses: test/no-docs

update:
  enabled: true
  github:
    identifier: moose/Role-Tiny
    strip-prefix: v
    use-tag: true
