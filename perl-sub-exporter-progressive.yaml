# Generated from https://git.alpinelinux.org/aports/plain/main/perl-sub-exporter-progressive/APKBUILD
package:
  name: perl-sub-exporter-progressive
  version: "0.001013"
  epoch: 6
  description: Only use Sub::Exporter if you need it
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl
  dependencies:
    runtime:
      - perl

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl
      - perl-dev
      - perl-dist-zilla

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/frioux/Sub-Exporter-Progressive
      tag: v${{package.version}}
      expected-commit: ba566703dc71fe0f44e07bea4a8bde6de9104427

  - uses: perl/dzil

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-sub-exporter-progressive-doc
    pipeline:
      - uses: split/manpages
    description: perl-sub-exporter-progressive manpages
    test:
      pipeline:
        - uses: test/docs

test:
  pipeline:
    - uses: test/perl-module-check
      with:
        modules: Sub::Exporter::Progressive
    - uses: test/no-docs

update:
  enabled: true
  github:
    identifier: frioux/Sub-Exporter-Progressive
    strip-prefix: v
    tag-filter-prefix: v
    use-tag: true
