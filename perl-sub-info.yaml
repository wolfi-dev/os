# Generated from https://git.alpinelinux.org/aports/plain/main/perl-sub-info/APKBUILD
package:
  name: perl-sub-info
  version: "0.002"
  epoch: 7 # go/wolfi-rsc/perl-sub-info
  description: Tool for inspecting subroutines.
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl
  dependencies:
    runtime:
      - perl
      - perl-importer
  resources:
    cpu: "2"
    memory: 5Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl
      - perl-dev
      - perl-importer

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/exodist/Sub-Info
      tag: v${{package.version}}
      expected-commit: fc4e4970b02ff9ecf2ca9e9452c0cc85d2185033

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-sub-info-doc
    pipeline:
      - uses: split/manpages
    description: perl-sub-info manpages
    test:
      pipeline:
        - uses: test/docs

test:
  pipeline:
    - uses: test/no-docs

update:
  enabled: true
  github:
    identifier: exodist/Sub-Info
    strip-prefix: v
    tag-filter-prefix: v
    use-tag: true
