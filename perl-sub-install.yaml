package:
  name: perl-sub-install
  version: "0.929"
  epoch: 1
  description: "Install subroutines into packages easily"
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl
  dependencies:
    runtime:
      - perl

environment:
  contents:
    packages:
      - build-base
      - perl
      - perl-dist-zilla

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/rjbs/Sub-Install
      tag: ${{package.version}}
      expected-commit: 7be1602400fa8f672c4642b65bd7a3f04e66a4d3

  - uses: perl/dzil

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: ${{package.name}}-doc
    pipeline:
      - uses: split/alldocs
    description: Documentation for ${{package.name}}
    test:
      pipeline:
        - uses: test/docs

test:
  pipeline:
    - uses: test/no-docs
    - uses: test/perl-module-check
      with:
        modules: Sub::Install

update:
  enabled: true
  github:
    identifier: rjbs/Sub-Install
    use-tag: true
