# Generated from https://git.alpinelinux.org/aports/plain/main/perl-syntax-keyword-try/APKBUILD
package:
  name: perl-syntax-keyword-try
  version: "0.29"
  epoch: 0
  description: C<try/catch/finally> syntax for perl
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl

environment:
  contents:
    packages:
      - build-base
      - busybox
      - perl
      - perl-dev
      - perl-module-build
      - perl-xs-parse-keyword

pipeline:
  - uses: fetch
    with:
      expected-sha256: cc320719d3608daa9514743a43dac2be99cb8ccd989b1fefa285290cb1d59d8f
      uri: https://cpan.metacpan.org/authors/id/P/PE/PEVANS/Syntax-Keyword-Try-${{package.version}}.tar.gz

  - runs: |
      export CFLAGS=$(perl -MConfig -E 'say $Config{ccflags}')
      perl Build.PL \
        --installdirs=vendor \
        --create_packlist=0
      perl Build
      perl Build test
      perl Build install --destdir="${{targets.contextdir}}"

subpackages:
  - name: perl-syntax-keyword-try-doc
    pipeline:
      - uses: split/manpages
    description: perl-syntax-keyword-try manpages

update:
  enabled: true
  release-monitor:
    identifier: 198011
