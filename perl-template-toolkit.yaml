# Generated from https://git.alpinelinux.org/aports/plain/community/perl-template-toolkit/APKBUILD
package:
  name: perl-template-toolkit
  version: "3.102"
  epoch: 1
  description: comprehensive template processing system
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl
  dependencies:
    runtime:
      - perl
      - perl-appconfig

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl
      - perl-appconfig
      - perl-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/abw/Template2.git
      tag: v${{package.version}}
      expected-commit: fa6e0a8a90a1e58fb170a9d183e040be0420648f

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-template-toolkit-doc
    pipeline:
      - uses: split/manpages
    description: perl-template-toolkit manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  github:
    identifier: abw/Template2
    strip-prefix: v
    use-tag: true

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        tpage --help
        ttree --help
    - uses: test/tw/ldd-check
