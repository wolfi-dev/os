# Generated from https://git.alpinelinux.org/aports/plain/main/perl-term-table/APKBUILD
package:
  name: perl-term-table
  version: "0.025"
  epoch: 0
  description: Format a header and rows into a table
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl
  dependencies:
    runtime:
      - perl
      - perl-importer

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl
      - perl-dev
      - perl-importer

pipeline:
  - uses: fetch
    with:
      expected-sha512: 0d857d3b13822333a928eb85f7d4015de4fd66a63de2469d718917524dd94a1639a3d0589d9fb754e1d527cb7ea67a3a6f1368598094fdf4f23750e9567d3dc3
      uri: https://cpan.metacpan.org/authors/id/E/EX/EXODIST/Term-Table-${{package.version}}.tar.gz

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-term-table-doc
    pipeline:
      - uses: split/manpages
    description: perl-term-table manpages
    test:
      pipeline:
        - uses: test/docs

test:
  pipeline:
    - uses: test/no-docs

update:
  enabled: true
  release-monitor:
    identifier: 12715
