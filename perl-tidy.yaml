# Generated from https://git.alpinelinux.org/aports/plain/testing/perl-tidy/APKBUILD
package:
  name: perl-tidy
  version: "20260204.01"
  epoch: 0
  description: Parses and beautifies perl source
  copyright:
    - license: GPL-2.0-only
  dependencies:
    runtime:
      - perl
  resources:
    cpu: "2"
    memory: 4Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/perltidy/perltidy
      tag: ${{package.version}}
      expected-commit: d6218ce44f0e064cbf36963369cf00fbc277ca5b

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-tidy-doc
    pipeline:
      - uses: split/manpages
    description: perl-tidy manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  github:
    identifier: perltidy/perltidy
    use-tag: true

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        perltidy --version
        perltidy --help
