# Generated from https://git.alpinelinux.org/aports/plain/testing/perl-tidy/APKBUILD
package:
  name: perl-tidy
  version: "20250711"
  epoch: 0
  description: Parses and beautifies perl source
  copyright:
    - license: GPL-2.0-only
  dependencies:
    runtime:
      - perl

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl

pipeline:
  - uses: fetch
    with:
      expected-sha256: 347aa90bcefbde2b590daf48d387ef1fd9b7a73a996b040269f11ab6fb8ba448
      uri: https://cpan.metacpan.org/authors/id/S/SH/SHANCOCK/Perl-Tidy-${{package.version}}.tar.gz

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-tidy-doc
    pipeline:
      - uses: split/manpages
    description: perl-tidy manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 3553

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        perltidy --version
        perltidy --help
