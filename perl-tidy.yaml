# Generated from https://git.alpinelinux.org/aports/plain/testing/perl-tidy/APKBUILD
package:
  name: perl-tidy
  version: "20260204"
  epoch: 0 # go/wolfi-rsc/perl-tidy
  description: Parses and beautifies perl source
  copyright:
    - license: GPL-2.0-only
  dependencies:
    runtime:
      - perl
  resources:
    cpu: "2"
    memory: 4Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl

pipeline:
  - uses: fetch
    with:
      expected-sha256: 56a1fc2f1f813e49026a0f284b9209a6b2824620993e7598c85b01c444ff0f64
      uri: https://cpan.metacpan.org/authors/id/S/SH/SHANCOCK/Perl-Tidy-${{package.version}}.tar.gz

  - uses: perl/make

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: perl/cleanup

  - uses: strip

subpackages:
  - name: perl-tidy-doc
    pipeline:
      - uses: split/manpages
    description: perl-tidy manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 3553

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        perltidy --version
        perltidy --help
