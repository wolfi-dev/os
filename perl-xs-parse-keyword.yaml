# Generated from https://git.alpinelinux.org/aports/plain/main/perl-xs-parse-keyword/APKBUILD
package:
  name: perl-xs-parse-keyword
  version: "0.42"
  epoch: 0
  description: XS functions to assist in parsing keyword syntax
  copyright:
    - license: GPL-1.0-or-later OR Artistic-1.0-Perl

environment:
  contents:
    packages:
      - build-base
      - busybox
      - perl
      - perl-dev
      - perl-extutils-cchecker
      - perl-module-build

pipeline:
  - uses: fetch
    with:
      expected-sha256: 7e498879e7813c9a7ecf4296c74774a32e40131e3a64efcc63c8010c0eb11382
      uri: https://cpan.metacpan.org/authors/id/P/PE/PEVANS/XS-Parse-Keyword-${{package.version}}.tar.gz

  - runs: |
      export CFLAGS=$(perl -MConfig -E 'say $Config{ccflags}')
      perl Build.PL \
        --installdirs=vendor \
        --create_packlist=0
      perl Build
      perl Build test
      perl Build install --destdir="${{targets.contextdir}}"

subpackages:
  - name: perl-xs-parse-keyword-doc
    pipeline:
      - uses: split/manpages
    description: perl-xs-parse-keyword manpages

update:
  enabled: true
  release-monitor:
    identifier: 198011
