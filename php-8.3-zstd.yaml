package:
  name: php-8.3-zstd
  version: "0.17.0"
  epoch: 1
  description: Zstd Extension for PHP
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - ${{package.name}}-config
      - php-${{vars.phpMM}}

var-transforms:
  - from: ${{package.name}}
    match: ^php-(\d+\.\d+)-.*$
    replace: "$1"
    to: phpMM

environment:
  contents:
    packages:
      - autoconf
      - build-base
      - busybox
      - file
      - php-${{vars.phpMM}}
      - php-${{vars.phpMM}}-dev
      - zstd-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/kjdev/php-ext-zstd
      tag: "${{package.version}}"
      expected-commit: 1774622c25aa3c20315b5f2a5769def976f5ac8c

  - name: Prepare build
    runs: phpize

  - name: Configure
    runs: ./configure --with-libzstd

  - uses: autoconf/make

  - name: Make install
    runs: INSTALL_ROOT="${{targets.destdir}}" DESTDIR="${{targets.destdir}}" make install

  - uses: strip

subpackages:
  - name: ${{package.name}}-config
    pipeline:
      - runs: |
          mkdir -p "${{targets.subpkgdir}}/etc/php/conf.d"
          echo "extension=zstd.so" > "${{targets.subpkgdir}}/etc/php/conf.d/zstd.ini"
    test:
      pipeline:
        - uses: test/tw/configpackage

update:
  enabled: true
  ignore-regex-patterns:
    - pie$
  github:
    identifier: kjdev/php-ext-zstd
    use-tag: true

test:
  pipeline:
    - uses: test/tw/ldd-check
    - name: Functional test - compress and decompress data via zstd extension
      runs: |
        php -r "
          \$data = 'Hello, zstd compression test!';
          \$compressed = zstd_compress(\$data);
          if (\$compressed === false) { echo 'FAIL: compression failed\n'; exit(1); }
          \$decompressed = zstd_uncompress(\$compressed);
          if (\$decompressed !== \$data) { echo 'FAIL: decompressed data does not match original\n'; exit(1); }
          echo 'PASS: compress/decompress roundtrip succeeded\n';
        "
