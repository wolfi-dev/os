name: qackage

inputs:
  exception:
    description: |
      Justification for requesting qackage evaluation exception (optional).
      Must cite infrastructure constraints: cloud credentials, live services,
      hardware features, or external dependencies unavailable in hermetic environment.
    required: false
    default: ""

pipeline:
  - name: qackage metadata
