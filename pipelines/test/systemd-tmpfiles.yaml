name: Create volatile files and directories using systemd-tmpfiles

needs:
  packages:
    - systemd-tmpfiles-standalone

pipeline:
  - name: Run systemd-tmpfiles
    runs: |
      systemd-tmpfiles --create --remove --exclude-prefix=/dev
