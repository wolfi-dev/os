name: By-Product Package Check

needs:
  packages:
    - package-type-check

pipeline:
  - name: Check if the package is a By-Product Package
    runs: |
      package-type-check byproduct "${{context.name}}"
