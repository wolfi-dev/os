name: Docs Package Check

needs:
  packages:
    - package-type-check

inputs:
  path-prefix:
    description: "Prefix used for documentation."
    default: "usr/share"

pipeline:
  - name: Check if the package is a Documentation Package
    runs: |
      package-type-check docs "${{context.name}}" \
          --path-prefix "${{inputs.path-prefix}}"
