name: no-docs

needs:
  packages:
    - no-docs-check

inputs:
  package:
    description: |
      Package name to check for documentation files.
      Defaults to the primary package being built.
    required: false
    default: ${{context.name}}

pipeline:
  - name: "check that package contains no documentation files"
    runs: |
      no-docs-check --package="${{inputs.package}}"
