name: enforce usrmerge sbin (merged usr)

needs:
  packages:
    - usrmerge-tool

inputs:
  dest:
    description: what directory to set up. default is targets.contextdir
    default: ${{targets.contextdir}}
    required: false

pipeline:
  - runs: |
      usrmerge-tool --sbin ${{targets.contextdir}}
