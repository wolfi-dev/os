package:
  name: prometheus-pushgateway
  version: "1.11.1"
  epoch: 2
  description: Push acceptor for ephemeral and batch jobs.
  copyright:
    - license: Apache-2.0
  dependencies:
    runtime:
      - merged-usrsbin
      - wolfi-baselayout

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/prometheus/pushgateway
      tag: v${{package.version}}
      expected-commit: aff0d834b730375fc965135e3e2d17d15f82a76f

  - uses: go/bump
    with:
      deps: |-
        golang.org/x/oauth2@v0.27.0
        golang.org/x/net@v0.38.0

  - uses: go/build
    with:
      packages: .
      output: prometheus-pushgateway

subpackages:
  - name: prometheus-pushgateway-bitnami-compat
    dependencies:
      runtime:
        # Required by startup scripts
        - bash
        - busybox
        - merged-usrsbin
        - wolfi-baselayout
    pipeline:
      - uses: bitnami/compat
        with:
          image: pushgateway
          version-path: 1/debian-12
      - runs: |
          mkdir -p  ${{targets.subpkgdir}}/opt/bitnami/pushgateway/bin/
          chmod g+rwX ${{targets.subpkgdir}}/opt/bitnami
          ln -sf /usr/bin/pushgateway ${{targets.subpkgdir}}/opt/bitnami/pushgateway/bin/pushgateway
    test:
      pipeline:
        - runs: |
            run-script --version
            run-script --help

update:
  enabled: true
  github:
    identifier: prometheus/pushgateway
    strip-prefix: v

test:
  environment:
    contents:
      packages:
        - curl
        - wait-for-it
  pipeline:
    - name: "Basic binary verification"
      runs: |
        prometheus-pushgateway --version
        prometheus-pushgateway --help
    - name: "Test Pushgateway functionality"
      uses: test/daemon-check-output
      with:
        setup: |
          mkdir -p /tmp/pushgateway
        start: prometheus-pushgateway --web.listen-address=:9091 --persistence.file=/tmp/pushgateway/metrics --persistence.interval=5s --web.enable-admin-api
        timeout: 30
        expected_output: |
          msg="starting pushgateway"
          caller=tls_config.go:347 msg="Listening on"
        post: |
          wait-for-it localhost:9091 -t 30 -- curl -s http://localhost:9091/-/ready | grep -q "OK"
          curl -s http://localhost:9091/-/healthy | grep -q "OK"

          # basic metrics push and verify
          echo "test_metric 42" | curl -s --data-binary @- http://localhost:9091/metrics/job/test_job
          curl -s http://localhost:9091/metrics | promtool check metrics

          # pushing with labels and verify
          echo "test_metric{label=\"value\"} 43" | curl -s --data-binary @- http://localhost:9091/metrics/job/test_job/instance/test_instance
          curl -s http://localhost:9091/metrics | promtool check metrics

          # DELETE functionality
          curl -s -X DELETE http://localhost:9091/metrics/job/test_job/instance/test_instance
          curl -s http://localhost:9091/metrics | grep -qv "test_metric{instance=\"test_instance\",job=\"test_job\",label=\"value\"}"

          # Test persistence
          echo "persistent_metric 123" | curl -s --data-binary @- http://localhost:9091/metrics/job/persistent_job
          curl -s http://localhost:9091/metrics | promtool check metrics

          # Wait for persistence file to be created
          for i in $(seq 1 10); do
            if [ -f /tmp/pushgateway/metrics ]; then
              break
            fi
            if [ $i -eq 10 ]; then
              exit 1
            fi
            sleep 1
          done

          # admin API wipe functionality
          curl -s -X PUT http://localhost:9091/api/v1/admin/wipe
          curl -s http://localhost:9091/metrics | grep -qv "test_metric"
