package:
  name: py3-backcall
  version: 0.2.0
  epoch: 8
  description: Specifications for callback functions passed in to an API
  copyright:
    - license: BSD-3-Clause
  dependencies:
    provider-priority: 0

vars:
  pypi-package: backcall
  import: backcall

data:
  - name: py-versions
    items:
      3.10: '310'
      3.11: '311'
      3.12: '312'
      3.13: '313'

environment:
  contents:
    packages:
      - py3-supported-build-base
      # Pin flit-core 3.x: backcall 0.2.0 is unmaintained and still uses the legacy
      # [tool.flit.metadata] table that flit-core 4 dropped; there's no upstream fix to cherry-pick.
      - py3.10-flit-core~3.12
      - py3.11-flit-core~3.12
      - py3.12-flit-core~3.12
      - py3.13-flit-core~3.12

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 76faffb16e2169606209255ef9e9d684d11c1e81
      repository: https://github.com/takluyver/backcall
      tag: ${{package.version}}

subpackages:
  - range: py-versions
    name: py${{range.key}}-${{vars.pypi-package}}
    description: python${{range.key}} version of ${{vars.pypi-package}}
    dependencies:
      provider-priority: ${{range.value}}
      provides:
        - py3-${{vars.pypi-package}}
    pipeline:
      - uses: py/pip-build-install
        with:
          python: python${{range.key}}
      - uses: strip
    test:
      pipeline:
        - uses: test/tw/pip-check
        - uses: python/import
          with:
            python: python${{range.key}}
            imports: |
              import ${{vars.import}}

  - name: py3-supported-${{vars.pypi-package}}
    description: meta package providing ${{vars.pypi-package}} for supported python versions.
    dependencies:
      runtime:
        - py3.10-${{vars.pypi-package}}
        - py3.11-${{vars.pypi-package}}
        - py3.12-${{vars.pypi-package}}
        - py3.13-${{vars.pypi-package}}
    test:
      pipeline:
        - uses: test/metapackage

test:
  pipeline:
    - uses: python/import
      with:
        imports: |
          import ${{vars.import}}

update:
  enabled: true
  manual: false
  github:
    identifier: takluyver/backcall
    use-tag: true
