diff --git a/setup.py b/setup.py
index fd2ff62..8267fec 100644
--- a/setup.py
+++ b/setup.py
@@ -67,9 +67,6 @@ def get_linux_openblas_link_args() -> List[str]:
         "/usr/lib",
         "/usr/local/lib",
     ]
-    for dir_path in possible_dirs:
-        if os.path.exists(os.path.join(dir_path, "libopenblas.a")):
-            return ["-l:libopenblas.a", "-lgfortran"]
     # Fallback if OpenBLAS static library is not found
     return ["-lopenblas", "-lgfortran"]
 
@@ -80,7 +77,7 @@ def linux_options(
     swig_opts: List[str],
 ) -> dict:
     """Linux options."""
-    default_link_args = ["-l:libfaiss.a"] + get_linux_openblas_link_args()
+    default_link_args = ["-lfaiss"] + get_linux_openblas_link_args()
     if FAISS_ENABLE_GPU:
         default_link_args += [
             "-lcublas_static",
