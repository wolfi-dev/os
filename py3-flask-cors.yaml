# Generated from https://pypi.org/project/Flask-Cors/
package:
  name: py3-flask-cors
  version: 4.0.0
  epoch: 0
  description: A Flask extension adding a decorator for CORS support
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - py3-flask
      - python-3.11

environment:
  contents:
    packages:
      - ca-certificates-bundle
      - wolfi-base
      - busybox
      - build-base
      - python-3.11
      - py3.11-setuptools

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 0b744011ac7252f2cd96f76b75a772c48e19f0d6
      repository: https://github.com/corydolphin/flask-cors
      tag: ${{package.version}}

  - name: Python Build
    runs: python setup.py build

  - name: Python Install
    runs: python setup.py install --prefix=/usr --root="${{targets.destdir}}"

  - uses: strip

update:
  enabled: true
  github:
    identifier: corydolphin/flask-cors
