package:
  name: py3-installer
  version: "1.0.0"
  epoch: 0 # go/wolfi-rsc/py3-installer
  description: A library for installing Python wheels.
  copyright:
    - license: "MIT"
  dependencies:
    provider-priority: 0
  resources:
    cpu: "2"
    memory: 5Gi

vars:
  pypi-package: installer

data:
  - name: py-versions
    items:
      3.10: "310"
      3.11: "311"
      3.12: "312"
      3.13: "313"
      3.14: "314"

environment:
  contents:
    packages:
      - build-base
      - busybox
      - py3-supported-python
      # py3-supported-python doesn't support py3.14 just yet
      - python-3.14-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/pypa/installer.git
      tag: ${{package.version}}
      expected-commit: 506a0158a9516009dbc838dd3f6d024c500eec13

subpackages:
  - range: py-versions
    name: py${{range.key}}-${{vars.pypi-package}}
    description: ${{vars.pypi-package}} installed for python${{range.key}}
    dependencies:
      provider-priority: ${{range.value}}
      provides:
        - ${{package.name}}
    pipeline:
      - uses: py/pip-build-install-bootstrap
        with:
          python: python${{range.key}}
    test:
      environment:
        contents:
          packages:
            - git
            - py3-supported-pytest
      pipeline:
        - uses: test/tw/pip-check
          with:
            python: python${{range.key}}
        - uses: python/import
          with:
            python: python${{range.key}}
            imports: |
              import installer
        - runs: |
            # out pytest package doesn't support 3.14 just yet
            if [ "${{range.key}}" == "3.14" ] ; then
              echo "WARNING: no py3.14 support on pytest. Skipping test ..." >&2
              exit 0
            fi
            git clone --branch ${{package.version}} https://github.com/pypa/installer.git py${{range.key}}-installer
            cd py${{range.key}}-installer
            # gets mad if you don't copy the tests to a separate directory
            cp -r tests ../
            cd ../tests
            python${{range.key}} -m pytest .

  - name: py3-supported-${{vars.pypi-package}}
    description: meta package providing ${{vars.pypi-package}} for supported python versions.
    dependencies:
      runtime:
        - py3.10-${{vars.pypi-package}}
        - py3.11-${{vars.pypi-package}}
        - py3.12-${{vars.pypi-package}}
        - py3.13-${{vars.pypi-package}}
        - py3.14-${{vars.pypi-package}}
    test:
      pipeline:
        - uses: test/tw/metapackage

update:
  enabled: true
  github:
    identifier: pypa/installer
    use-tag: true

test:
  pipeline:
    - uses: test/tw/byproductpackage
