# Generated from https://pypi.org/project/ptyprocess/
package:
  name: py3-ptyprocess
  version: 0.7.0
  epoch: 12 # go/wolfi-rsc/py3-ptyprocess
  description: Run a subprocess in a pseudo terminal
  copyright:
    - license: ISC
  dependencies:
    provider-priority: 0
  resources:
    cpu: "2"
    memory: 4Gi

vars:
  pypi-package: ptyprocess

data:
  - name: py-versions
    items:
      3.10: '310'
      3.11: '311'
      3.12: '312'
      3.13: '313'

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - py3-supported-pip
      # Pin flit-core 3.x: ptyprocess 0.7.0 still uses the legacy [tool.flit.metadata] table that
      # flit-core 4 dropped; upstream's fix (c7fcf9ea8) is unreleased and doesn't cherry-pick cleanly.
      - py3.10-flit-core~3.12
      - py3.11-flit-core~3.12
      - py3.12-flit-core~3.12
      - py3.13-flit-core~3.12
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/pexpect/ptyprocess
      tag: ${{package.version}}
      expected-commit: ee54ae2ee6bdd1e9f040dac79d799ae5b178e8fe

subpackages:
  - range: py-versions
    name: py${{range.key}}-${{vars.pypi-package}}
    description: python${{range.key}} version of ${{vars.pypi-package}}
    dependencies:
      provider-priority: ${{range.value}}
      provides:
        - py3-${{vars.pypi-package}}
    pipeline:
      - uses: py/pip-build-install
        with:
          python: python${{range.key}}
      - uses: strip
    test:
      pipeline:
        - uses: test/tw/pip-check
        - uses: python/import
          with:
            python: python${{range.key}}
            import: ${{vars.pypi-package}}

  - name: py3-supported-${{vars.pypi-package}}
    description: meta package providing ${{vars.pypi-package}} for supported python versions.
    dependencies:
      runtime:
        - py3.10-${{vars.pypi-package}}
        - py3.11-${{vars.pypi-package}}
        - py3.12-${{vars.pypi-package}}
        - py3.13-${{vars.pypi-package}}
    test:
      pipeline:
        - uses: test/metapackage

update:
  enabled: true
  github:
    identifier: pexpect/ptyprocess
    use-tag: true

test:
  pipeline:
    - uses: test/tw/byproductpackage
