# Generated from https://pypi.org/project/anyio/
package:
  name: py3.14-anyio
  version: "4.14.2"
  epoch: 1
  description: High level compatibility layer for multiple asynchronous event loop implementations
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - py${{vars.python-version}}-exceptiongroup
      - py${{vars.python-version}}-idna
      - py${{vars.python-version}}-sniffio
      - py${{vars.python-version}}-typing-extensions

vars:
  python-version: 3.14
  pypi-package: anyio
  import: anyio

environment:
  contents:
    packages:
      - py${{vars.python-version}}-pip
      - python-${{vars.python-version}}-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/agronholm/anyio
      tag: ${{package.version}}
      expected-commit: c384f99687c64c59ed8a11c3a0f11a2d57daff71

  - uses: py/pip-build-install
    with:
      python: python${{vars.python-version}}

  - uses: strip

update:
  enabled: true
  github:
    identifier: agronholm/anyio
    use-tag: true
  ignore-regex-patterns:
    - .*\.post.*

test:
  pipeline:
    - uses: test/tw/pip-check
    - uses: python/import
      with:
        python: python${{vars.python-version}}
        import: ${{vars.import}}
