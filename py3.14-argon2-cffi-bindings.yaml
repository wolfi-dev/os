package:
  name: py3.14-argon2-cffi-bindings
  version: "25.1.0"
  epoch: 0
  description: Low-level CFFI bindings for Argon2
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - py${{vars.python-version}}-cffi

vars:
  pypi-package: argon2-cffi-bindings
  import: _argon2_cffi_bindings
  python-version: "3.14"

environment:
  contents:
    packages:
      - argon2
      - argon2-dev
      - glibc-2.44-dev
      - py${{vars.python-version}}-build-base-dev
      - py${{vars.python-version}}-cffi
  environment:
    ARGON2_CFFI_USE_SYSTEM: 1

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 66f5b2340ef25699504bcc0f08eadcbefaf219aa
      repository: https://github.com/hynek/argon2-cffi-bindings
      tag: ${{package.version}}

  - uses: py/pip-build-install
    with:
      python: python${{vars.python-version}}

  - uses: strip

update:
  enabled: true
  github:
    identifier: hynek/argon2-cffi-bindings
    use-tag: true

test:
  pipeline:
    - uses: test/tw/pip-check
    - uses: python/import
      with:
        python: python${{vars.python-version}}
        imports: |
          import ${{vars.import}}
    - uses: test/tw/ldd-check
