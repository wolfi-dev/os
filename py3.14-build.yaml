package:
  name: py3.14-build
  version: "1.4.2"
  epoch: 0
  description: A simple, correct Python build frontend
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - py${{vars.py-version}}-packaging
      - py${{vars.py-version}}-pyproject-hooks
      - py${{vars.py-version}}-tomli

vars:
  import: build
  pypi-package: build
  py-version: "3.14"

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - py${{vars.py-version}}-flit-core
      - py${{vars.py-version}}-pip
      - python-${{vars.py-version}}-base
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/pypa/build
      tag: ${{package.version}}
      expected-commit: 7b7ae078aa1dabff33ea72d07ed15dd298acf80a

  - uses: py/pip-build-install
    with:
      python: python${{vars.py-version}}

  - uses: strip

test:
  pipeline:
    - uses: python/import
      with:
        python: python${{vars.py-version}}
        import: ${{vars.import}}
    - uses: test/tw/ver-check
      with:
        bins: pyproject-build
        version: ${{package.version}}

update:
  enabled: true
  ignore-regex-patterns:
    - post
  github:
    identifier: pypa/build
    use-tag: true
    strip-suffix: .post1
