package:
  name: py3.14-filelock
  version: "3.27.0"
  epoch: 0 # add pip-check test and import var
  description: A platform independent file lock.
  copyright:
    - license: Unlicense
  dependencies:
    runtime:
      - py${{vars.py-version}}-typing-extensions
  resources:
    memory: 7Gi

vars:
  pypi-package: filelock
  import: filelock
  py-version: "3.14"

environment:
  contents:
    packages:
      - build-base
      - busybox
      - py${{vars.py-version}}-hatch-vcs
      - py${{vars.py-version}}-hatchling
      - py${{vars.py-version}}-pip
      - py${{vars.py-version}}-wheel
      - python-${{vars.py-version}}-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/benediktschmitt/py-filelock
      tag: ${{package.version}}
      expected-commit: 824713edc32b54efd66566907f97c1238502810e

  - uses: py/pip-build-install
    with:
      python: python${{vars.py-version}}

  - uses: strip

test:
  pipeline:
    - uses: test/tw/pip-check
    - uses: python/import
      with:
        python: python${{vars.py-version}}
        import: ${{vars.import}}

update:
  enabled: true
  github:
    identifier: benediktschmitt/py-filelock
    use-tag: true
