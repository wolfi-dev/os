package:
  name: py3.14-installer
  version: "1.0.1"
  epoch: 2
  description: A library for installing Python wheels.
  copyright:
    - license: "MIT"
  resources:
    cpu: "1"
    memory: 1Gi
  test-resources:
    cpu: "1"
    memory: 1Gi

vars:
  pypi-package: installer
  import: installer
  python-version: "3.14"

environment:
  contents:
    packages:
      - busybox
      - python-${{vars.python-version}}-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/pypa/installer.git
      tag: ${{package.version}}
      expected-commit: 7ffa96f4ada5f38792e340ba7f9e1c43e0104768

  - uses: py/pip-build-install-bootstrap
    with:
      python: python${{vars.python-version}}

test:
  environment:
    contents:
      packages:
        - git
        - py${{vars.python-version}}-pytest
  pipeline:
    - uses: test/tw/pip-check
      with:
        python: python${{vars.python-version}}
    - uses: python/import
      with:
        python: python${{vars.python-version}}
        imports: |
          import installer
    - runs: |
        git clone --branch ${{package.version}} https://github.com/pypa/installer.git py${{vars.python-version}}-installer
        cd py${{vars.python-version}}-installer
        cp -r tests ../
        cd ../tests
        python${{vars.python-version}} -m pytest .

update:
  enabled: true
  github:
    identifier: pypa/installer
    use-tag: true
