package:
  name: py3.14-poetry-core
  version: "2.3.2"
  epoch: 1
  description: Poetry PEP 517 Build Backend
  copyright:
    - license: MIT

vars:
  pypi-package: poetry-core
  import: poetry.core
  py-version: "3.14"

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - py${{vars.py-version}}-pip
      - python-${{vars.py-version}}-base
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/python-poetry/poetry-core
      tag: ${{package.version}}
      expected-commit: 23bf0ac6b8a50a0756ef4d657d8ff67aef88aa67

  - uses: py/pip-build-install
    with:
      python: python${{vars.py-version}}

  - uses: strip

test:
  pipeline:
    - uses: test/tw/pip-check
    - uses: python/import
      with:
        python: python${{vars.py-version}}
        import: ${{vars.import}}

update:
  enabled: true
  github:
    identifier: python-poetry/poetry-core
    use-tag: true
