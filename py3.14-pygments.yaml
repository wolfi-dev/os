package:
  name: py3.14-pygments
  version: "2.21.0"
  epoch: 0
  description: Syntax highlighting package written in Python
  copyright:
    - license: BSD-2-Clause

vars:
  pypi-package: pygments
  import: pygments
  python-version: "3.14"

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - py${{vars.python-version}}-hatchling
      - py${{vars.python-version}}-pip
  environment:
    # This is needed to work around the error "ValueError: ZIP does not support timestamps before 1980"
    SOURCE_DATE_EPOCH: 315532800

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/pygments/pygments.git
      tag: ${{package.version}}
      expected-commit: a43b45dcf081b6010c6ab4428f149f7f6d2499c4

  - uses: py/pip-build-install
    with:
      python: python${{vars.python-version}}

  - uses: strip

test:
  pipeline:
    - uses: test/tw/pip-check
    - uses: python/import
      with:
        python: python${{vars.python-version}}
        import: ${{vars.import}}
    - uses: test/tw/ver-check
      with:
        bins: pygmentize
        version: ${{package.version}}

update:
  enabled: true
  github:
    identifier: pygments/pygments
