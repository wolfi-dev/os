package:
  name: py3.14-trove-classifiers
  version: "2026.5.20.13"
  epoch: 0
  description: Canonical source for classifiers on PyPI (pypi.org).
  copyright:
    - license: Apache-2.0
  dependencies:
    runtime:
      - py${{vars.py-version}}-zipp

vars:
  pypi-package: trove-classifiers
  module-name: trove_classifiers
  py-version: "3.14"

environment:
  contents:
    packages:
      - build-base
      - busybox
      - py${{vars.py-version}}-calver
      - py${{vars.py-version}}-pip
      - py${{vars.py-version}}-setuptools-scm
      - py${{vars.py-version}}-wheel
      - python-${{vars.py-version}}-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/pypa/trove-classifiers
      expected-commit: 19b58474c4461174d77fc54e75b6b752755411c5
      tag: ${{package.version}}

  - uses: py/pip-build-install
    with:
      python: python${{vars.py-version}}

  - uses: strip

test:
  pipeline:
    - uses: python/import
      with:
        python: python${{vars.py-version}}
        import: ${{vars.module-name}}

update:
  enabled: true
  github:
    identifier: pypa/trove-classifiers
