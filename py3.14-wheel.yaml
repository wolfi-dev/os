package:
  name: py3.14-wheel
  version: "0.48.0"
  epoch: 0
  description: "built-package format for Python"
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - py${{vars.python-version}}-packaging

vars:
  pypi-package: wheel
  python-version: "3.14"

environment:
  contents:
    packages:
      - busybox
      - python-${{vars.python-version}}-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/pypa/wheel
      tag: ${{package.version}}
      expected-commit: 21c4da4c54c3b354cc22dc7f6f6374ffcd560e84

  - runs: |
      # Fix license metadata to avoid flit-core exception
      sed -i 's/license = "MIT"/license = { file = "LICENSE.txt"}/' pyproject.toml

  - uses: py/pip-build-install-bootstrap
    with:
      python: python${{vars.python-version}}

test:
  pipeline:
    - uses: python/import
      with:
        python: python${{vars.python-version}}
        import: wheel

update:
  enabled: true
  github:
    identifier: pypa/wheel
    use-tag: true
