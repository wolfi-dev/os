package:
  name: pylint
  version: "4.0.7"
  epoch: 0
  description: A static code analyser for Python 2 and 3
  copyright:
    - license: GPL-2.0-only
  dependencies:
    runtime:
      - py${{vars.python-version}}-astroid<4.1
      - py${{vars.python-version}}-dill
      - py${{vars.python-version}}-isort
      - py${{vars.python-version}}-mccabe
      - py${{vars.python-version}}-platformdirs
      - py${{vars.python-version}}-tomlkit
      - python-${{vars.python-version}}-base
  resources:
    cpu: "1"
    memory: 5Gi

vars:
  python-version: 3.13

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - py${{vars.python-version}}-build-base
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/pylint-dev/pylint
      tag: v${{package.version}}
      expected-commit: c6768d3f1a45fbfad6a4ff9e167ca4f71dd5c991

  - uses: py/pip-build-install
    with:
      python: python${{vars.python-version}}

  - uses: strip

update:
  enabled: true
  github:
    identifier: pylint-dev/pylint
    strip-prefix: v

test:
  pipeline:
    - runs: |
        pylint --version | grep -F ${{package.version}}
        pylint --help
        pylint-config --version
        pyreverse --version
        pyreverse --help
        symilar --help
    - uses: test/tw/pip-check
    - uses: python/import
      with:
        python: python${{vars.python-version}}
        import: pylint
