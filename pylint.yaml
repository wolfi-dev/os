package:
  name: pylint
  version: "4.0.5"
  epoch: 0 # go/wolfi-rsc/pylint
  description: A static code analyser for Python 2 and 3
  copyright:
    - license: GPL-2.0-only
  dependencies:
    runtime:
      - py${{vars.python-version}}-astroid
      - py${{vars.python-version}}-dill
      - py${{vars.python-version}}-isort
      - py${{vars.python-version}}-platformdirs
      - py${{vars.python-version}}-tomlkit
  resources:
    cpu: "1"
    memory: 5Gi

vars:
  python-version: 3.13

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - py${{vars.python-version}}-build-base
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/pylint-dev/pylint
      tag: v${{package.version}}
      expected-commit: 88e1ab7545a4af4aea15c305a154c164a95ab842

  - uses: py/pip-build-install

  - uses: strip

update:
  enabled: true
  github:
    identifier: pylint-dev/pylint
    strip-prefix: v

test:
  pipeline:
    - runs: |
        pylint --version | grep ${{package.version}}
        pylint --help
        pylint-config --version
        pyreverse --version
        pyreverse --help
        symilar --help
