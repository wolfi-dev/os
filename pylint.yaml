package:
  name: pylint
  version: "4.0.1"
  epoch: 0
  description: A static code analyser for Python 2 and 3
  copyright:
    - license: GPL-2.0-only
  dependencies:
    runtime:
      - py${{vars.python-version}}-astroid
      - py${{vars.python-version}}-dill
      - py${{vars.python-version}}-isort
      - py${{vars.python-version}}-platformdirs
      - py${{vars.python-version}}-tomlkit

vars:
  python-version: 3.13

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - py${{vars.python-version}}-build-base
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/pylint-dev/pylint
      tag: v${{package.version}}
      expected-commit: 9a3035053154ba0c3ca3b300d6bc9fa72b95d552

  - uses: py/pip-build-install

  - uses: strip

update:
  enabled: true
  github:
    identifier: pylint-dev/pylint
    strip-prefix: v

test:
  pipeline:
    - runs: |
        pylint --version | grep ${{package.version}}
        pylint --help
        pylint-config --version
        pyreverse --version
        pyreverse --help
        symilar --help
