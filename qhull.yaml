# Generated from https://git.alpinelinux.org/aports/plain/community/qhull/APKBUILD
package:
  name: qhull
  version: "2020.2"
  epoch: 1
  description: Calculate convex hulls and related structures
  copyright:
    - license: Qhull

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - cmake

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/qhull/qhull
      expected-commit: 613debeaea72ee66626dace9ba1a2eff11b5d37d
      tag: ${{package.version}}

  - uses: cmake/configure

  - uses: cmake/build

  - uses: cmake/install

subpackages:
  - name: qhull-static
    pipeline:
      - uses: split/static
    description: qhull static

  - name: qhull-dev
    pipeline:
      - uses: split/dev
    description: qhull dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

  - name: qhull-doc
    pipeline:
      - uses: split/manpages
    description: qhull manpages
    test:
      pipeline:
        - uses: test/docs

update:
  github:
    identifier: qhull/qhull

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        qconvex --version
        qdelaunay --version
        qhalf --version
        qhull --version
        qvoronoi --version
        rbox -h
        qconvex --help
        qdelaunay --help
        qhalf --help
        qhull --help
        qvoronoi --help
    - uses: test/tw/ldd-check
