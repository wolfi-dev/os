Subject: Fix QUIC IntervalDeque method naming for GCC 15 compatibility
Upstream: Yes (backport from Chromium)
Origin: Chromium upstream, via Gentoo bug #936415
Author: Chromium Authors
Reference: https://chromium-review.googlesource.com/c/chromium/src/+/5512719
Reference: https://quiche.googlesource.com/quiche/+/c1af894e0f5c4f732a983e7c93227854e203570e
Reference: https://bugs.gentoo.org/936415

GCC 15 has stricter template name resolution. The QuicIntervalDeque class
has a member method named Size() (capital S) but template code was calling
size() (lowercase s). This mismatch is now caught as an error by GCC 15.

Fix by changing size() calls to Size() to match the actual class member.

--- a/src/3rdparty/chromium/net/third_party/quiche/src/quic/core/quic_interval_deque.h
+++ b/src/3rdparty/chromium/net/third_party/quiche/src/quic/core/quic_interval_deque.h
@@ -198,12 +198,12 @@ class QUIC_NO_EXPORT QuicIntervalDeque {
     Iterator operator+(difference_type amount) const {
       Iterator copy = *this;
       copy.index_ += amount;
-      DCHECK(copy.index_ < copy.deque_->size());
+      DCHECK(copy.index_ < copy.deque_->Size());
       return copy;
     }
     Iterator& operator+=(difference_type amount) {
       index_ += amount;
-      DCHECK(index_ < deque_->size());
+      DCHECK(index_ < deque_->Size());
       return *this;
     }
     Iterator operator-(difference_type amount) const {
