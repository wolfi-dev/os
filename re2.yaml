package:
  name: re2
  version: "2025-06-26" # On update, please check if -fdelete-null-pointer-checks is still required
  epoch: 0
  description: Efficient, principled regular expression library
  copyright:
    - license: BSD-3-Clause

var-transforms:
  - from: ${{package.version}}
    match: \.
    replace: "-"
    to: mangled-package-version

environment:
  contents:
    packages:
      - abseil-cpp-dev
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - icu-dev
  environment:
    # https://github.com/wolfi-dev/os/issues/34075
    CMAKE_CXX_FLAGS: -fdelete-null-pointer-checks

pipeline:
  - uses: fetch
    with:
      expected-sha256: d3c8fd1ba13e5779bf986b14c3ee6ddaeed0030473fa4ea3158840b1face618a
      uri: https://github.com/google/re2/archive/${{vars.mangled-package-version}}.tar.gz

  - uses: cmake/configure
    with:
      opts: |
        -DBUILD_SHARED_LIBS=ON \
        -DCMAKE_CXX_FLAGS="$CMAKE_CXX_FLAGS" \
        -DRE2_USE_ICU=ON \
        -DRE2_BUILD_TESTING="OFF"

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

subpackages:
  - name: re2-dev
    pipeline:
      - uses: split/dev
    description: re2 dev
    dependencies:
      runtime:
        - abseil-cpp-dev
        - icu-dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

update:
  enabled: true
  github:
    identifier: google/re2
