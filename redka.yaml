package:
  name: redka
  version: 0.3.0
  epoch: 0
  description: Redis re-implemented with SQLite
  copyright:
    - license: BSD-3-Clause
  dependencies:
    runtime:
      - ca-certificates-bundle

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - go

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/nalgeon/redka
      expected-commit: 1757da1a6d00a8328f193fcebc5ec1cda1c63d23
      tag: v${{package.version}}

  - runs: |
      make build
      mkdir -p ${{targets.destdir}}/usr/bin
      mv build/redka ${{targets.destdir}}/usr/bin

  - uses: strip

update:
  enabled: true
  github:
    identifier: nalgeon/redka
    strip-prefix: v

test:
  environment:
    contents:
      packages:
        - redis-cli
  pipeline:
    - runs: |
        redka&
        sleep 2
        redis-cli ping
        killall redka
