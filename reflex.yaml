package:
  name: reflex
  version: 0.2.4
  epoch: 0
  description: "Web apps in pure Python"
  copyright:
    - license: Apache-2.0
  dependencies:
    runtime:
      - python3
      # Reflex shells out to these these tools during init
      - bash
      - wget
      - curl

environment:
  contents:
    packages:
      - wolfi-base
      - wolfi-baselayout
      - busybox
      - ca-certificates-bundle
      - python3
      - py3-pip
      - build-base
      - python3-dev
      - poetry
      - py3-setuptools

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/reflex-dev/reflex
      tag: v${{package.version}}
      expected-commit: 9f7a7a1e4a38b1ab443451d1bbdc802c00a10739

  - runs: |
      poetry build
      pip install --root ${{targets.destdir}} dist/reflex-*.whl
      find ${{targets.destdir}}/usr/lib/python*/site-packages/ \( -type d -a -name test -o -name tests \) -o \( -type f -a -name '*.pyc' -o -name '*.pyo' \) -exec rm -rf '{}' \+

  - uses: strip

update:
  enabled: true
  github:
    identifier: reflex-dev/reflex
    strip-prefix: v
