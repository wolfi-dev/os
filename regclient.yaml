package:
  name: regclient
  version: "0.8.2"
  epoch: 1
  description: Docker and OCI Registry Client in Go and tooling using those libraries
  copyright:
    - license: Apache-2.0
  dependencies:
    runtime:
      - ca-certificates-bundle

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - go

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/regclient/regclient
      tag: v${{package.version}}
      expected-commit: e7e5436b4e93a897084aceca6b118b8002b20122

  - runs: |
      CGO_ENABLED=0 go build \
        -trimpath -ldflags "-extldflags -static -buildid=" \
        -o "${{targets.destdir}}/usr/bin/regctl" ./cmd/regctl

update:
  enabled: true
  github:
    identifier: regclient/regclient
    strip-prefix: v

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        regctl --help
        regctl version
