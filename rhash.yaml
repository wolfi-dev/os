package:
  name: rhash
  version: "1.4.6"
  epoch: 0
  description: "cross-platform asynchronous I/O library"
  copyright:
    - license: 0BSD

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - openssl-dev
      - wolfi-base

pipeline:
  - uses: fetch
    with:
      uri: https://downloads.sourceforge.net/rhash/rhash-${{package.version}}-src.tar.gz
      expected-sha256: 9f6019cfeeae8ace7067ad22da4e4f857bb2cfa6c2deaa2258f55b2227ec937a

  - runs: |
      ./configure \
        --prefix=/usr \
        --sysconfdir=/etc \
        --enable-openssl \
        --disable-openssl-runtime

  - uses: autoconf/make

  - uses: autoconf/make-install

  - runs: |
      make DESTDIR="${{targets.destdir}}" install-lib-headers install-lib-shared install-lib-so-link

  - uses: strip

subpackages:
  - name: "rhash-doc"
    description: "rhash documentation"
    pipeline:
      - uses: split/manpages
    test:
      pipeline:
        - uses: test/docs

  - name: "rhash-dev"
    description: "rhash development headers"
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - librhash0

  - name: "librhash0"
    description: "librhash runtime library"
    pipeline:
      - runs: |
          mkdir -p "${{targets.subpkgdir}}"/usr/lib
          mv "${{targets.destdir}}"/usr/lib/librhash.so.* "${{targets.subpkgdir}}"/usr/lib/
    test:
      pipeline:
        - uses: test/tw/ldd-check

update:
  enabled: true
  release-monitor:
    identifier: 13843

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        ed2k-link --version
        edonr256-hash --version
        edonr512-hash --version
        gost12-256-hash --version
        gost12-512-hash --version
        has160-hash --version
        magnet-link --version
        rhash --version
        sfv-hash --version
        tiger-hash --version
        tth-hash --version
        whirlpool-hash --version
        ed2k-link --help
        edonr256-hash --help
        edonr512-hash --help
        gost12-256-hash --help
        gost12-512-hash --help
        has160-hash --help
        magnet-link --help
        rhash --help
        sfv-hash --help
        tiger-hash --help
        tth-hash --help
        whirlpool-hash --help
