# Generated from https://git.alpinelinux.org/aports/plain/community/riemann-c-client/APKBUILD
package:
  name: riemann-c-client
  version: 2.1.0
  epoch: 6
  description: A C client library for the Riemann monitoring system
  copyright:
    - license: LGPL-3.0-or-later

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - json-c-dev
      - libtool
      - openssl-hardened-dev
      - pkgconf-dev
      - protobuf-c-dev

pipeline:
  - uses: git-checkout
    with:
      expected-commit: c35d3188ac1733e7420cb393a600e954802f96c2
      repository: https://git.madhouse-project.org/algernon/riemann-c-client
      tag: riemann-c-client-${{package.version}}

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: riemann-c-client-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - riemann-c-client
        - protobuf-c-dev
    description: riemann-c-client dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

  - name: riemann-c-client-doc
    pipeline:
      - uses: split/manpages
    description: riemann-c-client manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  git:
    strip-prefix: riemann-c-client-
    tag-filter-prefix: riemann-c-client-

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        riemann-client --version
        riemann-client --help
    - uses: test/tw/ldd-check
