# Generated from https://git.alpinelinux.org/aports/plain/community/riemann-c-client/APKBUILD
package:
  name: riemann-c-client
  version: 2.1.0
  epoch: 1
  description: A C client library for the Riemann monitoring system
  copyright:
    - license: LGPL-3.0-or-later

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - json-c-dev
      - libtool
      - openssl-dev
      - pkgconf-dev
      - protobuf-c-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 8a7aea4284e3505716de33d5408698d4c3dc4ba970e94fa11a171bfba2920f53
      uri: https://git.madhouse-project.org/algernon/riemann-c-client/archive/riemann-c-client-${{package.version}}.tar.gz

  - runs: autoreconf -iv

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: riemann-c-client-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - riemann-c-client
        - protobuf-c-dev
    description: riemann-c-client dev
    test:
      pipeline:
        - uses: test/pkgconf

  - name: riemann-c-client-doc
    pipeline:
      - uses: split/manpages
    description: riemann-c-client manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 21389

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        riemann-client --version
        riemann-client --help
