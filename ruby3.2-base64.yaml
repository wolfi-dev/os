package:
  name: ruby3.2-base64
  version: 0.1.1
  epoch: 1
  description: Support for encoding and decoding binary data using a Base64 representation.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - ca-certificates-bundle
      - ruby-3.2
      - ruby-3.2-dev
      - build-base
      - busybox
      - git

pipeline:
  - uses: git-checkout
    with:
      expected-commit: b9e23b27f94a37b1338541fc43aea4130ae56e66
      repository: https://github.com/ruby/base64.git
      tag: v${{package.version}}

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: base64

update:
  enabled: true
  github:
    identifier: ruby/base64
    strip-prefix: v
    use-tag: true
