# Generated from http://github.com/jruby/jruby/tree/master/gems/bouncy-castle-java/
package:
  name: ruby3.2-bouncy-castle-java
  version: 1.5.0146.1
  epoch: 7 # go/wolfi-rsc/ruby3.2-bouncy-castle-java
  description: Gem redistribution of "Legion of the Bouncy Castle Java cryptography APIs" jars at http://www.bouncycastle.org/java.html
  copyright:
    - license: MIT
  resources:
    cpu: "1"

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - curl
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/nahi/bouncy-castle-java
      tag: ${{package.version}}
      expected-commit: 401f0077cac504c62c449b860366f23393345c35

  - uses: patch
    with:
      patches: 0001-add-gemspec.patch

  # Bouncycastle does not actively maintain on GitHub so we need
  # to download the newer versions of dependencies from Maven.
  # https://mvnrepository.com/artifact/org.bouncycastle/bcprov-jdk18on
  - runs: rm lib/bcprov-jdk15-146.jar

  - uses: fetch
    working-directory: lib
    with:
      uri: https://repo1.maven.org/maven2/org/bouncycastle/bcprov-jdk18on/1.80/bcprov-jdk18on-1.80.jar
      expected-sha256: e8ad209f8c58d291a37ca9750e9e9fac60596956c983e49dd8282381dd8b3249
      extract: false

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

test:
  pipeline:
    - uses: test/tw/gem-installed

vars:
  gem: bouncy-castle-java

update:
  enabled: true
  github:
    identifier: nahi/bouncy-castle-java
    use-tag: true

var-transforms:
  - from: ${{package.name}}
    match: ^ruby(\d\.\d+)-.*
    replace: $1
    to: rubyMM
