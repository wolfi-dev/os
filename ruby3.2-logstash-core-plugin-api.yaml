# Generated from http://www.elastic.co/guide/en/logstash/current/index.html
package:
  name: ruby3.2-logstash-core-plugin-api
  version: "9.4.2"
  epoch: 0 # go/wolfi-rsc/ruby3.2-logstash-core-plugin-api
  description: Logstash plugin API
  copyright:
    - license: Apache-2.0
  dependencies:
    runtime:
      - ruby${{vars.rubyMM}}-logstash-core
  resources:
    cpu: "2"
    memory: 6Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - jruby-9.4
      - jruby-9.4-default-ruby
      - openjdk-11-default-jdk
  environment:
    JAVA_HOME: /usr/lib/jvm/java-11-openjdk
    # TODO: Ideally, we should read the version from the ../versions.yml file. But
    # melange pipeline does not support evaluating expressions in the `uses/with`.
    # We also cannot pass environment variables between steps. So, until we
    # figure out a better way, set a hardcoded version here.
    LOGSTASH_CORE_PLUGIN_API_VERSION: 2.1.16

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/elastic/logstash
      tag: v${{package.version}}
      expected-commit: 4d51b94a54decd3cae250e66ea42db5e23f8a5b6

  - working-directory: logstash-core-plugin-api
    pipeline:
      # We will be adding the runtime dependency in the all the other projects
      # anyway. So, remove the runtime dependency from the gemspec file to
      # skip the dependency-check during the build.
      - runs: sed -i '/add_runtime_dependency "logstash-core"/d' ${{vars.gem}}.gemspec
      - runs: jruby -S gem build ${{vars.gem}}.gemspec
      - runs: |
          # Ideally, we should read the version from the ../versions.yml file since
          # output file named like `logstash-core-plugin-api-2.1.16-java.gem`.
          # But melange pipeline does not support evaluating expressions in
          # the `uses/with` section. So, rename the gem file to exclude the version.
          mv ${{vars.gem}}-*.gem ${{vars.gem}}.gem
      - uses: ruby/install
        with:
          gem-file: ${{vars.gem}}.gem
          version: ${LOGSTASH_CORE_PLUGIN_API_VERSION}

  - uses: ruby/clean

  # for some reason we have an empty /usr/bin/ dir in this package
  - name: "remove empty /usr/bin directory"
    runs: rm -rf ${{targets.contextdir}}/usr/bin/

vars:
  gem: logstash-core-plugin-api

update:
  enabled: true
  github:
    identifier: elastic/logstash
    strip-prefix: v

var-transforms:
  - from: ${{package.name}}
    match: ^ruby(\d\.\d+)-.*
    replace: $1
    to: rubyMM

test:
  environment:
    contents:
      packages:
        - jruby-9.4
        - openjdk-11-default-jvm
  pipeline:
    - name: "verify some .rb files are included"
      runs: |
        set -euo pipefail
        dir="/usr/share/jruby/lib/ruby/gems/shared/gems/"
        file="*.rb"
        # Use `grep -F` (not `grep -q`) so the whole `find` output is consumed:
        # `grep -q` exits on the first match and closes the pipe, which makes
        # `find` die with SIGPIPE and, under the `set -o pipefail` wrapper
        # melange applies to test steps, falsely fails the check.
        if ! find "${dir}" -name "${file}" | grep -F ".rb"; then
          echo "FAIL: no '${file}' files found under '${dir}'. This is unexpected." >&2
          exit 1
        fi
        echo "PASS: '${file}' files found under '${dir}'. This is expected."
    - name: "verify some .gemspec files are included"
      runs: |
        set -euo pipefail
        dir="/usr/share/jruby/lib/ruby/gems/shared/specifications/"
        file="*.gemspec"
        if ! find "${dir}" -name "${file}" | grep -F ".gemspec"; then
          echo "FAIL: no '${file}' files found under '${dir}'. This is unexpected." >&2
          exit 1
        fi
        echo "PASS: '${file}' files found under '${dir}'. This is expected."
