# Generated from https://github.com/sidekiq/sidekiq
package:
  name: ruby3.2-sidekiq
  version: "8.1.3"
  epoch: 0
  description: Simple, efficient background processing for Ruby.
  copyright:
    - license: LGPL-3.0-or-later
  dependencies:
    runtime:
      - ruby${{vars.rubyMM}}-concurrent-ruby
      - ruby${{vars.rubyMM}}-connection_pool
      - ruby${{vars.rubyMM}}-json
      - ruby${{vars.rubyMM}}-logger
      - ruby${{vars.rubyMM}}-rack
      - ruby${{vars.rubyMM}}-redis-client
      - ruby-${{vars.rubyMM}}

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 52a08748a39030aa22e73861e1fd1bbc60bf3dea
      repository: https://github.com/sidekiq/sidekiq
      tag: v${{package.version}}

  - uses: ruby/unlock-spec

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: sidekiq

update:
  enabled: true
  github:
    identifier: sidekiq/sidekiq
    strip-prefix: v
    use-tag: true

test:
  pipeline:
    - uses: test/tw/gem-check
    - uses: test/tw/ver-check
      with:
        bins: sidekiq sidekiqmon
    - uses: test/tw/help-check
      with:
        bins: sidekiqmon

var-transforms:
  - from: ${{package.name}}
    match: ^ruby(\d\.\d+)-.*
    replace: $1
    to: rubyMM
