# Generated from https://github.com/ruby/abbrev
package:
  name: ruby3.4-abbrev
  version: 0.1.2
  epoch: 0
  description: Calculates a set of unique abbreviations for a given set of strings
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 9c5c7f34365c35342029c38f811ac43fe73f7f91f9dcc3ac0ccdfdea44f8b25b
      uri: https://github.com/ruby/abbrev/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: abbrev
  rubyMM: "3.4"
