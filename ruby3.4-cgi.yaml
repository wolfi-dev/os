# Generated from https://github.com/ruby/cgi
package:
  name: ruby3.4-cgi
  version: 0.4.1
  epoch: 0
  description: Support for the Common Gateway Interface protocol.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby${{vars.rubyMM}}-rubygems
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 799a34edbf3f33fdaf73e8e9d162498420190298ef533a4300fd05808eb1a212
      uri: https://github.com/ruby/cgi/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

update:
  enabled: true
  github:
    identifier: ruby/cgi
    strip-prefix: v

vars:
  gem: cgi
  rubyMM: "3.4"
