# Generated from https://github.com/ruby/digest
package:
  name: ruby3.4-digest
  version: 3.1.1
  epoch: 0
  description: Provides a framework for message digest libraries.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 27107e7a982ce090eb9d84f4fa2f0e537feb46389e5cdd50855f6b8f8531d280
      uri: https://github.com/ruby/digest/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: digest
  rubyMM: "3.4"
