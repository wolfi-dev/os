# Generated from https://github.com/ruby/English
package:
  name: ruby3.4-english
  version: 0.8.0
  epoch: 0
  description: Require 'English.rb' to reference global variables with less cryptic names.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: b8cf7fe263472af9111dc6991f6776de8bf53f174828d5d4a612f2c0beb811a6
      uri: https://github.com/ruby/English/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: english
  rubyMM: "3.4"
