# Generated from https://github.com/ruby/erb
package:
  name: ruby3.4-erb
  version: 4.0.4
  epoch: 0
  description: An easy to use but powerful templating system for Ruby.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause
  dependencies:
    runtime:
      - ruby3.4-cgi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-3.4
      - ruby-3.4-dev
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: d5cdd08c37416121d6b4f9c49345110250176baec9682bd61070546cb9730618
      uri: https://github.com/ruby/erb/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

update:
  enabled: true
  github:
    identifier: ruby/base64
    strip-prefix: v
    use-tag: true

vars:
  gem: erb
