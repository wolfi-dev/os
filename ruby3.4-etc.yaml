# Generated from https://github.com/ruby/etc
package:
  name: ruby3.4-etc
  version: 1.4.5
  epoch: 0
  description: Provides access to information typically stored in UNIX /etc directory.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 67f005018abb7a1bd5b9340c0619bdf478c6bb3cb03508fba734e5f1b2246735
      uri: https://github.com/ruby/etc/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: etc
  rubyMM: "3.4"
