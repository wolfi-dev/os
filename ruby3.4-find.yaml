# Generated from https://github.com/ruby/find
package:
  name: ruby3.4-find
  version: 0.2.0
  epoch: 0
  description: This module supports top-down traversal of a set of file paths.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: b1cadf8bcca4e16fa8e9f57c114071ff91dd9dc50abd957d0d4fd301369e64e6
      uri: https://github.com/ruby/find/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: find
  rubyMM: "3.4"
