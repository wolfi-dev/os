# Generated from https://github.com/ruby/forwardable
package:
  name: ruby3.4-forwardable
  version: 1.3.3
  epoch: 0
  description: Provides delegation of specified methods to a designated object.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby${{vars.rubyMM}}-rubygems
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 5f4cef25a6be9458b8fa2e43f3b5f8153ef528db37e0e4d802a3f9282953ee52
      uri: https://github.com/ruby/forwardable/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

update:
  enabled: true
  github:
    identifier: ruby/forwardable
    strip-prefix: v
    use-tag: true

vars:
  gem: forwardable
  rubyMM: "3.4"
