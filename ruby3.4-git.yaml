# Generated from http://github.com/ruby-git/ruby-git
package:
  name: ruby3.4-git
  version: 2.3.2
  epoch: 0
  description: The git gem provides an API that can be used to
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - ruby3.4-activesupport
      - ruby3.4-addressable
      - ruby3.4-process_executer
      - ruby3.4-rchardet

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: 2f772e87951127a1ef54fdbb0c026bb2951441dce4fd7c5adb0f1732d893e650
      uri: http://github.com/ruby-git/ruby-git/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: git
  rubyMM: "3.4"
