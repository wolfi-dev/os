# Generated from https://github.com/piotrmurach/github
package:
  name: ruby3.4-github_api
  version: 0.19.0
  epoch: 0
  description: ' Ruby client that supports all of the GitHub API methods. It"s build in a modular way, that is, you can either instantiate the whole api wrapper Github.new or use parts of it e.i. Github::Client::Repos.new if working solely with repositories is your main concern. Intuitive query methods allow you easily call API endpoints. '
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - ruby3.4-addressable
      - ruby3.4-descendants_tracker
      - ruby3.4-faraday
      - ruby3.4-hashie
      - ruby3.4-oauth2

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: 062d4131b6c4308701e8e15cd743843470b261e7e521bc70ebcb707e2d065b09
      uri: https://github.com/piotrmurach/github/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: github_api
  rubyMM: "3.4"
