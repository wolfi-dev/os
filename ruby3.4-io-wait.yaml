# Generated from https://github.com/ruby/io-wait
package:
  name: ruby3.4-io-wait
  version: 0.3.1
  epoch: 0
  description: Waits until IO is readable or writable without blocking.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 948d5fa3b6701c14cc736779625fb7f0593a41fa288efd51b2f6bc878c98381f
      uri: https://github.com/ruby/io-wait/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: io-wait
  rubyMM: "3.4"
