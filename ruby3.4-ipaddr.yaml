# Generated from https://github.com/ruby/ipaddr
package:
  name: ruby3.4-ipaddr
  version: 1.2.7
  epoch: 0
  description: IPAddr provides a set of methods to manipulate an IP address.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: bc21df81260515599f92b011f3640d099bc7bc0daff467d197638e51320ddee0
      uri: https://github.com/ruby/ipaddr/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: ipaddr
  rubyMM: "3.4"
