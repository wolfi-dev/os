# Generated from https://github.com/ruby/json
package:
  name: ruby3.4-json
  version: 2.9.0
  epoch: 0
  description: This is a JSON implementation as a Ruby extension in C.
  copyright:
    - license: Ruby

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 75f29f5d53839af494970024cc6f3e803801146a09d0e1ebd1bd0ac83458e84b
      uri: https://github.com/ruby/json/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: json
  rubyMM: "3.4"
