# Generated from https://github.com/evanphx/kpeg
package:
  name: ruby3.4-kpeg
  version: 1.3.3
  epoch: 0
  description: |-
    KPeg is a simple PEG library for Ruby. It provides an API as well as native
  copyright:
    - license: BSD-3-Clause
  dependencies:
    provider-priority: 34

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-3.4-bootstrap
      - ruby-3.4-bootstrap-dev
      - ruby3.4-hoe
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: 4a323e5176a462675678a6cc6a4e1982be2cdf4602a773a6ad34aa488492c0d8
      uri: https://github.com/evanphx/kpeg/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

update:
  enabled: true
  github:
    identifier: evanphx/kpeg
    strip-prefix: v

vars:
  gem: kpeg
