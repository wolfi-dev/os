package:
  name: ruby3.4-mail
  version: "2.9.1"
  epoch: 0 # go/wolfi-rsc/ruby3.4-mail
  description: A really Ruby Mail handler.
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - ruby${{vars.rubyMM}}-mini_mime
      - ruby${{vars.rubyMM}}-net-imap
  resources:
    cpu: "1"
    memory: 4Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/mikel/mail
      tag: ${{package.version}}
      expected-commit: d645d7b5b7d03218b0bc8971ef041c991a63f5f7

  - name: Fix gemspec version
    runs: |
      sed -i "s/BUILD = 'edge'/BUILD = nil/" lib/mail/version.rb

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: mail

update:
  enabled: true
  github:
    identifier: mikel/mail
    use-tag: true

var-transforms:
  - from: ${{package.name}}
    match: ^ruby(\d\.\d+)-.*
    replace: $1
    to: rubyMM

test:
  pipeline:
    - uses: test/tw/gem-check
