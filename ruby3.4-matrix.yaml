# Generated from https://github.com/ruby/matrix
package:
  name: ruby3.4-matrix
  version: 0.4.2
  epoch: 0
  description: An implementation of Matrix and Vector classes.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby${{vars.rubyMM}}-rubygems
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: 6e88428e55cc58c574a8e1392212f7bb72cf43cd1a42116de2e6d6ca5ab1f804
      uri: https://github.com/ruby/matrix/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: matrix
  rubyMM: "3.4"

update:
  enabled: true
  github:
    identifier: ruby/matrix
    strip-prefix: v
