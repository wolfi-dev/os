# Generated from https://github.com/minitest/minitest
package:
  name: ruby3.4-minitest
  version: 5.25.2
  epoch: 0
  description: minitest provides a complete suite of testing facilities supporting
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby${{vars.rubyMM}}-hoe
      - ruby${{vars.rubyMM}}-rake
      - ruby${{vars.rubyMM}}-rubygems
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: c6b340b5ad9bebfc1e8dc41e2e561178669f2b53cb8a30c73242338386fb4384
      uri: https://github.com/minitest/minitest/archive/refs/tags/v${{package.version}}.tar.gz

  - runs: |
      rake gem
      cp ./pkg/*.gem ./

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

update:
  enabled: true
  github:
    identifier: minitest/minitest
    strip-prefix: v
    use-tag: true

vars:
  gem: minitest
  rubyMM: "3.4"
