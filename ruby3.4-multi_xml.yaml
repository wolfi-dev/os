# Generated from https://github.com/sferik/multi_xml
package:
  name: ruby3.4-multi_xml
  version: 0.7.1
  epoch: 0
  description: Provides swappable XML backends utilizing LibXML, Nokogiri, Ox, or REXML.
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - ruby3.4-bigdecimal

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: 21738b8d51d96c972381930afbf74bd0b4a8bbdc4f2ffa1dd7ac27a07a81833e
      uri: https://github.com/sferik/multi_xml/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: multi_xml
  rubyMM: "3.4"
