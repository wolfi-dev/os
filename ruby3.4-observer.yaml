# Generated from https://github.com/ruby/observer
package:
  name: ruby3.4-observer
  version: 0.1.2
  epoch: 0
  description: Implementation of the Observer object-oriented design pattern.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: eb3d6b7b38eaa8cb00a6b59eb51b37faf3c04009ae6a5881ea7437ad2ed82ebb
      uri: https://github.com/ruby/observer/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: observer
  rubyMM: "3.4"
