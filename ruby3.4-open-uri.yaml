# Generated from https://github.com/ruby/open-uri
package:
  name: ruby3.4-open-uri
  version: 0.5.0
  epoch: 0
  description: An easy-to-use wrapper for Net::HTTP, Net::HTTPS and Net::FTP.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause
  dependencies:
    runtime:
      - ruby3.4-stringio
      - ruby3.4-time
      - ruby3.4-uri

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: a889bfe0e577ec2d29c7986abebc00142070d50e9477deb15df2e13f6b620d93
      uri: https://github.com/ruby/open-uri/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: open-uri
  rubyMM: "3.4"
