# Generated from https://github.com/ruby/ostruct
package:
  name: ruby3.4-ostruct
  version: 0.6.1
  epoch: 0
  description: Class to build custom data structures, similar to a Hash.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: e11502d37eea71bbe43ecac3e8aa4cf83ec968c87c34a19dcd2cdf52ee234548
      uri: https://github.com/ruby/ostruct/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: ostruct
  rubyMM: "3.4"
