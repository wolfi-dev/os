# Generated from https://github.com/ruby/pathname
package:
  name: ruby3.4-pathname
  version: 0.4.0
  epoch: 0
  description: Representation of the name of a file or directory on the filesystem
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: c79c595917eb1a0cf995955dcda41651867f00b9fb6b0048377c8de6e71a0234
      uri: https://github.com/ruby/pathname/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: pathname
  rubyMM: "3.4"
