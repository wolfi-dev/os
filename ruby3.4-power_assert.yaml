# Generated from https://github.com/ruby/power_assert
package:
  name: ruby3.4-power_assert
  version: 2.0.4
  epoch: 0
  description: Power Assert shows each value of variables and method calls in the expression. It is useful for testing, providing which value wasn't correct when the condition is not satisfied.
  copyright:
    - license: BSD-2-Clause
    - license: Ruby

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-3.4
      - ruby-3.4-dev
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: 35122c2496b69db03912db6604b2e7462ad726facfb3d7eda580213e2325ea99
      uri: https://github.com/ruby/power_assert/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

update:
  enabled: true
  github:
    identifier: ruby/power_assert
    strip-prefix: v
    use-tag: true

vars:
  gem: power_assert
