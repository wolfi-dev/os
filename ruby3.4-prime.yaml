# Generated from https://github.com/ruby/prime
package:
  name: ruby3.4-prime
  version: 0.1.3
  epoch: 0
  description: Prime numbers and factorization library.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause
  dependencies:
    runtime:
      - ruby3.4-forwardable
      - ruby3.4-singleton

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby${{vars.rubyMM}}-rubygems
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: d851979a21765f908e21acb2f2d897aad61b71e50062c5b53f89c2dd01c3326a
      uri: https://github.com/ruby/prime/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

update:
  enabled: true
  github:
    identifier: ruby/prime
    strip-prefix: v
    use-tag: true

vars:
  gem: prime
  rubyMM: "3.4"
