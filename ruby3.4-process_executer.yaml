# Generated from https://github.com/main-branch/process_executer
package:
  name: ruby3.4-process_executer
  version: 1.2.0
  epoch: 0
  description: An API for executing commands in a subprocess
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: ffc5d8d992679f7ea4c8493f85421ba8b5bc536cce44919efcdd683a5f38967a
      uri: https://github.com/main-branch/process_executer/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: process_executer
  rubyMM: "3.4"
