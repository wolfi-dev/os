# Generated from https://github.com/ruby/psych
package:
  name: ruby3.4-psych
  version: "5.4.0"
  epoch: 0
  description: Psych is a YAML parser and emitter.
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - ruby${{vars.rubyMM}}-stringio
      - ruby-${{vars.rubyMM}}

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev
      - yaml-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/ruby/psych.git
      tag: v${{package.version}}
      expected-commit: f7066d8f5ece9a62768c77182a6abbf261ffc6da

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: psych

test:
  pipeline:
    - runs: ruby -e "require 'psych'"
    - uses: test/tw/ldd-check

update:
  enabled: true
  require-sequential: true
  github:
    identifier: ruby/psych
    strip-prefix: v
    use-tag: true

var-transforms:
  - from: ${{package.name}}
    match: ^ruby(\d\.\d+)-.*
    replace: $1
    to: rubyMM
