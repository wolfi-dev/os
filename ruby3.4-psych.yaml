# Generated from https://github.com/ruby/psych
package:
  name: ruby3.4-psych
  version: 5.2.0
  epoch: 0
  description: Psych is a YAML parser and emitter. Psych leverages libyaml[https://pyyaml.org/wiki/LibYAML]
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - ruby3.4-stringio

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev
      - ruby3.4-rubygems
      - yaml-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 90d77fe09d2851bf819785f0286a846434b602703517f83d804c93f0e6f1f398
      uri: https://github.com/ruby/psych/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: psych
  rubyMM: "3.4"
