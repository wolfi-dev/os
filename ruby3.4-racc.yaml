# Generated from https://github.com/ruby/racc
package:
  name: ruby3.4-racc
  version: 1.8.1
  epoch: 0
  copyright:
    - license: Ruby

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-3.4
      - ruby-3.4-dev
      - ruby3.4-bundler
      - ruby3.4-rake
      - ruby3.4-rake-compiler
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: affc4397e1f90ed8baf9eeaf30aeba1b9f25ac7e22e4bacb644415fe7886cacc
      uri: https://github.com/ruby/racc/archive/refs/tags/v${{package.version}}.tar.gz

  - runs: |
      rake compile
      rake build

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: racc

update:
  enabled: true
  github:
    identifier: ruby/racc
    use-tag: true
    strip-prefix: v

test:
  pipeline:
    - runs: |
        racc --version
        ruby -e 'require "racc"'
