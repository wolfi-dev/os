# Generated from https://github.com/ruby/racc
package:
  name: ruby3.4-racc
  version: 1.8.1
  epoch: 0
  description: |
    Racc is an LALR(1) parser generator.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    build_repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-3.4
      - ruby-3.4-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: affc4397e1f90ed8baf9eeaf30aeba1b9f25ac7e22e4bacb644415fe7886cacc
      uri: https://github.com/ruby/racc/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: patch
    with:
      patches: fix-gemspec.patch

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

  - runs: |
      rm -f ${{targets.destdir}}/usr/bin/racc

vars:
  gem: racc
