# Generated from https://github.com/rack/rack
package:
  name: ruby3.4-rack
  version: 3.1.8
  epoch: 0
  description: Rack provides a minimal, modular and adaptable interface for developing
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: 49f04a6258be944d732d8360cdc754d996be68619ed4c202c1716a1f8679a5b7
      uri: https://github.com/rack/rack/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: rack
  rubyMM: "3.4"
