# Generated from https://github.com/ruby/resolv-replace
package:
  name: ruby3.4-resolv-replace
  version: 0.1.1
  epoch: 0
  description: Replace Socket DNS with Resolv.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause
  dependencies:
    runtime:
      - ruby3.4-resolv

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 866defbd3790d19de28e874c56106b456d3fcfa4f91fe5e2cafb7c17be182f82
      uri: https://github.com/ruby/resolv-replace/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: resolv-replace
  rubyMM: "3.4"
