# Generated from https://github.com/ruby/resolv
package:
  name: ruby3.4-resolv
  version: 0.5.0
  epoch: 0
  description: Thread-aware DNS resolver library in Ruby.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: af4a7be7ec71cca333550c0f32bb5be12e9a554a578e938f1781be8e0930991d
      uri: https://github.com/ruby/resolv/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: resolv
  rubyMM: "3.4"
