# Generated from https://github.com/ruby/securerandom
package:
  name: ruby3.4-securerandom
  version: 0.4.0
  epoch: 0
  description: Interface for secure random number generator.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: bc4409abe759660efa4db0c06c63f66bd746c81e08666fcec51e729c32ea4265
      uri: https://github.com/ruby/securerandom/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: securerandom
  rubyMM: "3.4"
