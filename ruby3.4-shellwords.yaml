# Generated from https://github.com/ruby/shellwords
package:
  name: ruby3.4-shellwords
  version: 0.2.1
  epoch: 0
  description: Manipulates strings with word parsing rules of UNIX Bourne shell.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 79e43f18822c6dc9405320f50dc58b1ce5ad81436eaef3feb18b65839fd46c74
      uri: https://github.com/ruby/shellwords/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: shellwords
  rubyMM: "3.4"
