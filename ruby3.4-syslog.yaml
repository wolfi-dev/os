# Generated from https://github.com/ruby/syslog
package:
  name: ruby3.4-syslog
  version: 0.1.2
  epoch: 0
  description: Ruby interface for the POSIX system logging facility.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 57a34cb2ae2f6646661ca415b7236f82e4d339af647e3bae6388505e51f724e2
      uri: https://github.com/ruby/syslog/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: syslog
  rubyMM: "3.4"
