# Generated from https://github.com/test-unit/test-unit
package:
  name: ruby3.4-test-unit
  version: 3.6.2
  epoch: 0
  description: |
    test-unit (Test::Unit) is unit testing framework for Ruby, based on xUnit
  copyright:
    - license: Ruby
  dependencies:
    runtime:
      - ruby3.4-power_assert

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-3.4-bootstrap
      - ruby-3.4-bootstrap-dev
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: 66ed3c3dad041b6af3932cde37d007a600870357099f06d364a0c1d986225295
      uri: https://github.com/test-unit/test-unit/archive/refs/tags/${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: test-unit
