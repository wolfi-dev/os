# Generated from https://github.com/ruby/time
package:
  name: ruby3.4-time
  version: 0.4.1
  epoch: 0
  description: Extends the Time class with methods for parsing and conversion.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause
  dependencies:
    runtime:
      - ruby3.4-date

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby${{vars.rubyMM}}-rubygems
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 3cb14bc84d3d8bbcd5f596d57a649049f60dd37a175e9c6a3445b59ef8978b31
      uri: https://github.com/ruby/time/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: time
  rubyMM: "3.4"
