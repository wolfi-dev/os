# Generated from https://github.com/ruby/tmpdir
package:
  name: ruby3.4-tmpdir
  version: 0.3.0
  epoch: 0
  description: Extends the Dir class to manage the OS temporary file path.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause
  dependencies:
    runtime:
      - ruby3.4-fileutils

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 5c0389cf95a18a81bf46c71a9992a604b0da85e43c0c10d69595f75e02ee2a31
      uri: https://github.com/ruby/tmpdir/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: tmpdir
  rubyMM: "3.4"
