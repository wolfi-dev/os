# Generated from https://github.com/ruby/typeprof
package:
  name: ruby3.4-typeprof
  version: 0.21.11
  epoch: 0
  description: TypeProf performs a type analysis of non-annotated Ruby code.
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - ruby3.4-rbs

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby${{vars.rubyMM}}-rubygems
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 14349e8112537d6a592d7fbff1f1885ef96ed37d530798c21cb3512800b94e58
      uri: https://github.com/ruby/typeprof/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: typeprof
  rubyMM: "3.4"
