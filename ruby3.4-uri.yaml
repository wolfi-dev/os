# Generated from https://github.com/ruby/uri
package:
  name: ruby3.4-uri
  version: 1.0.2
  epoch: 0
  description: URI is a module providing classes to handle Uniform Resource Identifiers
  copyright:
    - license: Ruby
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev
      - ruby3.4-rubygems

pipeline:
  - uses: fetch
    with:
      expected-sha256: c93760bcf144a0452fe74a180f47ca21892c2e7d8c22ed4cc1999b0d2da96852
      uri: https://github.com/ruby/uri/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: uri
  rubyMM: "3.4"
