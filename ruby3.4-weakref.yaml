# Generated from https://github.com/ruby/weakref
package:
  name: ruby3.4-weakref
  version: 0.1.3
  epoch: 0
  description: Allows a referenced object to be garbage-collected.
  copyright:
    - license: Ruby
    - license: BSD-2-Clause
  dependencies:
    runtime:
      - ruby3.4-delegate

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 281da03d5e8060fb986ddc4405f95bb6ad7462ff23f91141024eecaffe73399b
      uri: https://github.com/ruby/weakref/archive/refs/tags/v${{package.version}}.tar.gz

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

vars:
  gem: weakref
  rubyMM: "3.4"
