package:
  name: ruby4.0-console
  version: "1.37.0"
  epoch: 1
  description: Beautiful logging for Ruby.
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - ruby${{vars.rubyMM}}-fiber-annotation
      - ruby${{vars.rubyMM}}-fiber-local
      - ruby${{vars.rubyMM}}-fiber-storage
      - ruby${{vars.rubyMM}}-json
      - ruby-${{vars.rubyMM}}

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ruby-${{vars.rubyMM}}
      - ruby-${{vars.rubyMM}}-dev

vars:
  gem: console

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 4d03637ae5c53cdcb199b7e56a394fba6f0442ae
      repository: https://github.com/socketry/console
      tag: v${{package.version}}

  - uses: ruby/build
    with:
      gem: ${{vars.gem}}

  - uses: ruby/install
    with:
      gem: ${{vars.gem}}
      version: ${{package.version}}

  - uses: ruby/clean

test:
  pipeline:
    - uses: test/tw/gem-check
    - runs: |
        ruby -e "require 'console'; puts 'Console library loaded successfully!'"
    - runs: |
        ruby <<-EOF
        require 'console'

        # Create a logger instance
        logger = Console.logger

        # Log a single message
        logger.info "Test log message"

        # Verify it doesn't raise errors
        puts "Console basic logging verified!"
        EOF

update:
  enabled: true
  github:
    identifier: socketry/console
    strip-prefix: v
    use-tag: true

var-transforms:
  - from: ${{package.name}}
    match: ^ruby(\d\.\d+)-.*
    replace: $1
    to: rubyMM
