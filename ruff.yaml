package:
  name: ruff
  version: 0.1.2
  epoch: 0
  description: An extremely fast Python linter, written in Rust.
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - ca-certificates-bundle
      - busybox
      - build-base
      - rust

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/astral-sh/ruff
      tag: v${{package.version}}
      expected-commit: 3127c79b29d72f5b38d8a6bc81e069e14085ce11

  - runs: |
      cargo build --release
      mkdir -p ${{targets.contextdir}}/usr/bin
      mv target/release/ruff ${{targets.contextdir}}/usr/bin/

  - uses: strip

subpackages:
  - name: flake8-to-ruff
    pipeline:
      - runs: |
          mkdir -p ${{targets.contextdir}}/usr/bin
          mv target/release/flake8-to-ruff ${{targets.contextdir}}/usr/bin/

  - name: ruff-python-formatter
    pipeline:
      - runs: |
          mkdir -p ${{targets.contextdir}}/usr/bin
          ls target/release/
          mv target/release/ruff_python_formatter ${{targets.contextdir}}/usr/bin/

update:
  enabled: true
  github:
    identifier: astral-sh/ruff
    strip-prefix: v
