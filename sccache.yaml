package:
  name: sccache
  version: 0.9.0
  epoch: 1
  description: sccache is ccache with cloud storage
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - cargo-auditable
      - openssf-compiler-options
      - openssl-dev
      - rust

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/mozilla/sccache
      tag: v${{package.version}}
      expected-commit: ac3bff7e70728a7e15ad756a1a9acc4312616624

  - uses: rust/cargobump

  - runs: |
      cargo auditable build --release
      mkdir -p ${{targets.destdir}}/usr/bin
      mv target/release/sccache ${{targets.destdir}}/usr/bin/

  - uses: strip

update:
  enabled: true
  github:
    identifier: mozilla/sccache
    strip-prefix: v

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        sccache --version
        sccache --help
