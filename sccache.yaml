package:
  name: sccache
  version: 0.8.2
  epoch: 0
  description: sccache is ccache with cloud storage
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - cargo-auditable
      - openssl-dev
      - rust

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/mozilla/sccache
      tag: v${{package.version}}
      expected-commit: d7763fc54d52a4bf12de2f75a55ae089753b8320

  - uses: rust/cargobump

  - runs: |
      cargo auditable build --release
      mkdir -p ${{targets.destdir}}/usr/bin
      mv target/release/sccache ${{targets.destdir}}/usr/bin/

  - uses: strip

update:
  enabled: true
  github:
    identifier: mozilla/sccache
    strip-prefix: v

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        sccache --version
