package:
  name: sftpgo-plugin-geoipfilter
  version: "1.0.12"
  epoch: 5 # CVE-2026-32280
  description: "Geo-IP filtering support for SFTPGo"
  copyright:
    - license: AGPL-3.0-only
  resources:
    cpu: "2"
    memory: 7Gi

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/sftpgo/sftpgo-plugin-geoipfilter
      tag: v${{package.version}}
      expected-commit: 24595d9024f1358ebf63dbd531155428ff7a7d0b

  - uses: go/bump
    with:
      deps: |-
        google.golang.org/grpc@v1.79.3

  - uses: go/build
    with:
      packages: .
      output: sftpgo-plugin-geoipfilter
      ldflags: -X github.com/sftpgo/sftpgo-plugin-geoipfilter/cmd.version=${{package.version}} -X github.com/sftpgo/sftpgo-plugin-geoipfilter/cmd.commitHash=$(git describe --always) -X github.com/sftpgo/sftpgo-plugin-geoipfilter/cmd.buildDate=$(date -u -d "@$SOURCE_DATE_EPOCH" +%FT%TZ)

test:
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/tw/help-check
      with:
        bins: sftpgo-plugin-geoipfilter
        expect-contains: serve
    - uses: test/tw/ver-check
      with:
        bins: sftpgo-plugin-geoipfilter
        version: ${{package.version}}
    - name: Verify plugin serve subcommand
      runs: |
        set -euo pipefail
        # This is a HashiCorp go-plugin binary that implements the SFTPGo
        # geo-IP filter plugin protocol. The 'serve' subcommand is the core
        # entry point that starts the gRPC plugin server when invoked by SFTPGo.
        # Verify it is registered and accessible.
        sftpgo-plugin-geoipfilter serve --help 2>&1

update:
  enabled: true
  github:
    identifier: sftpgo/sftpgo-plugin-geoipfilter
    use-tag: true
    strip-prefix: v
