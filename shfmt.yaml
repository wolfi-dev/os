package:
  name: shfmt
  version: "3.13.0"
  epoch: 0 # CVE-2025-61732
  description: A shell formatter
  copyright:
    - license: BSD-3-Clause
  resources:
    cpu: "2"
    memory: 1Gi

environment:
  environment:
    CGO_ENABLED: "0"

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/mvdan/sh
      tag: v${{package.version}}
      expected-commit: 5c4d285c3e8fa3b85137b34cec5ce66b98d97bdc

  - uses: go/build
    with:
      packages: ./cmd/shfmt
      ldflags: -X main.version=v${{package.version}}
      output: shfmt

update:
  enabled: true
  github:
    identifier: mvdan/sh
    strip-prefix: v

test:
  pipeline:
    - runs: |
        shfmt --version
        shfmt --help
