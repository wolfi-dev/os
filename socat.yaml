package:
  name: socat
  version: "1.8.1.1"
  epoch: 0
  description: Multipurpose relay for binary protocols
  copyright:
    - license: GPL-2.0-only

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - linux-headers
      - openssl-dev
      - readline-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: f68b602c80e94b4b7498d74ec408785536fe33534b39467977a82ab2f7f01ddb
      uri: http://www.dest-unreach.org/socat/download/socat-${{package.version}}.tar.gz

  - uses: patch
    with:
      patches: netdb-internal.patch use-linux-headers.patch

  - uses: autoconf/configure

  - runs: |
      make
      make DESTDIR="${{targets.destdir}}" install

  - uses: strip

subpackages:
  - name: socat-doc
    pipeline:
      - uses: split/manpages
      - runs: |
          mkdir -p "${{targets.subpkgdir}}"/usr/share/socat
          install -m644 EXAMPLES doc/*.html doc/*.css "${{targets.subpkgdir}}"/usr/share/socat
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 4848

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        filan -h
        procan -h
        socat -h
        socat1 -h
