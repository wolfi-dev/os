# Generated from https://git.alpinelinux.org/aports/plain/main/spirv-tools/APKBUILD
package:
  name: spirv-tools
  version: 1.3.261.1
  epoch: 0
  description: API and commands for processing SPIR-V modules
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - cmake
      - python3
      - samurai
      - spirv-headers

pipeline:
  - uses: fetch
    with:
      expected-sha256: ead95c626ad482882a141d1aa0ce47b9453871f72c42c0b28d39c82f60a52008
      uri: https://github.com/KhronosGroup/SPIRV-tools/archive/refs/tags/sdk-${{package.version}}.tar.gz

  - runs: |
      CXXFLAGS="$CXXFLAGS -g1 -flto=auto" \
      cmake -B build -G Ninja \
        -DCMAKE_INSTALL_PREFIX=/usr \
        -DCMAKE_INSTALL_LIBDIR=lib \
        -DCMAKE_BUILD_TYPE=MinSizeRel \
        -DBUILD_SHARED_LIBS=ON \
        -DSPIRV_WERROR=OFF \
        -DSPIRV-Headers_SOURCE_DIR=/usr \
        -DSPIRV_TOOLS_BUILD_STATIC=OFF
      cmake --build build
      DESTDIR="${{targets.destdir}}" cmake --install build

  - uses: strip

subpackages:
  - name: spirv-tools-dev
    pipeline:
      - uses: split/dev
    dependencies:
    description: spirv-tools dev

update:
  enabled: true
  release-monitor:
    identifier: 334920
