# Generated from https://git.alpinelinux.org/aports/plain/main/spirv-tools/APKBUILD
package:
  name: spirv-tools
  version: 1.3.261.1
  epoch: 2
  description: API and commands for processing SPIR-V modules
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - cmake
      - python3
      - samurai
      - spirv-headers

pipeline:
  - uses: git-checkout
    with:
      expected-commit: e553b884c7c9febaa4e52334f683641fb5f196a0
      repository: https://github.com/KhronosGroup/SPIRV-tools
      tag: sdk-${{package.version}}

  - runs: |
      CXXFLAGS="$CXXFLAGS -g1 -flto=auto" \
      cmake -B build -G Ninja \
        -DCMAKE_INSTALL_PREFIX=/usr \
        -DCMAKE_INSTALL_LIBDIR=lib \
        -DCMAKE_BUILD_TYPE=MinSizeRel \
        -DBUILD_SHARED_LIBS=ON \
        -DSPIRV_WERROR=OFF \
        -DSPIRV-Headers_SOURCE_DIR=/usr \
        -DSPIRV_TOOLS_BUILD_STATIC=OFF
      cmake --build build
      DESTDIR="${{targets.destdir}}" cmake --install build

  - uses: strip

subpackages:
  - name: spirv-tools-dev
    pipeline:
      - uses: split/dev
    dependencies:
    description: spirv-tools dev
    test:
      pipeline:
        - uses: test/pkgconf

update:
  enabled: true
  github:
    identifier: KhronosGroup/SPIRV-tools
    strip-prefix: sdk-
    tag-filter: sdk-
    use-tag: true

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        spirv-as --version
        spirv-cfg --version
        spirv-dis --version
        spirv-link --version
        spirv-lint --version
        spirv-objdump --version
        spirv-opt --version
        spirv-reduce --version
        spirv-val --version
        spirv-as --help
        spirv-cfg --help
        spirv-dis --help
        spirv-link --help
        spirv-lint --help
        spirv-objdump --help
        spirv-opt --help
        spirv-reduce --help
        spirv-val --help
    - uses: test/tw/ldd-check
