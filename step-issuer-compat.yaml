package:
  name: step-issuer-compat
  version: 0.0.1
  epoch: 0
  description: step-issuer compatability
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - busybox

pipeline:
  - runs: |
      cd ${{targets.destdir}}
      ln -s /usr/bin/step-issuer manager

update:
  enabled: false