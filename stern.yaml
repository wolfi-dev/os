package:
  name: stern
  version: "1.33.1"
  epoch: 1 # CVE-2025-61729
  description: Multi pod and container log tailing for Kubernetes
  copyright:
    - license: Apache-2.0

environment:
  environment:
    CGO_ENABLED: "0"

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/stern/stern
      tag: v${{package.version}}
      expected-commit: 72148a9169efb0cefc43086c1ff05216e88ed829

  - uses: go/build
    with:
      packages: .
      output: stern
      ldflags: |
        -X github.com/stern/stern/cmd.version=v${{package.version}}
        -X github.com/stern/stern/cmd.commit=$(git rev-parse HEAD)
        -X github.com/stern/stern/cmd.date=$(date -d@${SOURCE_DATE_EPOCH} +%F-%T)

update:
  enabled: true
  github:
    identifier: stern/stern
    strip-prefix: v

test:
  pipeline:
    - runs: |
        stern --version
        stern --help
