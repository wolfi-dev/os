package:
  name: suricata-update
  version: 1.3.3
  epoch: 6
  description: "The tool for updating your Suricata rules"
  copyright:
    - license: GPL-2.0-only
  dependencies:
    runtime:
      - py3-pyyaml
      - python3

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - py3-pip

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/OISF/suricata-update
      tag: ${{package.version}}
      expected-commit: a81f80518bac1cda2698937422c72a16f39fef73

  - uses: py/pip-build-install

  - uses: strip

update:
  enabled: true
  github:
    identifier: OISF/suricata-update
    use-tag: true

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        suricata-update --version
