package:
  name: swig
  version: "4.3.1"
  epoch: 0
  description: A compiler that makes it easy to integrate C and C++ code with scripting languages
  copyright:
    - license: GPL-3.0-only

environment:
  contents:
    packages:
      - autoconf
      - automake
      - bison
      - build-base
      - busybox
      - ca-certificates-bundle
      - guile
      - pcre2-dev
      - zlib-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 44fc829f70f1e17d635a2b4d69acab38896699ecc24aa023e516e0eabbec61b8
      uri: https://downloads.sourceforge.net/project/swig/swig/swig-${{package.version}}/swig-${{package.version}}.tar.gz

  - uses: autoconf/configure
    with:
      opts: |
        --host=${{host.triplet.gnu}} \
        --build=${{host.triplet.gnu}} \
        --with-python3=/usr/bin/python3

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

update:
  enabled: true
  release-monitor:
    identifier: 4919

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        ccache-swig -h
        swig --help
