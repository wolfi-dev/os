package:
  name: tealdeer
  version: "1.8.0"
  epoch: 0
  description: A very fast implementation of tldr in Rust.
  copyright:
    - license: Apache-2.0
    - license: MIT
  dependencies:
    provides:
      - tldr=${{package.full-version}}

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/tealdeer-rs/tealdeer
      tag: v${{package.version}}
      expected-commit: d49c4a9e05eab5018e322c03da01279040def538

  - uses: rust/cargobump

  - name: Configure and Build
    uses: cargo/build
    with:
      modroot: .
      output: tldr

  - uses: strip

update:
  enabled: true
  github:
    identifier: tealdeer-rs/tealdeer
    strip-prefix: v

test:
  pipeline:
    - name: tldr version
      runs: |
        tldr --version
        tldr --help
    - name: do a tldr test
      runs: |
        tldr --update
        tldr strace
