package:
  name: test
  version: 0.1.0
  epoch: 0

environment:
  contents:
    packages:
      - build-base
      - busybox

pipeline:
  - runs: |
      set -x
      id
      id build
      grep Cap /proc/self/status
      sleep infinity
      exit 1
