# Generated from https://git.alpinelinux.org/aports/plain/main/tig/APKBUILD
package:
  name: tig
  version: 2.5.10
  epoch: 1
  description: Text-mode interface for the git revision control system
  copyright:
    - license: GPL-2.0-or-later

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ncurses-dev
      - openssf-compiler-options
      - pcre2-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: f655cc1366fc10058a2bd505bb88ca78e653ff7526c1b81774c44b9d841210e3
      uri: https://github.com/jonas/tig/releases/download/tig-${{package.version}}/tig-${{package.version}}.tar.gz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: tig-doc
    pipeline:
      - uses: split/manpages
    description: tig manpages

update:
  enabled: true
  github:
    identifier: jonas/tig
    strip-prefix: tig-

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        tig --version
        tig --help
