# Generated from https://git.alpinelinux.org/aports/plain/main/tig/APKBUILD
package:
  name: tig
  version: "2.5.12"
  epoch: 0
  description: Text-mode interface for the git revision control system
  copyright:
    - license: GPL-2.0-or-later

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - ncurses-dev
      - pcre2-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 5dda8a098810bb499096e17fc9f69c0a5915a23f46be27209fc8195d7a792108
      uri: https://github.com/jonas/tig/releases/download/tig-${{package.version}}/tig-${{package.version}}.tar.gz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install
    with:
      opts: install-doc-man

  - uses: strip

subpackages:
  - name: tig-doc
    pipeline:
      - uses: split/manpages
    description: tig manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  github:
    identifier: jonas/tig
    strip-prefix: tig-

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        tig --version
        tig --help
