package:
  name: tofu-controller
  version: "0.16.1"
  epoch: 8
  description: A GitOps OpenTofu and Terraform controller for Flux
  copyright:
    - license: Apache-2.0
  dependencies:
    runtime:
      - git
      - gnupg
      - libcrypto3
      - libssl3
      - openssh-client
      - tini
  resources:
    cpu: "5"
    memory: 7Gi
  test-resources:
    cpu: "1"
    memory: 4Gi

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/flux-iac/tofu-controller.git
      tag: v${{package.version}}
      expected-commit: fa21532c44bffbff0e43cd63031e35ca201fc719

  - uses: go/bump
    with:
      deps: |-
        google.golang.org/grpc@v1.79.3

  - uses: go/build
    with:
      extra-args: -gcflags="-N -l"
      packages: ./cmd/manager
      output: tofu-controller
      ldflags: |
        -X main.BuildSHA=$(git rev-parse HEAD)
        -X main.BuildVersion=${{package.version}}

subpackages:
  - name: ${{package.name}}-runner
    description: tf-runner binary for tofu-controller
    dependencies:
      runtime:
        - git
        - gnupg
        - libcrypto3
        - libssl3
        - openssh-client
        - tini
    pipeline:
      - uses: go/build
        with:
          extra-args: -gcflags="-N -l"
          packages: ./cmd/runner/main.go
          output: tf-runner
          ldflags: |
            -X main.BuildSHA=$(git rev-parse HEAD)
            -X main.BuildVersion=${{package.version}}
    test:
      pipeline:
        - uses: test/tw/help-check
          with:
            bins: tf-runner
            expect-contains: "Usage of tf-runner:"

  - name: ${{package.name}}-compat
    description: Compat package for ${{package.name}}
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/local/bin
          ln -sf /usr/bin/tofu-controller "${{targets.contextdir}}"/usr/local/bin/tofu-controller
    test:
      environment:
        contents:
          packages:
            # -compat packages can be satisfied by multiple source packages; install here for test only, not as a runtime dep
            - ${{package.name}}
      pipeline:
        - uses: test/tw/symlink-check
          with:
            allow-absolute: true

  - name: ${{package.name}}-runner-compat
    description: Compat package for ${{package.name}} runner
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"/usr/local/bin
          ln -sf /usr/bin/tf-runner "${{targets.contextdir}}"/usr/local/bin/tf-runner
    test:
      environment:
        contents:
          packages:
            # -compat packages can be satisfied by multiple source packages; install here for test only, not as a runtime dep
            - ${{package.name}}-runner
      pipeline:
        - uses: test/tw/symlink-check
          with:
            allow-absolute: true

update:
  enabled: true
  github:
    identifier: flux-iac/tofu-controller
    strip-prefix: v
    use-tag: true

test:
  environment:
    environment:
      POD_NAMESPACE: default
    contents:
      packages:
        - openssl
        - ${{package.name}}-runner
  pipeline:
    - uses: test/kwok/cluster
    - name: "Install required CRDs for tofu controller to pick up"
      runs: |
        set -euo pipefail
        cat <<EOF | kubectl apply -f -
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          name: terraforms.infra.contrib.fluxcd.io
        spec:
          group: infra.contrib.fluxcd.io
          names:
            kind: Terraform
            listKind: TerraformList
            plural: terraforms
            singular: terraform
          scope: Namespaced
          versions:
            - name: v1alpha2
              served: true
              storage: true
              schema:
                openAPIV3Schema:
                  type: object
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          name: gitrepositories.source.toolkit.fluxcd.io
        spec:
          group: source.toolkit.fluxcd.io
          names:
            kind: GitRepository
            listKind: GitRepositoryList
            plural: gitrepositories
            singular: gitrepository
          scope: Namespaced
          versions:
            - name: v1
              served: true
              storage: true
              schema:
                openAPIV3Schema:
                  type: object
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          name: buckets.source.toolkit.fluxcd.io
        spec:
          group: source.toolkit.fluxcd.io
          names:
            kind: Bucket
            listKind: BucketList
            plural: buckets
            singular: bucket
          scope: Namespaced
          versions:
            - name: v1
              served: true
              storage: true
              schema:
                openAPIV3Schema:
                  type: object
        ---
        apiVersion: apiextensions.k8s.io/v1
        kind: CustomResourceDefinition
        metadata:
          name: ocirepositories.source.toolkit.fluxcd.io
        spec:
          group: source.toolkit.fluxcd.io
          names:
            kind: OCIRepository
            listKind: OCIRepositoryList
            plural: ocirepositories
            singular: ocirepository
          scope: Namespaced
          versions:
            - name: v1
              served: true
              storage: true
              schema:
                openAPIV3Schema:
                  type: object
        EOF
    - name: "Start tofu-controller"
      runs: |
        set -euo pipefail
        sleep 10
        tofu-controller > /tmp/tofu-controller.log 2>&1 &
        sleep 30
        ps aux | grep -qF tofu-controller
    - name: "Validations"
      runs: |
        cat /tmp/tofu-controller.log
        grep -qF "Starting manager" /tmp/tofu-controller.log
        grep -qF "starting server" /tmp/tofu-controller.log
        grep -qF "Starting EventSource" /tmp/tofu-controller.log
        grep -qF "Starting Controller" /tmp/tofu-controller.log
    - name: "Startup tf-runner"
      runs: |
        set -euo pipefail
        # Generating a self-signed TLS cert key for use with tf-runner.
        # Upstream code for tf-runner attempts to parse the private key using 'x509.ParsePKCS1PrivateKey', which
        # only supports traditional PKCS#1 (RSA) key format.
        # REF: https://github.com/flux-iac/tofu-controller/blob/67198a27f35b97119492965ea1fe7a9034cc4476/mtls/rotator.go#L521
        #
        # If the key is provided in PKCS#8 format (default in OpenSSL 3.x),
        # tf-runner will fail at startup with:
        #     "failed to parse private key (use ParsePKCS8PrivateKey instead for this key format)"
        #
        # To avoid this error, we convert the PKCS#8 key to to PKCS#1

        CERTDIR="$(mktemp -d)"
        openssl genpkey -algorithm RSA -out "${CERTDIR}/tmp.key" -pkeyopt rsa_keygen_bits:2048
        openssl pkey -in "${CERTDIR}/tmp.key" -out "${CERTDIR}/tls.key" -traditional

        openssl req -new -x509 -sha256 -days 365 \
        -key "${CERTDIR}/tls.key" \
        -out "${CERTDIR}/tls.crt" \
        -subj "/CN=localhost/O=Test tfrunner./C=US"

        # Create secret for tf-runner
        kubectl create secret generic tf-runner-tls \
        --from-file=tls.crt="${CERTDIR}/tls.crt" \
        --from-file=tls.key="${CERTDIR}/tls.key" \
        --from-file=ca.crt="${CERTDIR}/tls.crt" \
        --from-file=ca.key="${CERTDIR}/tls.key"

        # Startup tf-runner
        tf-runner --tls-secret-name="tf-runner-tls" > /tmp/tf-runner.log 2>&1 &
        sleep 3
        ps aux | grep -qF tf-runner
    - name: "TF-Runner Validations"
      runs: |
        grep -qF "Starting the runner" /tmp/tf-runner.log

environment:
  contents:
    packages:
      - go-1.25
