package:
  name: traefik-3.7
  version: "3.7.3"
  epoch: 1 # GHSA-vvgj-x9jq-8cj9
  description: The Cloud Native Application Proxy
  copyright:
    - license: MIT
  dependencies:
    provides:
      - traefik=${{package.full-version}}
  resources:
    cpu: "8"
    memory: 17Gi
  test-resources:
    cpu: "1"
    memory: 1Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - git
      - go-1.26
      - nodejs-20
      - yarn-berry

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 5ea71f1c3af049067a0b6d5afd97bad1cf1a0071
      repository: https://github.com/traefik/traefik
      tag: v${{package.version}}

  - uses: bump
    with:
      deps: github.com/quic-go/quic-go@v0.59.1

  - working-directory: webui
    runs: |
      yarn install
      yarn build

  - uses: go/build
    with:
      modroot: .
      packages: ./cmd/traefik
      output: traefik
      ldflags: |
        -X github.com/traefik/traefik/v3/pkg/version.Version=${{package.version}}
        -X github.com/traefik/traefik/v3/pkg/version.BuildDate=$(date -u -d "@$SOURCE_DATE_EPOCH" '+%Y-%m-%d_%I:%M:%S%p')
        -X github.com/traefik/traefik/v3/pkg/version.Codename=$CODENAME
      go-package: go-1.26

update:
  enabled: true
  github:
    identifier: traefik/traefik
    strip-prefix: v
    tag-filter-prefix: v3.7.

test:
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/tw/ver-check
      with:
        bins: traefik
        version: ${{package.version}}
    - uses: test/tw/help-check
      with:
        bins: traefik
    - name: Run and test traefik
      uses: test/daemon-check-output
      with:
        start: traefik --ping=true --ping.entryPoint=traefik --api.insecure=true --log.level=DEBUG
        timeout: 30
        expected_output: |
          Creating middleware
          Setting up redirection
