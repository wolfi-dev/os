package:
  name: tree
  version: "2.3.0"
  epoch: 0
  description: A recursive directory indented listing of files
  copyright:
    - license: GPL-2.0-or-later

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle

pipeline:
  - uses: fetch
    with:
      expected-sha256: 0636eadf7f0fee4d4714272c3bda7bb24031f288355ca6629d3865d208e6d14f
      uri: https://gitlab.com/OldManProgrammer/unix-tree/-/archive/${{package.version}}/unix-tree-${{package.version}}.tar.gz

  - uses: autoconf/make

  - runs: |
      make PREFIX="${{targets.destdir}}/usr" \
        MANDIR="${{targets.destdir}}/usr/share/man" \
        install

  - uses: strip

subpackages:
  - name: tree-doc
    pipeline:
      - uses: split/manpages
    description: tree manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 5006

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        tree --version
        tree --help
