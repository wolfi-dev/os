package:
  name: trurl
  version: "0.12"
  epoch: 1
  description: 'trurl is a command line tool for URL parsing and manipulation.'
  copyright:
    - license: curl

environment:
  contents:
    packages:
      - build-base
      - busybox
      - curl-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/curl/trurl
      tag: trurl-0.10
      expected-commit: 43b02a40ab9b2fddc77382a924278a7fd6e6c7db

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: "trurl-doc"
    description: "documentation for trurl"
    pipeline:
      - uses: split/manpages

update:
  enabled: true
  version-separator: _
  github:
    identifier: curl/trurl
    strip-prefix: trurl-

test:
  pipeline:
    - runs: |
        trurl --version
        trurl --url https://curl.se/we/are.html --get '{port}'
