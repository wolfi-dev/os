# Generated from https://git.alpinelinux.org/aports/plain/community/ugrep/APKBUILD
package:
  name: ugrep
  version: 7.1.2
  epoch: 0
  description: Ultra fast grep with interactive query UI and fuzzy search
  copyright:
    - license: BSD-3-Clause

environment:
  contents:
    packages:
      - autoconf
      - automake
      - brotli-dev
      - build-base
      - busybox
      - bzip2-dev
      - ca-certificates-bundle
      - libbzip3
      - lz4-dev
      - openssf-compiler-options
      - pcre2-dev
      - xz-dev
      - zlib-dev
      - zstd-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/Genivia/ugrep
      expected-commit: 7df8274d9d3cd1ddd1ec8a04920d576a8d8203d9
      tag: v${{package.version}}

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: ugrep-doc
    pipeline:
      - uses: split/manpages
    description: ugrep manpages

update:
  enabled: true
  github:
    identifier: Genivia/ugrep
    strip-prefix: v

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        ug --version
        ugrep --version
        ugrep-indexer --version
        ugrep-indexer --help
