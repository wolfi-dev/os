# Generated from https://git.alpinelinux.org/aports/plain/community/ugrep/APKBUILD
package:
  name: ugrep
  version: "7.8.2"
  epoch: 0 # go/wolfi-rsc/ugrep
  description: Ultra fast grep with interactive query UI and fuzzy search
  copyright:
    - license: BSD-3-Clause
  resources:
    cpu: "1"
    memory: 4Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - brotli-dev
      - build-base
      - busybox
      - bzip2-dev
      - ca-certificates-bundle
      - libbzip3
      - lz4-dev
      - pcre2-dev
      - xz-dev
      - zlib-dev
      - zstd-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/Genivia/ugrep
      expected-commit: d71b793556d5bf29f9e67b0e28ce4b23d90ba5e7
      tag: v${{package.version}}

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: ugrep-doc
    pipeline:
      - uses: split/manpages
    description: ugrep manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  github:
    identifier: Genivia/ugrep
    strip-prefix: v

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        ug --version
        ugrep --version
        ugrep-indexer --version
        ugrep-indexer --help
