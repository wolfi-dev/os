package:
  name: up
  version: 0.34.2
  epoch: 1
  description: The Upbound CLI
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - bash
      - build-base
      - busybox
      - go
  environment:
    CGO_ENABLED: 0

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/upbound/up
      tag: v${{package.version}}
      expected-commit: 489ad4b4d871d0915daa8ed5fa66baf87fb18528

  - uses: go/bump
    with:
      deps: oras.land/oras-go@v1.2.5 helm.sh/helm/v3@v3.14.2 google.golang.org/protobuf@v1.33.0 golang.org/x/net@v0.23.0 github.com/Azure/azure-sdk-for-go/sdk/azidentity@v1.6.0 github.com/docker/docker@v26.1.5 golang.org/x/crypto@v0.31.0
      replaces: github.com/docker/cli=github.com/docker/cli@v25.0.5+incompatible github.com/docker/docker=github.com/docker/docker@v26.1.5+incompatible oras.land/oras-go=oras.land/oras-go@v1.2.5
      show-diff: true

  - runs: |
      make submodules
      make install

      # This installs `up` into `$GOPATH/bin/up`, but we want it in `/usr/bin/up`.
      mkdir -p "${{targets.destdir}}"/usr/bin
      install -Dm755 $(go env GOPATH)/bin/up "${{targets.destdir}}"/usr/bin/up

  - uses: strip

update:
  enabled: true
  github:
    identifier: upbound/up
    strip-prefix: v

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        up --help
        up version
