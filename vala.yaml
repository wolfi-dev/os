package:
  name: vala
  version: 0.56.8
  epoch: 0
  description: Compiler for the GObject type system
  copyright:
    - license: LGPL-2.0-or-later

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - build-base
      - automake
      - autoconf
      - libxslt-dev
      - bash
      - flex
      - bison
      - gobject-introspection-dev
      - graphviz-dev
      - glib-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 93f81dcfc6a93b77baa271d65e6be981ee3238ad451ef380af118e295d904bde
      uri: https://download.gnome.org/sources/vala/0.56/vala-${{package.version}}.tar.xz

  - uses: autoconf/configure
    with:
      opts: |
        --host=${{host.triplet.gnu}} \
        --build=${{host.triplet.gnu}} \

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: vala-doc
    pipeline:
      - uses: split/manpages
    description: vala manpages

update:
  enabled: true
  release-monitor:
    identifier: 5065
