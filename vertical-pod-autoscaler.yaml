package:
  name: vertical-pod-autoscaler
  version: "1.6.0"
  epoch: 2
  description: Autoscaling components for Kubernetes
  copyright:
    - license: Apache-2.0

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/kubernetes/autoscaler
      tag: vertical-pod-autoscaler-${{package.version}}
      expected-commit: 9196162ba12e637ce4482107e1c17bcb80599050

  - uses: go/bump
    with:
      deps: |-
        golang.org/x/net@v0.51.0
      modroot: vertical-pod-autoscaler

  - uses: go/bump
    with:
      deps: |-
        golang.org/x/net@v0.51.0
      modroot: vertical-pod-autoscaler/e2e

  - uses: go/bump
    with:
      deps: |-
        golang.org/x/net@v0.51.0
      modroot: cluster-autoscaler/hack/genproto

  - uses: go/bump
    with:
      deps: |-
        golang.org/x/net@v0.51.0
      modroot: cluster-autoscaler

  - uses: go/bump
    with:
      deps: |-
        golang.org/x/net@v0.51.0
      modroot: cluster-autoscaler/e2e

  - uses: go/bump
    with:
      deps: |-
        golang.org/x/net@v0.51.0
      modroot: cluster-autoscaler/cloudprovider/azure/test

  - uses: go/bump
    with:
      deps: |-
        golang.org/x/net@v0.51.0
      modroot: cluster-autoscaler/apis

  - uses: go/bump
    with:
      deps: |-
        golang.org/x/net@v0.51.0
      modroot: balancer

  - uses: go/build
    with:
      modroot: vertical-pod-autoscaler
      packages: ./pkg/admission-controller
      output: admission-controller

subpackages:
  - name: vertical-pod-autoscaler-updater
    pipeline:
      - uses: go/build
        with:
          modroot: vertical-pod-autoscaler
          packages: ./pkg/updater
          output: updater

  - name: vertical-pod-autoscaler-recommender
    pipeline:
      - uses: go/build
        with:
          modroot: vertical-pod-autoscaler
          packages: ./pkg/recommender
          output: recommender
          vendor: "true"

update:
  enabled: true
  github:
    identifier: kubernetes/autoscaler
    strip-prefix: vertical-pod-autoscaler-
    use-tag: true
    tag-filter: vertical-pod-autoscaler-

test:
  environment:
    contents:
      packages:
        - vertical-pod-autoscaler-updater
        - vertical-pod-autoscaler-recommender
  pipeline:
    - runs: |
        for component in admission-controller updater recommender; do
          $component -h 2>&1 | grep -q "Usage of $component"
          $component 2>&1 | grep -q "unable to load in-cluster configuration"
        done

environment:
  contents:
    packages:
      - go-1.25
