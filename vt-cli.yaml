package:
  name: vt-cli
  version: "1.1.1"
  epoch: 0
  description: "VirusTotal Command Line Interface"
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - go

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/VirusTotal/vt-cli
      tag: ${{package.version}}
      expected-commit: 8f6f0fe6526e96f01237b2b7db464131044b3c70

  - uses: go/build
    with:
      packages: ./vt/
      output: vt
      ldflags: -X github.com/VirusTotal/vt-cli/cmd.Version=${{package.version}}

update:
  enabled: true
  github:
    identifier: VirusTotal/vt-cli
    use-tag: true

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        vt --help
        vt version
