package:
  name: wait-for-port
  version: "1.0.9"
  epoch: 0
  description: CLI tool for waiting until a TCP port reaches the desired state
  copyright:
    - license: Apache-2.0

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/bitnami/wait-for-port
      tag: v${{package.version}}
      expected-commit: 90c6562f5b8b366f5a6019b810c0a7b6f6f825f3

  - uses: go/build
    with:
      packages: .
      output: wait-for-port

update:
  enabled: true
  github:
    identifier: bitnami/wait-for-port
    strip-prefix: v
    use-tag: true

test:
  environment:
    contents:
      packages:
        - netcat-openbsd
  pipeline:
    - runs: |
        wait-for-port --help 2>&1 | grep wait-for-port
        nc -l -p 2568 > /dev/null 2>&1 &
        wait-for-port 2568
