package:
  name: wait-for-port
  version: "1.0.10"
  epoch: 3 # CVE-2025-61725
  description: CLI tool for waiting until a TCP port reaches the desired state
  copyright:
    - license: Apache-2.0

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/bitnami/wait-for-port
      tag: v${{package.version}}
      expected-commit: 55a31d45febe69c337fd7e5a16ebd0ad749482d2

  - uses: go/build
    with:
      packages: .
      output: wait-for-port

update:
  enabled: true
  github:
    identifier: bitnami/wait-for-port
    strip-prefix: v
    use-tag: true

test:
  environment:
    contents:
      packages:
        - netcat-openbsd
  pipeline:
    - runs: |
        wait-for-port --help 2>&1 | grep wait-for-port
        nc -l -p 2568 > /dev/null 2>&1 &
        wait-for-port 2568
