package:
  name: wasi-sdk
  version: "25"
  epoch: 1
  description: "WASI-enabled WebAssembly C/C++ toolchain"
  copyright:
    - license: Apache-2.0
  # Ships custom clang
  options:
    no-commands: true

vars:
  llvm-vers: 19

environment:
  contents:
    packages:
      - bash
      - build-base
      - busybox
      - ca-certificates-bundle
      - clang-${{vars.llvm-vers}}
      - cmake
      - llvm-${{vars.llvm-vers}}
      - perl
      - python3
      - rust
      - samurai
      - wolfi-baselayout

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/WebAssembly/wasi-sdk
      tag: wasi-sdk-${{package.version}}
      expected-commit: ccdf52e17eec09577e7e25acef96c4d630bfb8d7
      recurse-submodules: true

  - name: Build toolchain
    runs: |
      cmake -G Ninja -B build/toolchain -S . -DWASI_SDK_BUILD_TOOLCHAIN=ON -DCMAKE_INSTALL_PREFIX=build/install
      cmake --build build/toolchain --target install

  - name: Build sysroot
    runs: |
      export CFLAGS="-O2 -Wall -fno-omit-frame-pointer -mno-omit-leaf-frame-pointer"
      export CXXFLAGS="$CFLAGS"
      export LDFLAGS="-Wl,-z,stack-size=8192"

      cmake -G Ninja -B build/sysroot -S . \
          -DCMAKE_INSTALL_PREFIX=build/install \
          -DCMAKE_TOOLCHAIN_FILE=build/install/share/cmake/wasi-sdk.cmake \
          -DCMAKE_C_COMPILER_WORKS=ON \
          -DCMAKE_CXX_COMPILER_WORKS=ON
      cmake --build build/toolchain --target dist
      cmake --build build/sysroot --target dist

  - name: Merge artifacts to buiild SDK
    runs: |
      mkdir dist-wolfi
      cp build/toolchain/dist/* build/sysroot/dist/* dist-wolfi
      ./ci/merge-artifacts.sh

  - name: Install SDK
    runs: |
      mkdir -p ${{targets.destdir}}/usr
      tar -xvf dist/wasi-sdk-*.tar.gz
      mv wasi-sdk-*/* ${{targets.destdir}}/usr/

  - uses: strip

subpackages:
  # This has to come before the split/dev step
  - name: wasi-sdk-libclang-rt-builtins
    pipeline:
      - runs: |
          mkdir -p ${{targets.subpkgdir}}/usr/lib/clang/lib/wasi
          # Copy instead of move because this also needs to be in the -dev package
          cp ${{targets.destdir}}/usr/lib/clang/${{vars.llvm-vers}}/lib/wasi/libclang_rt.builtins-wasm32.a ${{targets.subpkgdir}}/usr/lib/clang/lib/wasi/

  - name: wasi-sdk-sysroot
    pipeline:
      - runs: |
          mkdir -p ${{targets.subpkgdir}}/usr/share
          mv ${{targets.destdir}}/usr/share/wasi-sysroot/ ${{targets.subpkgdir}}/usr/share/
    test:
      pipeline:
        - uses: test/tw/ldd-check

  - name: wasi-sdk-dev
    # Ships custom clang
    options:
      no-commands: true
    pipeline:
      - uses: split/dev
    test:
      pipeline:
        - runs: |
            llvm-config --version
            llvm-config --help
        - uses: test/tw/ldd-check

update:
  enabled: true
  github:
    identifier: WebAssembly/wasi-sdk
    strip-prefix: wasi-sdk-
    use-tag: true

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        ar --version
        c++filt --version
        clang --version
        clang++ --version
        clang-${{vars.llvm-vers}} --version
        clang-apply-replacements --version
        clang-cl --version
        clang-cpp --version
        clang-format --version
        clang-tidy --version
        ld.lld --version
        ld64.lld --version
        lld-link --version
        llvm-ar --version
        llvm-cxxfilt --version
        llvm-dwarfdump --version
        llvm-mc --version
        llvm-nm --version
        llvm-objcopy --version
        llvm-objdump --version
        llvm-ranlib --version
        llvm-size --version
        llvm-strings --version
        llvm-strip --version
        nm --version
        objcopy --version
        objdump --version
        ranlib --version
        size --version
        strings --version
        strip --version
        wasm-component-ld --version
        wasm-ld --version
        wasm32-wasi-clang --version
        wasm32-wasi-clang++ --version
        wasm32-wasi-threads-clang --version
        wasm32-wasi-threads-clang++ --version
        wasm32-wasip1-clang --version
        wasm32-wasip1-clang++ --version
        wasm32-wasip1-threads-clang --version
        wasm32-wasip1-threads-clang++ --version
        wasm32-wasip2-clang --version
        wasm32-wasip2-clang++ --version
        ar --help
        c++filt --help
        clang --help
        clang++ --help
        clang-${{vars.llvm-vers}} --help
        clang-apply-replacements --help
        clang-cpp --help
        clang-format --help
        clang-tidy --help
        ld.lld --help
        ld64.lld --help
        llvm-ar --help
        llvm-cxxfilt --help
        llvm-dwarfdump --help
        llvm-mc --help
        llvm-nm --help
        llvm-objcopy --help
        llvm-objdump --help
        llvm-ranlib --help
        llvm-size --help
        llvm-strings --help
        llvm-strip --help
        nm --help
        objcopy --help
        objdump --help
        ranlib --help
        size --help
        strings --help
        strip --help
        wasm-component-ld --help
        wasm-ld --help
        wasm32-wasi-clang --help
        wasm32-wasi-clang++ --help
        wasm32-wasi-threads-clang --help
        wasm32-wasi-threads-clang++ --help
        wasm32-wasip1-clang --help
        wasm32-wasip1-clang++ --help
        wasm32-wasip1-threads-clang --help
        wasm32-wasip1-threads-clang++ --help
        wasm32-wasip2-clang --help
        wasm32-wasip2-clang++ --help
