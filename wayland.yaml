package:
  name: wayland
  version: 1.22.0
  epoch: 0
  description: A computer display server protocol
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - build-base
      - libffi-dev
      - expat-dev
      - libxml2-dev
      - meson

pipeline:
  - uses: fetch
    with:
      expected-sha256: 1540af1ea698a471c2d8e9d288332c7e0fd360c8f1d12936ebb7e7cbc2425842
      uri: https://gitlab.freedesktop.org/wayland/wayland/-/releases/${{package.version}}/downloads/wayland-${{package.version}}.tar.xz

  - uses: meson/configure
    with:
      opts: |
        -Db_lto=true \
        -Ddocumentation=false

  - uses: meson/compile

  - uses: meson/install

  - uses: strip

data:
  - name: libs
    items:
      client:
      egl:
      cursor:
      server:

subpackages:
  - name: wayland-dev
    pipeline:
      - uses: split/dev
      - runs: |
          mkdir -p ${{targets.contextdir}}/usr/share
          mv ${{targets.contextdir}}/usr/bin ${{targets.contextdir}}/usr/
          mv ${{targets.contextdir}}/usr/share/* ${{targets.contextdir}}/usr/share/
    dependencies:
      runtime:
        - wayland
        - libffi-dev
        - expat-dev
        - wayland-libs-client
        - wayland-libs-cursor
        - wayland-libs-egl
        - wayland-libs-server
    description: wayland dev

  - range: libs
    name: wayland-libs-${{range.key}}
    pipeline:
      - runs: |
          mkdir -p ${{targets.contextdir}}/usr/lib
          mv ${{targets.contextdir}}/usr/lib/libwayland-${{range.key}}.so.* ${{targets.contextdir}}/usr/lib/
    description: wayland ${{range.key}} libs

update:
  enabled: true
  release-monitor:
    identifier: 10061
