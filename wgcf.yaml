package:
  name: wgcf
  version: "2.3.0"
  epoch: 0 # toolchain rebuild for  go-1.27-1.27.1
  description: Cross-platform, unofficial CLI for Cloudflare Warp
  copyright:
    - license: MIT
  resources:
    cpu: "1"
    memory: 4Gi
  test-resources:
    cpu: "1"
    memory: 1Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - go-1.27

pipeline:
  - uses: git-checkout
    with:
      expected-commit: ace873cbaa618365beebde5790a7fb3481e5a211
      repository: https://github.com/ViRb3/wgcf
      tag: v${{package.version}}

  - uses: go/build
    with:
      output: wgcf
      packages: .
      go-package: go-1.27

  - uses: strip

update:
  enabled: true
  github:
    identifier: ViRb3/wgcf
    strip-prefix: v

test:
  environment:
    contents:
      packages:
        - ca-certificates-bundle
  pipeline:
    - uses: test/tw/ldd-check
    - name: "Run wgcf"
      runs: |
        wgcf || exit 1
        wgcf --help
    - name: "Test wgcf generate"
      runs: |
        (wgcf generate 2>&1 || exit 0) | grep "register one first"
