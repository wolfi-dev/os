package:
  name: wit-bindgen
  version: "0.54.0"
  epoch: 1 # go/wolfi-rsc/wit-bindgen
  description: "A language binding generator for WebAssembly interface types"
  copyright:
    - license: Apache-2.0
  resources:
    cpu: "5"
    memory: 10Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - cargo-auditable
      - rust-1.94
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/bytecodealliance/wit-bindgen
      tag: v${{package.version}}
      expected-commit: b858f8e0bfacf371dee1bbf7097b3022339feb93

  - uses: rust/cargobump

  - uses: cargo/build
    with:
      output: wit-bindgen

  - uses: strip

update:
  enabled: true
  ignore-regex-patterns:
    - wit-bindgen-cli-*
  github:
    identifier: bytecodealliance/wit-bindgen
    strip-prefix: v

test:
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/tw/ver-check
      with:
        bins: wit-bindgen
    - uses: test/tw/help-check
      with:
        bins: wit-bindgen
    - name: Generate C bindings from a WIT file
      runs: |
        mkdir -p /tmp/wit-test/wit
        cat > /tmp/wit-test/wit/world.wit << 'EOF'
        package example:hello;

        world hello {
          export greet: func(name: string) -> string;
        }
        EOF
        wit-bindgen c /tmp/wit-test/wit --world hello --out-dir /tmp/wit-out
        test -f /tmp/wit-out/hello.c
        test -f /tmp/wit-out/hello.h
        grep -q "greet" /tmp/wit-out/hello.h
