# Generated from https://git.alpinelinux.org/aports/plain/community/xdg-utils/APKBUILD
package:
  name: xdg-utils
  version: 1.2.1
  epoch: 1
  description: Basic desktop integration functions
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - autoconf
      - automake
      - bash
      - build-base
      - busybox
      - ca-certificates-bundle
      - docbook-xml
      - file
      - libxml2-utils
      - libxslt
      - lynx
      - xmlto
      - xprop
      - xset

pipeline:
  - uses: fetch
    with:
      expected-sha256: f6b648c064464c2636884c05746e80428110a576f8daacf46ef2e554dcfdae75
      uri: https://gitlab.freedesktop.org/xdg/xdg-utils/-/archive/v${{package.version}}/xdg-utils-v${{package.version}}.tar.gz

  - runs: autoreconf

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: xdg-utils-doc
    pipeline:
      - uses: split/manpages
    description: xdg-utils manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 5176

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        xdg-desktop-icon --version
        xdg-desktop-menu --version
        xdg-email --version
        xdg-icon-resource --version
        xdg-mime --version
        xdg-open --version
        xdg-screensaver --version
        xdg-settings --version
        xdg-desktop-icon --help
        xdg-desktop-menu --help
        xdg-email --help
        xdg-icon-resource --help
        xdg-mime --help
        xdg-open --help
        xdg-screensaver --help
        xdg-settings --help
