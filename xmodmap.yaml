# Generated from https://git.alpinelinux.org/aports/plain/community/xmodmap/APKBUILD
package:
  name: xmodmap
  version: 1.0.11
  epoch: 1
  description: Utility for modifying keymaps and pointer button mappings in X
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libx11-dev
      - util-macros

pipeline:
  - uses: fetch
    with:
      expected-sha256: c4fac9df448b98ac5a1620f364e74ed5f7084baae0d09123700f34d4b63cb5d8
      uri: https://www.x.org/releases/individual/app/xmodmap-${{package.version}}.tar.gz

  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: xmodmap-doc
    pipeline:
      - uses: split/manpages
    description: xmodmap manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 14960

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        xmodmap -h
