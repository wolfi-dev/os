# Generated from https://git.alpinelinux.org/aports/plain/community/xrdb/APKBUILD
package:
  name: xrdb
  version: "1.2.3"
  epoch: 0 # go/wolfi-rsc/xrdb
  description: X server resource database utility
  copyright:
    - license: MIT
  resources:
    cpu: "1"
    memory: 5Gi

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libx11-dev
      - libxmu-dev
      - pkgconf-dev
      - util-macros-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://gitlab.freedesktop.org/xorg/app/xrdb
      tag: xrdb-${{package.version}}
      expected-commit: aaf6f3df9377a44081af49a7fb2ae14aa3a18e60

  # xrdb does not support --enable-fuse, --disable-pam, or --enable-tjpeg;
  # those options were incorrectly copied from other packages and are not
  # valid configure flags for this utility.
  - uses: autoconf/configure

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: xrdb-doc
    pipeline:
      - uses: split/manpages
    description: xrdb manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 14956

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        xrdb -v
    - uses: test/no-docs
