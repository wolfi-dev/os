# Generated from https://git.alpinelinux.org/aports/plain/community/xrdb/APKBUILD
package:
  name: xrdb
  version: 1.2.2
  epoch: 3
  description: X server resource database utility
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libx11-dev
      - libxmu-dev
      - pkgconf-dev
      - util-macros

pipeline:
  - uses: git-checkout
    with:
      repository: https://gitlab.freedesktop.org/xorg/app/xrdb
      tag: xrdb-${{package.version}}
      expected-commit: ff688ceacaddb8e2f345caadfe33e408d97782a0

  - uses: autoconf/configure
    with:
      opts: |
        --enable-fuse \
        --disable-pam \
        --enable-tjpeg

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: xrdb-doc
    pipeline:
      - uses: split/manpages
    description: xrdb manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 14956

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        xrdb -v
    - uses: test/no-docs
