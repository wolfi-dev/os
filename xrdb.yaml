# Generated from https://git.alpinelinux.org/aports/plain/community/xrdb/APKBUILD
package:
  name: xrdb
  version: 1.2.2
  epoch: 1
  description: X server resource database utility
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - autoconf
      - automake
      - build-base
      - busybox
      - ca-certificates-bundle
      - libx11-dev
      - libxmu-dev
      - util-macros

pipeline:
  - uses: fetch
    with:
      expected-sha256: 31f5fcab231b38f255b00b066cf7ea3b496df712c9eb2d0d50c670b63e5033f4
      uri: https://www.x.org/releases/individual/app/xrdb-${{package.version}}.tar.xz

  - uses: autoconf/configure
    with:
      opts: |
        --enable-fuse \
        --disable-pam \
        --enable-tjpeg

  - uses: autoconf/make

  - uses: autoconf/make-install

  - uses: strip

subpackages:
  - name: xrdb-doc
    pipeline:
      - uses: split/manpages
    description: xrdb manpages
    test:
      pipeline:
        - uses: test/docs

update:
  enabled: true
  release-monitor:
    identifier: 14956

test:
  pipeline:
    # AUTOGENERATED
    - runs: |
        xrdb -v
