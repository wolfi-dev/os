package:
  name: yaml-cpp
  version: 0.8.0
  epoch: 5
  description: "yaml-cpp is a YAML parser and emitter in C++ matching the YAML 1.2 spec."
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - openssf-compiler-options
      - wolfi-base

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/jbeder/yaml-cpp
      expected-commit: f7320141120f720aecc4c32be25586e7da9eb978
      tag: ${{package.version}}

  - name: configure for static library build
    uses: cmake/configure
    with:
      opts: |
        -DCMAKE_INSTALL_PREFIX=/usr \
        -DCMAKE_INSTALL_LIBDIR=lib \
        -DCMAKE_BUILD_TYPE=Release \
        -DCMAKE_POSITION_INDEPENDENT_CODE:BOOL=true \
        -DYAML_BUILD_SHARED_LIBS=OFF \
        -DYAML_CPP_BUILD_TESTS=OFF \
        -DYAML_CPP_BUILD_TOOLS=OFF

  - name: static library build
    uses: cmake/build

  - name: static library install
    uses: cmake/install

  - name: configure for shared library build
    uses: cmake/configure
    with:
      opts: |
        -DCMAKE_INSTALL_PREFIX=/usr \
        -DCMAKE_INSTALL_LIBDIR=lib \
        -DCMAKE_BUILD_TYPE=Release \
        -DCMAKE_POSITION_INDEPENDENT_CODE:BOOL=true \
        -DYAML_BUILD_SHARED_LIBS=ON \
        -DYAML_CPP_BUILD_TESTS=OFF \
        -DYAML_CPP_BUILD_TOOLS=OFF

  - name: shared library build
    uses: cmake/build

  - name: shared library install
    uses: cmake/install

  - uses: strip

subpackages:
  - name: "yaml-cpp-dev"
    description: "development files for yaml-cpp"
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - yaml-cpp
    test:
      pipeline:
        - uses: test/pkgconf

update:
  enabled: true
  github:
    identifier: jbeder/yaml-cpp
    tag-filter: 0.
