packages:
  - name: url
    version: 2.5.4
  - name: tokio
    version: 1.38.2
  - name: bytes
    version: 1.11.1
  - name: time
    version: 0.3.47
  - name: rustls-webpki
    version: 0.103.13
  # GHSA-cq8v-f236-94qc (low): rand unsoundness with custom logger using rand::rng().
  # Two copies of rand are in the lockfile; the 0.8.x line is patched in 0.8.6
  # and the 0.9.x line is patched in 0.9.3. Use name@version for both copies so
  # each entry unambiguously targets a single copy.
  - name: rand@0.8.5
    version: 0.8.6
  - name: rand@0.9.0
    version: 0.9.3
